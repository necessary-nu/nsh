target
corpus
# The campaign corpus is derived from corpus/ by `fuzz/run.sh --derive`, so
# it is reproducible from the archive and belongs no more in git than the
# archive does.
campaign
artifacts
coverage

# cargo-fuzz's -jobs supervisor writes one worker log per job here.
fuzz-*.log
