#!name gen0
#!mode=file
B0=$PWD
set -- -- -b -a x; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set --; echo "$#"
%%%
#!name gen2
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
env | grep -c '^PPID='
g() { local A B; A='POSIX'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen3
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
env | grep -c '^LC_NUMERIC='
set -- -b -b; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen4
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen5
#!mode=file
B0=$PWD
g() { local IFS; IFS='.'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -- -ab -c; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
env | grep -c '^PS4='
%%%
#!name gen6
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
alias echo='echo pre'; echo hi
%%%
#!name gen7
#!mode=file
B0=$PWD
set +o nounset; echo "$-"
set +o | md5sum
read v <<E
value
E
echo "[$v]"
%%%
#!name gen8
#!mode=file
B0=$PWD
set --; echo "$#"
unset LC_ALL
%%%
#!name gen9
#!mode=file
B0=$PWD
export -p | grep -c 'MAIL' 
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
set --; echo "$#"
%%%
#!name gen10
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -o | md5sum
%%%
#!name gen11
#!mode=file
B0=$PWD
set -- -a -b; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
g() { local B B; B='a'b'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen12
#!mode=file
B0=$PWD
trap ALRM 2>&1; trap; echo rc=$?
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen13
#!mode=file
B0=$PWD
g() { local A='x=y'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -- x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local IFS B; IFS='1'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen14
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen15
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
V1='0'
%%%
#!name gen16
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen17
#!mode=file
B0=$PWD
set --; echo "$#"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen18
#!mode=file
B0=$PWD
trap '' INT 2>&1; trap; echo rc=$?
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen19
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen20
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen21
#!mode=file
B0=$PWD
LC_ALL='1'
exec 6<<E
fromfd
E
read v <&6; echo "[$v]"; exec 6<&-
%%%
#!name gen22
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
%%%
#!name gen23
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen24
#!mode=file
B0=$PWD
set -o | md5sum
trap 'echo X' 1 2>&1; (trap); echo ---; trap
%%%
#!name gen25
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen26
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set | grep -c '^PS2='
%%%
#!name gen27
#!mode=file
B0=$PWD
set -i; echo "$-"
{ echo p; } 6>&- ; echo rc=$?
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen28
#!mode=file
B0=$PWD
set -- x -a x -z; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen29
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen30
#!mode=file
B0=$PWD
set -- -bvalue -ab -bvalue; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset -v PPID
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen31
#!mode=file
B0=$PWD
trap PIPE 2>&1; trap; echo rc=$?
trap 'echo X' 0 2>&1; (trap); echo ---; trap
%%%
#!name gen32
#!mode=file
B0=$PWD
g() { local -; set -u; echo "in:$-"; }; g; echo "out:$-"
set +o nolog; echo "$-"
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
%%%
#!name gen33
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen34
#!mode=file
B0=$PWD
set | grep -c '^OLDPWD='
%%%
#!name gen35
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
exec 4<&-; read v <&4 2>&1; echo rc=$?
%%%
#!name gen36
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen37
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen38
#!mode=file
B0=$PWD
g() { local -; set -x; echo "in:$-"; }; g; echo "out:$-"
trap '' INT 2>&1; trap; echo rc=$?
exec 4<<E
fromfd
E
read v <&4; echo "[$v]"; exec 4<&-
%%%
#!name gen39
#!mode=file
B0=$PWD
set -- -ab; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export LANG
%%%
#!name gen40
#!mode=file
B0=$PWD
set -- a 1 a b; echo "$#:$*"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen41
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -; echo "$-"
set -- -a -b; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen42
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen43
#!mode=file
B0=$PWD
set -- -b; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -z; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen44
#!mode=file
B0=$PWD
trap 'echo T' INT 2>&1; trap; echo rc=$?
g() { local IFS B; IFS=':'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -- -c; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen45
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen46
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen47
#!mode=file
B0=$PWD
g() { local OPTIND='a'b'; OPTIND='a'b'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
g() { local IFS B; IFS='abc'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen48
#!mode=file
B0=$PWD
trap '' INT 2>&1; trap; echo rc=$?
set -- -bvalue; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen49
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen50
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen51
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen52
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen53
#!mode=file
B0=$PWD
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
read v <<E
value
E
echo "[$v]"
vv='C'
%%%
#!name gen54
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen55
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='1'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
set -- -a x; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen56
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
%%%
#!name gen57
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -- -c x -a x; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen58
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen59
#!mode=file
B0=$PWD
env | grep -c '^OPTIND='
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen60
#!mode=file
B0=$PWD
unset -v LINENO
g() { local V1='a'b'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen61
#!mode=file
B0=$PWD
trap '' USR1 2>&1; trap; echo rc=$?
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
g() { local vv; vv='a b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen62
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen63
#!mode=file
B0=$PWD
readonly OLDPWD='\t'
g() { local B B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen64
#!mode=file
B0=$PWD
set -ee; echo "$-"
%%%
#!name gen65
#!mode=file
B0=$PWD
unset -v LC_ALL
%%%
#!name gen66
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
%%%
#!name gen67
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen68
#!mode=file
B0=$PWD
set -- -b -a -b -a -b; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local -; set +e; echo "in:$-"; }; g; echo "out:$-"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen69
#!mode=file
B0=$PWD
trap 'echo T' INT 2>&1; trap; echo rc=$?
alias echo='echo pre'; echo hi
cat <<'E'
$notexpanded
E
%%%
#!name gen70
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set +f; echo "$-"
%%%
#!name gen71
#!mode=file
B0=$PWD
set -bv; echo "$-"
unset -v HISTSIZE
%%%
#!name gen72
#!mode=file
B0=$PWD
export B='abc'
set -a; echo "$-"
trap '' 0 2>&1; trap; echo rc=$?
%%%
#!name gen73
#!mode=file
B0=$PWD
set -- b ; echo "$#:$*"
set -m; echo "$-"
%%%
#!name gen74
#!mode=file
B0=$PWD
trap '' PIPE 2>&1; trap; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen75
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen76
#!mode=file
B0=$PWD
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local IFS='/tmp'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
g() { local -; set -o xtrace; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen77
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local IFS='0'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -- a b c d e; shift 0 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen78
#!mode=file
B0=$PWD
set | grep -c '^A='
set -- -z -ab -z; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen79
#!mode=file
B0=$PWD
set -- -bvalue -a -b; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen80
#!mode=file
B0=$PWD
set -- -bvalue -a; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen81
#!mode=file
B0=$PWD
g() { local A B; A='.'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen82
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen83
#!mode=file
B0=$PWD
set -- -bvalue -c -a -b; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -- -c; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen84
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
g() { local OPTIND; OPTIND='\t'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen85
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
echo "[${PS2-U}]"
%%%
#!name gen86
#!mode=file
B0=$PWD
PS1='/tmp'
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen87
#!mode=file
B0=$PWD
set +E; echo "$-"
%%%
#!name gen88
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
echo "[${PS4-U}]"
%%%
#!name gen89
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen90
#!mode=file
B0=$PWD
set -- -bvalue; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo A' QUIT 2>&1; trap - QUIT 2>&1; trap; echo rc=$?
%%%
#!name gen91
#!mode=file
B0=$PWD
set +o emacs; echo "$-"
set -- x -bvalue; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias echo='echo pre'; echo hi
%%%
#!name gen92
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen93
#!mode=file
B0=$PWD
set -- -a --; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen94
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen95
#!mode=file
B0=$PWD
set -- -c -a; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -bvalue -a -z; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen96
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen97
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
trap 'echo X' 1 2>&1; (trap); echo ---; trap
LC_CTYPE='0'
%%%
#!name gen98
#!mode=file
B0=$PWD
g() { local B='C'; B='C'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen99
#!mode=file
B0=$PWD
unset -v PWD
set -- -a -b -ab -b; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local -; set -o nounset; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen100
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen101
#!mode=file
B0=$PWD
export -p | grep -c 'LINENO' 
%%%
#!name gen102
#!mode=file
B0=$PWD
echo "[${OLDPWD#$B0}]"
set -- -bvalue; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen103
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
set -- -c -ab -c; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen104
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- 1 b c; echo "$#:$*"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen105
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen106
#!mode=file
B0=$PWD
trap HUP 2>&1; trap; echo rc=$?
trap 'echo X' 12 2>&1; (trap); echo ---; trap
set -a; echo "$-"
%%%
#!name gen107
#!mode=file
B0=$PWD
set -; echo "$-"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
trap 'echo T' INT 2>&1; trap; echo rc=$?
%%%
#!name gen108
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- x -bvalue -bvalue; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen109
#!mode=file
B0=$PWD
set -x; echo "$-"
trap 'echo X' 1 2>&1; (trap); echo ---; trap
%%%
#!name gen110
#!mode=file
B0=$PWD
exec 9<&-; read v <&9 2>&1; echo rc=$?
exec 9>r9.txt; echo hi >&9; exec 9>&-; cat r9.txt
%%%
#!name gen111
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen112
#!mode=file
B0=$PWD
set | grep -c '^LINENO='
set -; echo "$-"
set -; echo "$-"
%%%
#!name gen113
#!mode=file
B0=$PWD
exec 9<&-; read v <&9 2>&1; echo rc=$?
%%%
#!name gen114
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen115
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
g() { local IFS B; IFS='abc'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen116
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen117
#!mode=file
B0=$PWD
readonly PS4='/tmp'
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
set +o | md5sum
%%%
#!name gen118
#!mode=file
B0=$PWD
trap 'echo A' USR2 2>&1; trap - USR2 2>&1; trap; echo rc=$?
%%%
#!name gen119
#!mode=file
B0=$PWD
unset MAIL
export -p | grep -c 'V1' 
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen120
#!mode=file
B0=$PWD
trap '' CHLD 2>&1; trap; echo rc=$?
export -p | grep -c 'IFS' 
%%%
#!name gen121
#!mode=file
B0=$PWD
set --; echo "$#"
cd /nope 2>&1; echo rc=$?
g() { local B='/tmp'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen122
#!mode=file
B0=$PWD
set -- -bvalue -a x; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen123
#!mode=file
B0=$PWD
set +o xtrace; echo "$-"
set -- -a -a -a; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<E
line1
line2
E
%%%
#!name gen124
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
g() { local -; set +I; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen125
#!mode=file
B0=$PWD
g() { local OPTIND='/tmp'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen126
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen127
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set --; echo "$#"
%%%
#!name gen128
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen129
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
export OLDPWD='abc'
exec 4<<E
fromfd
E
read v <&4; echo "[$v]"; exec 4<&-
%%%
#!name gen130
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen131
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen132
#!mode=file
B0=$PWD
trap '' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen133
#!mode=file
B0=$PWD
trap 'echo X' QUIT 2>&1; (trap); echo ---; trap
%%%
#!name gen134
#!mode=file
B0=$PWD
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen135
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
set --; echo "$#"
%%%
#!name gen136
#!mode=file
B0=$PWD
CDPATH='C'
trap 'echo A' HUP 2>&1; trap - HUP 2>&1; trap; echo rc=$?
%%%
#!name gen137
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- -bvalue -- -a -b; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen138
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
trap 'echo A' HUP 2>&1; trap - HUP 2>&1; trap; echo rc=$?
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen139
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set -ni; echo "$-"
cat <<E
line1
line2
E
%%%
#!name gen140
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
%%%
#!name gen141
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -- -z; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen142
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen143
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen144
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen145
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -bvalue -a -b --; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen146
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen147
#!mode=file
B0=$PWD
trap 'echo X' 1 2>&1; (trap); echo ---; trap
%%%
#!name gen148
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
g() { local B='x=y'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
g() { local vv='C'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen149
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
trap 'echo X' ALRM 2>&1; (trap); echo ---; trap
%%%
#!name gen150
#!mode=file
B0=$PWD
readonly CDPATH='.'
%%%
#!name gen151
#!mode=file
B0=$PWD
g() { local B=''; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen152
#!mode=file
B0=$PWD
set -- -- -bvalue -a -b; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
env | grep -c '^LC_CTYPE='
%%%
#!name gen153
#!mode=file
B0=$PWD
trap '' 10 2>&1; trap; echo rc=$?
set -o | md5sum
%%%
#!name gen154
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set +o | md5sum
read v <<E
value
E
echo "[$v]"
%%%
#!name gen155
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
set -- -b; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
readonly HISTSIZE='"q"'
%%%
#!name gen156
#!mode=file
B0=$PWD
set +b; echo "$-"
{ echo p; } 6>&- ; echo rc=$?
set | grep -c '^LINENO='
%%%
#!name gen157
#!mode=file
B0=$PWD
g() { local vv='1'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen158
#!mode=file
B0=$PWD
trap 'echo X' 1 2>&1; (trap); echo ---; trap
set -o nolog; echo "$-"
%%%
#!name gen159
#!mode=file
B0=$PWD
trap '' ALRM 2>&1; trap; echo rc=$?
read v <<E
value
E
echo "[$v]"
cat <<-E
	indented
	E
%%%
#!name gen160
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen161
#!mode=file
B0=$PWD
exec 4<&-; read v <&4 2>&1; echo rc=$?
%%%
#!name gen162
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen163
#!mode=file
B0=$PWD
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set -- -- -a -bvalue; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen164
#!mode=file
B0=$PWD
exec 4<&-; read v <&4 2>&1; echo rc=$?
%%%
#!name gen165
#!mode=file
B0=$PWD
set -- -c; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen166
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set -o xtrace; echo "$-"
%%%
#!name gen167
#!mode=file
B0=$PWD
PS2='"q"'
%%%
#!name gen168
#!mode=file
B0=$PWD
exec 6<<E
fromfd
E
read v <&6; echo "[$v]"; exec 6<&-
%%%
#!name gen169
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen170
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
unset -v IFS
%%%
#!name gen171
#!mode=file
B0=$PWD
trap USR2 2>&1; trap; echo rc=$?
%%%
#!name gen172
#!mode=file
B0=$PWD
trap 'echo A' 10 2>&1; trap - 10 2>&1; trap; echo rc=$?
cat <<'E'
$notexpanded
E
set -o nounset; echo "$-"
%%%
#!name gen173
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen174
#!mode=file
B0=$PWD
set --  c; echo "$#:$*"
%%%
#!name gen175
#!mode=file
B0=$PWD
set +o | md5sum
g() { local -; set +I; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen176
#!mode=file
B0=$PWD
set +e; echo "$-"
g() { local -; set -E; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen177
#!mode=file
B0=$PWD
set | grep -c '^OLDPWD='
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen178
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen179
#!mode=file
B0=$PWD
set +o pipefail; echo "$-"
%%%
#!name gen180
#!mode=file
B0=$PWD
g() { local V1; V1='\t'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
g() { local IFS; IFS='1'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set +o noglob; echo "$-"
%%%
#!name gen181
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen182
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
LC_ALL='"q"'
g() { local -; set -V; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen183
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
g() { local vv; vv='/tmp'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen184
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
echo "[${OLDPWD#$B0}]"
%%%
#!name gen185
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen186
#!mode=file
B0=$PWD
trap 'echo T' TERM 2>&1; trap; echo rc=$?
%%%
#!name gen187
#!mode=file
B0=$PWD
g() { local A='a'b'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
export -p | grep -c 'HISTSIZE' 
g() { local PATH B; PATH='0'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen188
#!mode=file
B0=$PWD
trap 'echo X' 0 2>&1; (trap); echo ---; trap
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen189
#!mode=file
B0=$PWD
set +V; echo "$-"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen190
#!mode=file
B0=$PWD
g() { local B; B='.'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
readonly LC_NUMERIC='\t'
%%%
#!name gen191
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- -b -bvalue -b; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 5<&-; read v <&5 2>&1; echo rc=$?
%%%
#!name gen192
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
echo "[${CDPATH-U}]"
%%%
#!name gen193
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen194
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
alias 'a b'=1 2>&1; echo rc=$?
g() { local OPTIND='/tmp'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen195
#!mode=file
B0=$PWD
g() { local PATH=':'; PATH=':'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen196
#!mode=file
B0=$PWD
MAILPATH='a b'
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen197
#!mode=file
B0=$PWD
set -mv; echo "$-"
%%%
#!name gen198
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen199
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen200
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set -- -a; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen201
#!mode=file
B0=$PWD
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
%%%
#!name gen202
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen203
#!mode=file
B0=$PWD
set -- -c -ab -z; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -c; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export PWD
%%%
#!name gen204
#!mode=file
B0=$PWD
unset OLDPWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
set -- -b -a; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen205
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen206
#!mode=file
B0=$PWD
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
trap 'echo X' INT 2>&1; (trap); echo ---; trap
%%%
#!name gen207
#!mode=file
B0=$PWD
set -o vi; echo "$-"
set -- -ab -bvalue; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export OLDPWD='a'b'
%%%
#!name gen208
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen209
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
export B
set -- -b -b; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen210
#!mode=file
B0=$PWD
set -- x -ab; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen211
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
alias echo='echo pre'; echo hi
trap '' 12 2>&1; trap; echo rc=$?
%%%
#!name gen212
#!mode=file
B0=$PWD
exec 6>&1; echo x >&6; exec 6>&-; echo y >&6 2>&1; echo rc=$?
alias echo='echo pre'; echo hi
%%%
#!name gen213
#!mode=file
B0=$PWD
set -o | md5sum
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen214
#!mode=file
B0=$PWD
exec 6<<E
fromfd
E
read v <&6; echo "[$v]"; exec 6<&-
g() { local -; set -n; echo "in:$-"; }; g; echo "out:$-"
set -- -b -a; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen215
#!mode=file
B0=$PWD
set +o errexit; echo "$-"
g() { local -; set -o vi; echo "in:$-"; }; g; echo "out:$-"
echo "[${HISTSIZE-U}]"
%%%
#!name gen216
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
set -- -a x -a; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen217
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen218
#!mode=file
B0=$PWD
trap '' TERM 2>&1; trap; echo rc=$?
cd /nope 2>&1; echo rc=$?
readonly OLDPWD='a'b'
%%%
#!name gen219
#!mode=file
B0=$PWD
set -- -z x -a -b; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set +o ignoreeof; echo "$-"
%%%
#!name gen220
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
export -p | grep -c 'PS1' 
%%%
#!name gen221
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
set -- -c x; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- ; echo "$#:$*"
%%%
#!name gen222
#!mode=file
B0=$PWD
unset IFS
%%%
#!name gen223
#!mode=file
B0=$PWD
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
g() { local vv B; vv='/tmp'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen224
#!mode=file
B0=$PWD
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
%%%
#!name gen225
#!mode=file
B0=$PWD
set +o | md5sum
%%%
#!name gen226
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen227
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen228
#!mode=file
B0=$PWD
set -- -a x -c; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set | grep -c '^LC_COLLATE='
%%%
#!name gen229
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen230
#!mode=file
B0=$PWD
set -- a b c d e; shift 3 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen231
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
trap 'echo T' ALRM 2>&1; trap; echo rc=$?
set | grep -c '^LC_COLLATE='
%%%
#!name gen232
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen233
#!mode=file
B0=$PWD
unset PPID
g() { local V1; V1='\t'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen234
#!mode=file
B0=$PWD
trap 'echo X' USR2 2>&1; (trap); echo ---; trap
unset CDPATH
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen235
#!mode=file
B0=$PWD
set -- --; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen236
#!mode=file
B0=$PWD
trap '' 1 2>&1; trap; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -- x -ab -a -b; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen237
#!mode=file
B0=$PWD
set +o | md5sum
%%%
#!name gen238
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
cat <<E
line1
line2
E
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen239
#!mode=file
B0=$PWD
g() { local PATH='.'; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen240
#!mode=file
B0=$PWD
trap 'echo A' 12 2>&1; trap - 12 2>&1; trap; echo rc=$?
%%%
#!name gen241
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
{ echo p; } 9>&- ; echo rc=$?
%%%
#!name gen242
#!mode=file
B0=$PWD
set -- -b -ab; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen243
#!mode=file
B0=$PWD
exec 9>r9.txt; echo hi >&9; exec 9>&-; cat r9.txt
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen244
#!mode=file
B0=$PWD
set -- -c -a x -a x; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen245
#!mode=file
B0=$PWD
export vv='x=y'
%%%
#!name gen246
#!mode=file
B0=$PWD
trap 'echo T' TERM 2>&1; trap; echo rc=$?
B='x=y'
trap 'echo T' 1 2>&1; trap; echo rc=$?
%%%
#!name gen247
#!mode=file
B0=$PWD
trap 'echo A' QUIT 2>&1; trap - QUIT 2>&1; trap; echo rc=$?
alias a='b'; alias b='echo B'; a
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen248
#!mode=file
B0=$PWD
set -- x; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen249
#!mode=file
B0=$PWD
set -- -bvalue; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen250
#!mode=file
B0=$PWD
echo "[${CDPATH}]"
g() { local PATH='C'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen251
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen252
#!mode=file
B0=$PWD
LC_CTYPE='POSIX'
trap 'echo E' EXIT; echo body
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen253
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
exec 6>&1; echo x >&6; exec 6>&-; echo y >&6 2>&1; echo rc=$?
set -- -c -z -bvalue; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen254
#!mode=file
B0=$PWD
set -- -z -a -b -a x; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
read v <<E
value
E
echo "[$v]"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen255
#!mode=file
B0=$PWD
set | grep -c '^HISTSIZE='
%%%
#!name gen256
#!mode=file
B0=$PWD
set +i; echo "$-"
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen257
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set -- -a -b x; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; a; alias a
%%%
#!name gen258
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
set -- c c a b; echo "$#:$*"
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
%%%
#!name gen259
#!mode=file
B0=$PWD
export -p | grep -c 'OLDPWD' 
%%%
#!name gen260
#!mode=file
B0=$PWD
g() { local V1='POSIX'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen261
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
set -o | md5sum
trap 'echo E' EXIT; echo body
%%%
#!name gen262
#!mode=file
B0=$PWD
set -- -a -b; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen263
#!mode=file
B0=$PWD
set -- -c; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -bvalue -bvalue -c; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b'; alias b='echo B'; a
%%%
#!name gen264
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
trap 'echo X' 12 2>&1; (trap); echo ---; trap
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen265
#!mode=file
B0=$PWD
trap 'echo T' PIPE 2>&1; trap; echo rc=$?
g() { local -; set +b; echo "in:$-"; }; g; echo "out:$-"
set -- -bvalue -c -a -b; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen266
#!mode=file
B0=$PWD
set -- -a -- -a -b; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
readonly LC_NUMERIC='C'
%%%
#!name gen267
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen268
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
echo "[${LC_ALL}]"
%%%
#!name gen269
#!mode=file
B0=$PWD
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen270
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen271
#!mode=file
B0=$PWD
trap 'echo A' USR2 2>&1; trap - USR2 2>&1; trap; echo rc=$?
set | grep -c '^vv='
%%%
#!name gen272
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
set -o | md5sum
%%%
#!name gen273
#!mode=file
B0=$PWD
readonly TERM='a'b'
read v <<E
value
E
echo "[$v]"
%%%
#!name gen274
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local vv B; vv='"q"'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen275
#!mode=file
B0=$PWD
trap '' PIPE 2>&1; trap; echo rc=$?
trap '' 12 2>&1; trap; echo rc=$?
%%%
#!name gen276
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set --; echo "$#"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen277
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -; echo "$-"
set -- -c x -bvalue; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen278
#!mode=file
B0=$PWD
set +v; echo "$-"
alias 'a b'=1 2>&1; echo rc=$?
set -; echo "$-"
%%%
#!name gen279
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen280
#!mode=file
B0=$PWD
set -- -ab -a -b; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias echo='echo pre'; echo hi
echo "[${LC_CTYPE-U}]"
%%%
#!name gen281
#!mode=file
B0=$PWD
readonly HISTSIZE='abc'
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
trap '' 12 2>&1; trap; echo rc=$?
%%%
#!name gen282
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
set -- -bvalue -z -b; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen283
#!mode=file
B0=$PWD
trap 'echo T' EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen284
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen285
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen286
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen287
#!mode=file
B0=$PWD
set -- --; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- b; echo "$#:$*"
%%%
#!name gen288
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen289
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen290
#!mode=file
B0=$PWD
trap 'echo A' 12 2>&1; trap - 12 2>&1; trap; echo rc=$?
%%%
#!name gen291
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
exec 5>&1; echo x >&5; exec 5>&-; echo y >&5 2>&1; echo rc=$?
g() { local PATH; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen292
#!mode=file
B0=$PWD
g() { local PATH B; PATH='POSIX'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen293
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -- -ab -ab; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen294
#!mode=file
B0=$PWD
trap 'echo T' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen295
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen296
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen297
#!mode=file
B0=$PWD
unset -v PWD
(trap 'echo SE' EXIT; echo sub); echo main
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen298
#!mode=file
B0=$PWD
set | grep -c '^LINENO='
%%%
#!name gen299
#!mode=file
B0=$PWD
set -- x; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen300
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen301
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen302
#!mode=file
B0=$PWD
g() { local V1 B; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen303
#!mode=file
B0=$PWD
trap '' EXIT 2>&1; trap; echo rc=$?
export LANG='C'
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen304
#!mode=file
B0=$PWD
set -- -a -b -- -b; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- a b c d e; shift 3 2>&1; echo "rc=$? $#:$*"
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
%%%
#!name gen305
#!mode=file
B0=$PWD
set -; echo "$-"
set | grep -c '^OLDPWD='
g() { local OPTIND; OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen306
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen307
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen308
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
exec 4<&-; read v <&4 2>&1; echo rc=$?
%%%
#!name gen309
#!mode=file
B0=$PWD
g() { local OPTIND; OPTIND='C'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
g() { local B; B='.'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen310
#!mode=file
B0=$PWD
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
unset -v PS2
set -- -ab; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen311
#!mode=file
B0=$PWD
set --; echo "$#"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
env | grep -c '^vv='
%%%
#!name gen312
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -bvalue; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen313
#!mode=file
B0=$PWD
set +o notify; echo "$-"
%%%
#!name gen314
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen315
#!mode=file
B0=$PWD
set +b; echo "$-"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen316
#!mode=file
B0=$PWD
g() { local V1='.'; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -ff; echo "$-"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen317
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
echo "[${LC_ALL-U}]"
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen318
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
g() { local PATH B; PATH='abc'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen319
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen320
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
readonly LC_COLLATE='"q"'
%%%
#!name gen321
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
PATH='a'b'
%%%
#!name gen322
#!mode=file
B0=$PWD
env | grep -c '^CDPATH='
echo "[${OPTIND}]"
g() { local V1 B; V1='x=y'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen323
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
set -- -c x --; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen324
#!mode=file
B0=$PWD
set -- -a x -ab -z; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo X' PIPE 2>&1; (trap); echo ---; trap
%%%
#!name gen325
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
{ echo p; } 4>&- ; echo rc=$?
%%%
#!name gen326
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen327
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen328
#!mode=file
B0=$PWD
echo "[${PATH}]"
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen329
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
read v <<E
value
E
echo "[$v]"
%%%
#!name gen330
#!mode=file
B0=$PWD
echo "[${PATH}]"
%%%
#!name gen331
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen332
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
trap QUIT 2>&1; trap; echo rc=$?
{ echo p; } 9>&- ; echo rc=$?
%%%
#!name gen333
#!mode=file
B0=$PWD
trap 'echo T' CHLD 2>&1; trap; echo rc=$?
g() { local B='"q"'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen334
#!mode=file
B0=$PWD
export LC_NUMERIC='0'
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set -o | md5sum
%%%
#!name gen335
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- x; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen336
#!mode=file
B0=$PWD
set -- -a x -ab -a -b; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen337
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -- -- -z -a; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- --; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen338
#!mode=file
B0=$PWD
set +o ignoreeof; echo "$-"
g() { local OPTIND='POSIX'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
alias a='b'; alias b='echo B'; a
%%%
#!name gen339
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
trap '' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen340
#!mode=file
B0=$PWD
trap 'echo A' 10 2>&1; trap - 10 2>&1; trap; echo rc=$?
%%%
#!name gen341
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen342
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen343
#!mode=file
B0=$PWD
set | grep -c '^LANG='
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen344
#!mode=file
B0=$PWD
trap '' QUIT 2>&1; trap; echo rc=$?
g() { local B; B='\t'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen345
#!mode=file
B0=$PWD
set -- -a -b -c; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen346
#!mode=file
B0=$PWD
set -- -bvalue -a; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; a; alias a
%%%
#!name gen347
#!mode=file
B0=$PWD
trap '' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen348
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
g() { local V1 B; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen349
#!mode=file
B0=$PWD
trap 'echo X' QUIT 2>&1; (trap); echo ---; trap
set -- -z -- -bvalue; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen350
#!mode=file
B0=$PWD
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
cat <<-E
	indented
	E
%%%
#!name gen351
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
exec 6>&1; echo x >&6; exec 6>&-; echo y >&6 2>&1; echo rc=$?
set --; echo "$#"
%%%
#!name gen352
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
alias a='b'; alias b='echo B'; a
g() { local A='0'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen353
#!mode=file
B0=$PWD
set -- -ab; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen354
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
g() { local A B; A='"q"'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
trap USR1 2>&1; trap; echo rc=$?
%%%
#!name gen355
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
cat <<'E'
$notexpanded
E
%%%
#!name gen356
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen357
#!mode=file
B0=$PWD
set -- b b 1; echo "$#:$*"
cat <<'E'
$notexpanded
E
%%%
#!name gen358
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen359
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen360
#!mode=file
B0=$PWD
export MAIL
%%%
#!name gen361
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -- -b -a x; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen362
#!mode=file
B0=$PWD
set -- -- -a; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen363
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
set -o | md5sum
%%%
#!name gen364
#!mode=file
B0=$PWD
readonly PS4='0'
%%%
#!name gen365
#!mode=file
B0=$PWD
set -o ignoreeof; echo "$-"
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
echo "[${LINENO}]"
%%%
#!name gen366
#!mode=file
B0=$PWD
set -- ; echo "$#:$*"
%%%
#!name gen367
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
trap 'echo X' PIPE 2>&1; (trap); echo ---; trap
%%%
#!name gen368
#!mode=file
B0=$PWD
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen369
#!mode=file
B0=$PWD
unset PPID
%%%
#!name gen370
#!mode=file
B0=$PWD
set -i; echo "$-"
set -- x -a x -ab; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen371
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set -e; echo "$-"
%%%
#!name gen372
#!mode=file
B0=$PWD
set -- -z -a; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen373
#!mode=file
B0=$PWD
g() { local -; set +u; echo "in:$-"; }; g; echo "out:$-"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen374
#!mode=file
B0=$PWD
set -mC; echo "$-"
set -- -- -c -b; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen375
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen376
#!mode=file
B0=$PWD
echo "[${PS1}]"
%%%
#!name gen377
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen378
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -o verbose; echo "$-"
%%%
#!name gen379
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen380
#!mode=file
B0=$PWD
{ echo p; } 5>&- ; echo rc=$?
%%%
#!name gen381
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
g() { local -; set +C; echo "in:$-"; }; g; echo "out:$-"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen382
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
trap 'echo E' EXIT; echo body
%%%
#!name gen383
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen384
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
readonly MAIL='a'b'
%%%
#!name gen385
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
alias a='b'; alias b='echo B'; a
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
%%%
#!name gen386
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen387
#!mode=file
B0=$PWD
set -x; echo "$-"
trap 'echo E' EXIT; echo body
g() { local vv=':'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen388
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen389
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen390
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
trap INT 2>&1; trap; echo rc=$?
%%%
#!name gen391
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen392
#!mode=file
B0=$PWD
CDPATH='/tmp'
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen393
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
trap 'echo X' 0 2>&1; (trap); echo ---; trap
set -o | md5sum
%%%
#!name gen394
#!mode=file
B0=$PWD
g() { local -; set +C; echo "in:$-"; }; g; echo "out:$-"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen395
#!mode=file
B0=$PWD
set +o noexec; echo "$-"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen396
#!mode=file
B0=$PWD
set -- -a -a x -ab; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 4<&-; read v <&4 2>&1; echo rc=$?
set -vm; echo "$-"
%%%
#!name gen397
#!mode=file
B0=$PWD
trap 'echo T' 12 2>&1; trap; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen398
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen399
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen400
#!mode=file
B0=$PWD
exec 4<<E
fromfd
E
read v <&4; echo "[$v]"; exec 4<&-
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
cat <<'E'
$notexpanded
E
%%%
#!name gen401
#!mode=file
B0=$PWD
trap 'echo T' PIPE 2>&1; trap; echo rc=$?
set -o | md5sum
alias a='echo A'; a; alias a
%%%
#!name gen402
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen403
#!mode=file
B0=$PWD
trap PIPE 2>&1; trap; echo rc=$?
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set +x; echo "$-"
%%%
#!name gen404
#!mode=file
B0=$PWD
set --; echo "$#"
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
%%%
#!name gen405
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen406
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
g() { local OPTIND; OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen407
#!mode=file
B0=$PWD
set -a; echo "$-"
alias echo='echo pre'; echo hi
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen408
#!mode=file
B0=$PWD
set -- c; echo "$#:$*"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen409
#!mode=file
B0=$PWD
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
cat <<E
line1
line2
E
g() { local vv; vv='x=y'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen410
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen411
#!mode=file
B0=$PWD
set +o vi; echo "$-"
g() { local -; set -o nolog; echo "in:$-"; }; g; echo "out:$-"
trap 'echo X' 1 2>&1; (trap); echo ---; trap
%%%
#!name gen412
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set | grep -c '^vv='
%%%
#!name gen413
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
alias a='echo A'; a; alias a
echo "[${PATH-U}]"
%%%
#!name gen414
#!mode=file
B0=$PWD
trap HUP 2>&1; trap; echo rc=$?
set -; echo "$-"
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
%%%
#!name gen415
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen416
#!mode=file
B0=$PWD
set +o allexport; echo "$-"
%%%
#!name gen417
#!mode=file
B0=$PWD
exec 5>&1; echo x >&5; exec 5>&-; echo y >&5 2>&1; echo rc=$?
%%%
#!name gen418
#!mode=file
B0=$PWD
set -- -a -b; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -z -z -a x; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen419
#!mode=file
B0=$PWD
trap 'echo X' 12 2>&1; (trap); echo ---; trap
%%%
#!name gen420
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen421
#!mode=file
B0=$PWD
set -a; echo "$-"
%%%
#!name gen422
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
env | grep -c '^OLDPWD='
{ echo p; } 6>&- ; echo rc=$?
%%%
#!name gen423
#!mode=file
B0=$PWD
PPID='\t'
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen424
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen425
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen426
#!mode=file
B0=$PWD
set -- -a -b -a; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -z -bvalue --; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen427
#!mode=file
B0=$PWD
trap 'echo T' QUIT 2>&1; trap; echo rc=$?
%%%
#!name gen428
#!mode=file
B0=$PWD
g() { local PATH='\t'; PATH='\t'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -- -z -a -b; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen429
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen430
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
echo "[${CDPATH-U}]"
%%%
#!name gen431
#!mode=file
B0=$PWD
export TERM
set +o | md5sum
g() { local IFS='.'; IFS='.'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen432
#!mode=file
B0=$PWD
unset PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen433
#!mode=file
B0=$PWD
g() { local IFS=''; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen434
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen435
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set -- -z -z; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen436
#!mode=file
B0=$PWD
trap 'echo A' INT 2>&1; trap - INT 2>&1; trap; echo rc=$?
%%%
#!name gen437
#!mode=file
B0=$PWD
echo "[${HOME-U}]"
set +o nounset; echo "$-"
set +o nounset; echo "$-"
%%%
#!name gen438
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen439
#!mode=file
B0=$PWD
B='POSIX'
set +e; echo "$-"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen440
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen441
#!mode=file
B0=$PWD
trap '' HUP 2>&1; trap; echo rc=$?
set +f; echo "$-"
%%%
#!name gen442
#!mode=file
B0=$PWD
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen443
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen444
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local V1='"q"'; V1='"q"'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen445
#!mode=file
B0=$PWD
PATH=':'
unset -v HISTSIZE
%%%
#!name gen446
#!mode=file
B0=$PWD
g() { local -; set -i; echo "in:$-"; }; g; echo "out:$-"
export -p | grep -c 'vv' 
%%%
#!name gen447
#!mode=file
B0=$PWD
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen448
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
%%%
#!name gen449
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set -i; echo "$-"
%%%
#!name gen450
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
f() { echo F; }; f >f.txt 2>&1; cat f.txt
alias echo='echo pre'; echo hi
%%%
#!name gen451
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- -b x; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen452
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen453
#!mode=file
B0=$PWD
g() { local IFS B; IFS=':'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
cat <<E
line1
line2
E
%%%
#!name gen454
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local -; set +m; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen455
#!mode=file
B0=$PWD
exec 9<&-; read v <&9 2>&1; echo rc=$?
%%%
#!name gen456
#!mode=file
B0=$PWD
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
trap 'echo A' USR1 2>&1; trap - USR1 2>&1; trap; echo rc=$?
cat <<-E
	indented
	E
%%%
#!name gen457
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen458
#!mode=file
B0=$PWD
set -Em; echo "$-"
export -p | grep -c 'PWD' 
set -o noclobber; echo "$-"
%%%
#!name gen459
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen460
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen461
#!mode=file
B0=$PWD
set -o pipefail; echo "$-"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen462
#!mode=file
B0=$PWD
set -- -- -b x; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen463
#!mode=file
B0=$PWD
readonly LANG='C'
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
trap USR1 2>&1; trap; echo rc=$?
%%%
#!name gen464
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen465
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen466
#!mode=file
B0=$PWD
echo "[${PWD#$B0}]"
%%%
#!name gen467
#!mode=file
B0=$PWD
unset PS2
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen468
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen469
#!mode=file
B0=$PWD
g() { local vv B; vv='1'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
cat <<'E'
$notexpanded
E
%%%
#!name gen470
#!mode=file
B0=$PWD
set -- -ab -a -bvalue; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x --; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local vv; vv='\t'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen471
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
set -- -bvalue; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen472
#!mode=file
B0=$PWD
g() { local -; set -o xtrace; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen473
#!mode=file
B0=$PWD
set -- -b x -a; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen474
#!mode=file
B0=$PWD
readonly CDPATH='C'
%%%
#!name gen475
#!mode=file
B0=$PWD
set -- -- x -b; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen476
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen477
#!mode=file
B0=$PWD
trap '' INT 2>&1; trap; echo rc=$?
%%%
#!name gen478
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
cat <<E
line1
line2
E
%%%
#!name gen479
#!mode=file
B0=$PWD
set -- -- -c; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen480
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
set -- -c -ab; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen481
#!mode=file
B0=$PWD
g() { local IFS='1'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -- -z x --; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen482
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen483
#!mode=file
B0=$PWD
set -- -z --; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set | grep -c '^LC_COLLATE='
%%%
#!name gen484
#!mode=file
B0=$PWD
g() { local IFS='C'; IFS='C'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
alias a='echo A'; a; alias a
%%%
#!name gen485
#!mode=file
B0=$PWD
exec 4<<E
fromfd
E
read v <&4; echo "[$v]"; exec 4<&-
IFS='/tmp'
echo "[${LANG}]"
%%%
#!name gen486
#!mode=file
B0=$PWD
set +o verbose; echo "$-"
%%%
#!name gen487
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen488
#!mode=file
B0=$PWD
HOME='x=y'
%%%
#!name gen489
#!mode=file
B0=$PWD
unset MAIL
exec 6<<E
fromfd
E
read v <&6; echo "[$v]"; exec 6<&-
%%%
#!name gen490
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
trap 'echo T' 0 2>&1; trap; echo rc=$?
%%%
#!name gen491
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set -- -c; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen492
#!mode=file
B0=$PWD
set -au; echo "$-"
%%%
#!name gen493
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
f() { echo F; }; f >f.txt 2>&1; cat f.txt
g() { local OPTIND='\t'; OPTIND='\t'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen494
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen495
#!mode=file
B0=$PWD
set -- -bvalue; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -z -ab; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen496
#!mode=file
B0=$PWD
unset -v LC_ALL
set +o | md5sum
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
%%%
#!name gen497
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen498
#!mode=file
B0=$PWD
g() { local A=''; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- b 1 a; echo "$#:$*"
%%%
#!name gen499
#!mode=file
B0=$PWD
g() { local OPTIND; OPTIND='0'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
%%%
#!name gen500
#!mode=file
B0=$PWD
echo "[${B-U}]"
read v <<E
value
E
echo "[$v]"
trap 'echo X' TERM 2>&1; (trap); echo ---; trap
%%%
#!name gen501
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
trap INT 2>&1; trap; echo rc=$?
%%%
#!name gen502
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set -- -b; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo E' EXIT; echo body
%%%
#!name gen503
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
set -; echo "$-"
set -Eb; echo "$-"
%%%
#!name gen504
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen505
#!mode=file
B0=$PWD
trap 'echo A' QUIT 2>&1; trap - QUIT 2>&1; trap; echo rc=$?
%%%
#!name gen506
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
trap '' 10 2>&1; trap; echo rc=$?
%%%
#!name gen507
#!mode=file
B0=$PWD
set +o | md5sum
%%%
#!name gen508
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
%%%
#!name gen509
#!mode=file
B0=$PWD
g() { local vv='a'b'; vv='a'b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
trap '' INT 2>&1; trap; echo rc=$?
unset -v PWD
%%%
#!name gen510
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set | grep -c '^OLDPWD='
set -- -b -ab; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen511
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen512
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
cat <<'E'
$notexpanded
E
export OPTIND=':'
%%%
#!name gen513
#!mode=file
B0=$PWD
g() { local A=''; A=''; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen514
#!mode=file
B0=$PWD
g() { local OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen515
#!mode=file
B0=$PWD
trap TERM 2>&1; trap; echo rc=$?
set -- -c -z --; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export HISTSIZE
%%%
#!name gen516
#!mode=file
B0=$PWD
trap 10 2>&1; trap; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen517
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
%%%
#!name gen518
#!mode=file
B0=$PWD
TERM='POSIX'
%%%
#!name gen519
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen520
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen521
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen522
#!mode=file
B0=$PWD
HISTSIZE='0'
set -nu; echo "$-"
set -- b 1 1 ; echo "$#:$*"
%%%
#!name gen523
#!mode=file
B0=$PWD
trap 'echo X' INT 2>&1; (trap); echo ---; trap
%%%
#!name gen524
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
alias echo='echo pre'; echo hi
set -Iv; echo "$-"
%%%
#!name gen525
#!mode=file
B0=$PWD
set -o | md5sum
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen526
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen527
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
%%%
#!name gen528
#!mode=file
B0=$PWD
set -- -c -a -b; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset -v OPTIND
%%%
#!name gen529
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set +u; echo "$-"
%%%
#!name gen530
#!mode=file
B0=$PWD
set -- -- -a; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -bvalue -c -c; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen531
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen532
#!mode=file
B0=$PWD
set -- -a; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen533
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
trap '' 0 2>&1; trap; echo rc=$?
%%%
#!name gen534
#!mode=file
B0=$PWD
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen535
#!mode=file
B0=$PWD
set -VC; echo "$-"
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen536
#!mode=file
B0=$PWD
set --  b a; echo "$#:$*"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- -z -a x; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen537
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
alias a='b'; alias b='echo B'; a
%%%
#!name gen538
#!mode=file
B0=$PWD
set -- -bvalue --; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen539
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen540
#!mode=file
B0=$PWD
trap '' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen541
#!mode=file
B0=$PWD
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
%%%
#!name gen542
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen543
#!mode=file
B0=$PWD
export -p | grep -c 'PPID' 
g() { local B='x=y'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen544
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen545
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen546
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen547
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
trap 'echo X' ALRM 2>&1; (trap); echo ---; trap
%%%
#!name gen548
#!mode=file
B0=$PWD
unset IFS
%%%
#!name gen549
#!mode=file
B0=$PWD
unset TERM
%%%
#!name gen550
#!mode=file
B0=$PWD
g() { local -; set +x; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen551
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen552
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen553
#!mode=file
B0=$PWD
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
trap TERM 2>&1; trap; echo rc=$?
%%%
#!name gen554
#!mode=file
B0=$PWD
set -; echo "$-"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
cat <<-E
	indented
	E
%%%
#!name gen555
#!mode=file
B0=$PWD
g() { local vv='a'b'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen556
#!mode=file
B0=$PWD
g() { local -; set -o ignoreeof; echo "in:$-"; }; g; echo "out:$-"
alias echo='echo pre'; echo hi
set | grep -c '^PS1='
%%%
#!name gen557
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -- -bvalue -z -c; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen558
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
trap 'echo T' ALRM 2>&1; trap; echo rc=$?
alias a='b'; alias b='echo B'; a
%%%
#!name gen559
#!mode=file
B0=$PWD
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -- -a -b -ab -c; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen560
#!mode=file
B0=$PWD
set -- -ab -b; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen561
#!mode=file
B0=$PWD
trap 'echo T' 12 2>&1; trap; echo rc=$?
set +f; echo "$-"
%%%
#!name gen562
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen563
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
export HOME='/tmp'
%%%
#!name gen564
#!mode=file
B0=$PWD
set -o | md5sum
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
%%%
#!name gen565
#!mode=file
B0=$PWD
set -- -b x; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen566
#!mode=file
B0=$PWD
trap 'echo A' QUIT 2>&1; trap - QUIT 2>&1; trap; echo rc=$?
%%%
#!name gen567
#!mode=file
B0=$PWD
trap 10 2>&1; trap; echo rc=$?
set -V; echo "$-"
%%%
#!name gen568
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen569
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen570
#!mode=file
B0=$PWD
set -x; echo "$-"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen571
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set +f; echo "$-"
%%%
#!name gen572
#!mode=file
B0=$PWD
g() { local A='\t'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -- -bvalue -- -a x; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen573
#!mode=file
B0=$PWD
set -- -a x; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen574
#!mode=file
B0=$PWD
set +o | md5sum
set -- -b -bvalue x; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen575
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
g() { local -; set -i; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen576
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
set -u; echo "$-"
%%%
#!name gen577
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen578
#!mode=file
B0=$PWD
unset -v LC_ALL
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen579
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
exec 3<&-; read v <&3 2>&1; echo rc=$?
%%%
#!name gen580
#!mode=file
B0=$PWD
unset -v A
g() { local -; set -v; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen581
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen582
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
env | grep -c '^MAILPATH='
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
%%%
#!name gen583
#!mode=file
B0=$PWD
export vv='POSIX'
%%%
#!name gen584
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen585
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
{ echo p; } 9>&- ; echo rc=$?
export PS1
%%%
#!name gen586
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen587
#!mode=file
B0=$PWD
g() { local -; set -o noclobber; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen588
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
alias a='b'; alias b='echo B'; a
set -- -bvalue -a -b; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen589
#!mode=file
B0=$PWD
trap 'echo A' 12 2>&1; trap - 12 2>&1; trap; echo rc=$?
%%%
#!name gen590
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
g() { local B; B='"q"'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen591
#!mode=file
B0=$PWD
set +o | md5sum
%%%
#!name gen592
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set -- -ab; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen593
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
set -- -a -b -c -a x; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap '' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen594
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen595
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen596
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
(trap 'echo SE' EXIT; echo sub); echo main
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
%%%
#!name gen597
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen598
#!mode=file
B0=$PWD
set -- x; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
env | grep -c '^LANG='
%%%
#!name gen599
#!mode=file
B0=$PWD
set +o | md5sum
g() { local A; A='C'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen600
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
trap 'echo E' EXIT; echo body
%%%
#!name gen601
#!mode=file
B0=$PWD
env | grep -c '^vv='
readonly PS2='a'b'
set -- -b; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen602
#!mode=file
B0=$PWD
trap '' 0 2>&1; trap; echo rc=$?
trap 'echo E' EXIT; echo body
set -- -bvalue -- -a -b; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen603
#!mode=file
B0=$PWD
trap PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen604
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen605
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
set -o | md5sum
%%%
#!name gen606
#!mode=file
B0=$PWD
set -- a; echo "$#:$*"
set +E; echo "$-"
%%%
#!name gen607
#!mode=file
B0=$PWD
echo "[${MAILPATH-U}]"
%%%
#!name gen608
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen609
#!mode=file
B0=$PWD
g() { local A='/tmp'; A='/tmp'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
export B
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen610
#!mode=file
B0=$PWD
set -- -a -b -ab; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen611
#!mode=file
B0=$PWD
set -f; echo "$-"
%%%
#!name gen612
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
cd /nope 2>&1; echo rc=$?
set -- -a x x; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen613
#!mode=file
B0=$PWD
g() { local -; set +E; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen614
#!mode=file
B0=$PWD
set -o vi; echo "$-"
trap 'echo T' 1 2>&1; trap; echo rc=$?
%%%
#!name gen615
#!mode=file
B0=$PWD
readonly HISTSIZE='POSIX'
set -- -- -ab; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -b --; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen616
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen617
#!mode=file
B0=$PWD
set --  c; echo "$#:$*"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen618
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
unset -v HOME
{ echo p; } 4>&- ; echo rc=$?
%%%
#!name gen619
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen620
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen621
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
trap 'echo T' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen622
#!mode=file
B0=$PWD
TERM='C'
export -p | grep -c 'LC_CTYPE' 
%%%
#!name gen623
#!mode=file
B0=$PWD
trap 'echo A' 1 2>&1; trap - 1 2>&1; trap; echo rc=$?
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen624
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
%%%
#!name gen625
#!mode=file
B0=$PWD
trap '' 12 2>&1; trap; echo rc=$?
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen626
#!mode=file
B0=$PWD
set -- -a x -a x; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export -p | grep -c 'OPTIND' 
%%%
#!name gen627
#!mode=file
B0=$PWD
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen628
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen629
#!mode=file
B0=$PWD
set -- -a x x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo E' EXIT; echo body
trap '' EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen630
#!mode=file
B0=$PWD
set -; echo "$-"
set -- -a x -a -b; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen631
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- -a x -bvalue x; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
%%%
#!name gen632
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen633
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen634
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen635
#!mode=file
B0=$PWD
TERM='0'
%%%
#!name gen636
#!mode=file
B0=$PWD
export OPTIND
%%%
#!name gen637
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen638
#!mode=file
B0=$PWD
g() { local B='\t'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen639
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen640
#!mode=file
B0=$PWD
trap 'echo A' CHLD 2>&1; trap - CHLD 2>&1; trap; echo rc=$?
unset -v MAILPATH
set -- ; echo "$#:$*"
%%%
#!name gen641
#!mode=file
B0=$PWD
{ echo p; } 4>&- ; echo rc=$?
%%%
#!name gen642
#!mode=file
B0=$PWD
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
%%%
#!name gen643
#!mode=file
B0=$PWD
PS2='a b'
set -- -b; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo "[${OLDPWD#$B0}]"
%%%
#!name gen644
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
set -- -a; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen645
#!mode=file
B0=$PWD
echo "[${V1}]"
%%%
#!name gen646
#!mode=file
B0=$PWD
set -- -c; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
trap EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen647
#!mode=file
B0=$PWD
set -vV; echo "$-"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
set -- 1 b ; echo "$#:$*"
%%%
#!name gen648
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
trap 'echo T' ALRM 2>&1; trap; echo rc=$?
set -- -bvalue -a x; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen649
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen650
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen651
#!mode=file
B0=$PWD
set -- -ab -z; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen652
#!mode=file
B0=$PWD
set -; echo "$-"
set -- -- -a -a -b; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen653
#!mode=file
B0=$PWD
export LC_NUMERIC='x=y'
%%%
#!name gen654
#!mode=file
B0=$PWD
set -- -a -- -a -b; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen655
#!mode=file
B0=$PWD
set | grep -c '^MAILPATH='
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen656
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='C'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -- -bvalue; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen657
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen658
#!mode=file
B0=$PWD
unset -v PS2
set -; echo "$-"
set -- -a x; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen659
#!mode=file
B0=$PWD
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
set -- -b; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen660
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen661
#!mode=file
B0=$PWD
set +v; echo "$-"
%%%
#!name gen662
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
readonly LC_CTYPE=''
%%%
#!name gen663
#!mode=file
B0=$PWD
export OLDPWD='\t'
set --; echo "$#"
%%%
#!name gen664
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen665
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
set +o | md5sum
%%%
#!name gen666
#!mode=file
B0=$PWD
set -- -a x; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen667
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen668
#!mode=file
B0=$PWD
set -- -bvalue -ab x; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen669
#!mode=file
B0=$PWD
set +o noglob; echo "$-"
set +o monitor; echo "$-"
%%%
#!name gen670
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen671
#!mode=file
B0=$PWD
unset LC_ALL
trap HUP 2>&1; trap; echo rc=$?
%%%
#!name gen672
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen673
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen674
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
env | grep -c '^HOME='
%%%
#!name gen675
#!mode=file
B0=$PWD
trap 'echo X' PIPE 2>&1; (trap); echo ---; trap
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
trap 'echo X' PIPE 2>&1; (trap); echo ---; trap
%%%
#!name gen676
#!mode=file
B0=$PWD
set +o noclobber; echo "$-"
trap TERM 2>&1; trap; echo rc=$?
alias a='echo A'; a; alias a
%%%
#!name gen677
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap 'echo X' INT 2>&1; (trap); echo ---; trap
set -- -b --; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen678
#!mode=file
B0=$PWD
trap 'echo X' ALRM 2>&1; (trap); echo ---; trap
readonly V1='x=y'
%%%
#!name gen679
#!mode=file
B0=$PWD
env | grep -c '^PS2='
%%%
#!name gen680
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='POSIX'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap PIPE 2>&1; trap; echo rc=$?
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen681
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
env | grep -c '^B='
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen682
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
export MAILPATH
%%%
#!name gen683
#!mode=file
B0=$PWD
g() { local -; set +V; echo "in:$-"; }; g; echo "out:$-"
unset -v LINENO
g() { local vv; vv=':'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen684
#!mode=file
B0=$PWD
{ echo p; } 4>&- ; echo rc=$?
exec 5>&1; echo x >&5; exec 5>&-; echo y >&5 2>&1; echo rc=$?
g() { local A='x=y'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen685
#!mode=file
B0=$PWD
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen686
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen687
#!mode=file
B0=$PWD
set -- x x -z; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b'; alias b='echo B'; a
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen688
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen689
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen690
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen691
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -; echo "$-"
%%%
#!name gen692
#!mode=file
B0=$PWD
set +x; echo "$-"
set -- --; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -iv; echo "$-"
%%%
#!name gen693
#!mode=file
B0=$PWD
set -b; echo "$-"
%%%
#!name gen694
#!mode=file
B0=$PWD
set -- b b c; echo "$#:$*"
set | grep -c '^vv='
%%%
#!name gen695
#!mode=file
B0=$PWD
trap ALRM 2>&1; trap; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen696
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
trap '' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen697
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen698
#!mode=file
B0=$PWD
trap '' USR2 2>&1; trap; echo rc=$?
set -- -a -a -b; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export A
%%%
#!name gen699
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
cd /nope 2>&1; echo rc=$?
%%%
#!name gen700
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
g() { local A B; A='POSIX'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen701
#!mode=file
B0=$PWD
set | grep -c '^A='
cat <<E
line1
line2
E
%%%
#!name gen702
#!mode=file
B0=$PWD
set -- -b -bvalue; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -c -z -z; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen703
#!mode=file
B0=$PWD
{ echo p; } 3>&- ; echo rc=$?
alias 'a b'=1 2>&1; echo rc=$?
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen704
#!mode=file
B0=$PWD
set -- -ab -- -b; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo "[${LC_CTYPE-U}]"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen705
#!mode=file
B0=$PWD
PS4='0'
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen706
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
(trap 'echo SE' EXIT; echo sub); echo main
alias echo='echo pre'; echo hi
%%%
#!name gen707
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen708
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
trap '' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen709
#!mode=file
B0=$PWD
export V1
LC_ALL='x=y'
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
%%%
#!name gen710
#!mode=file
B0=$PWD
set -- -a -b -- -a; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
g() { local IFS='x=y'; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen711
#!mode=file
B0=$PWD
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
echo "[${LC_NUMERIC}]"
%%%
#!name gen712
#!mode=file
B0=$PWD
unset MAIL
%%%
#!name gen713
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -Vv; echo "$-"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen714
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
g() { local A='"q"'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen715
#!mode=file
B0=$PWD
OLDPWD=''
%%%
#!name gen716
#!mode=file
B0=$PWD
set -n; echo "$-"
%%%
#!name gen717
#!mode=file
B0=$PWD
set -o notify; echo "$-"
%%%
#!name gen718
#!mode=file
B0=$PWD
set +o noglob; echo "$-"
set -; echo "$-"
%%%
#!name gen719
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen720
#!mode=file
B0=$PWD
g() { local A B; A=':'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen721
#!mode=file
B0=$PWD
echo "[${LC_COLLATE}]"
cat <<'E'
$notexpanded
E
readonly MAIL='1'
%%%
#!name gen722
#!mode=file
B0=$PWD
export A
%%%
#!name gen723
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
PS2='.'
%%%
#!name gen724
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
g() { local -; set -o allexport; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen725
#!mode=file
B0=$PWD
set --; echo "$#"
alias a='echo A'; a; alias a
%%%
#!name gen726
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
cd /nope 2>&1; echo rc=$?
%%%
#!name gen727
#!mode=file
B0=$PWD
trap 'echo X' QUIT 2>&1; (trap); echo ---; trap
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen728
#!mode=file
B0=$PWD
g() { local V1 B; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
trap 'echo X' CHLD 2>&1; (trap); echo ---; trap
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen729
#!mode=file
B0=$PWD
trap PIPE 2>&1; trap; echo rc=$?
export MAIL='1'
alias echo='echo pre'; echo hi
%%%
#!name gen730
#!mode=file
B0=$PWD
export HISTSIZE='.'
%%%
#!name gen731
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set -V; echo "$-"
alias a='echo A'; a; alias a
%%%
#!name gen732
#!mode=file
B0=$PWD
g() { local -; set -x; echo "in:$-"; }; g; echo "out:$-"
cat <<-E
	indented
	E
%%%
#!name gen733
#!mode=file
B0=$PWD
g() { local A='"q"'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen734
#!mode=file
B0=$PWD
echo "[${OPTIND}]"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen735
#!mode=file
B0=$PWD
set -- -a x; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
set -o | md5sum
%%%
#!name gen736
#!mode=file
B0=$PWD
unset TERM
unset -v OPTIND
%%%
#!name gen737
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen738
#!mode=file
B0=$PWD
set -o nounset; echo "$-"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen739
#!mode=file
B0=$PWD
g() { local A='POSIX'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
alias a='echo A'; a; alias a
%%%
#!name gen740
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
g() { local vv; vv=':'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen741
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen742
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen743
#!mode=file
B0=$PWD
{ echo p; } 5>&- ; echo rc=$?
%%%
#!name gen744
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set -mf; echo "$-"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen745
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen746
#!mode=file
B0=$PWD
trap 'echo T' EXIT 2>&1; trap; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen747
#!mode=file
B0=$PWD
set -- -- -bvalue -ab; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen748
#!mode=file
B0=$PWD
set +o | md5sum
%%%
#!name gen749
#!mode=file
B0=$PWD
env | grep -c '^CDPATH='
%%%
#!name gen750
#!mode=file
B0=$PWD
set -E; echo "$-"
export PPID='"q"'
set -; echo "$-"
%%%
#!name gen751
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -- -- -bvalue -a -b; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen752
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen753
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen754
#!mode=file
B0=$PWD
g() { local -; set -o notify; echo "in:$-"; }; g; echo "out:$-"
set | grep -c '^vv='
set +o | md5sum
%%%
#!name gen755
#!mode=file
B0=$PWD
set -; echo "$-"
alias a='echo A'; a; alias a
%%%
#!name gen756
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen757
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set | grep -c '^LINENO='
g() { local OPTIND='\t'; OPTIND='\t'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen758
#!mode=file
B0=$PWD
export -p | grep -c 'PWD' 
%%%
#!name gen759
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen760
#!mode=file
B0=$PWD
export HISTSIZE='.'
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
g() { local IFS='POSIX'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen761
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='0'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -- -z; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen762
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen763
#!mode=file
B0=$PWD
g() { local -; set -V; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen764
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen765
#!mode=file
B0=$PWD
trap USR2 2>&1; trap; echo rc=$?
%%%
#!name gen766
#!mode=file
B0=$PWD
exec 6>&1; echo x >&6; exec 6>&-; echo y >&6 2>&1; echo rc=$?
alias echo='echo pre'; echo hi
%%%
#!name gen767
#!mode=file
B0=$PWD
set -- x; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set +E; echo "$-"
%%%
#!name gen768
#!mode=file
B0=$PWD
set -- -a -ab; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen769
#!mode=file
B0=$PWD
set -- -a -b --; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen770
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
g() { local -; set -e; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen771
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set --; echo "$#"
trap 'echo X' ALRM 2>&1; (trap); echo ---; trap
%%%
#!name gen772
#!mode=file
B0=$PWD
{ echo p; } 3>&- ; echo rc=$?
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
g() { local OPTIND='.'; OPTIND='.'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen773
#!mode=file
B0=$PWD
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
set -- x -c; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local -; set +x; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen774
#!mode=file
B0=$PWD
set -- -a -b; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export LC_COLLATE
g() { local B B; B='\t'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen775
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -o | md5sum
%%%
#!name gen776
#!mode=file
B0=$PWD
set -- -bvalue -a -b; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias echo='echo pre'; echo hi
%%%
#!name gen777
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
export -p | grep -c 'LINENO' 
%%%
#!name gen778
#!mode=file
B0=$PWD
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -o nolog; echo "$-"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen779
#!mode=file
B0=$PWD
g() { local PATH; PATH=''; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local -; set -o errexit; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen780
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen781
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
(trap 'echo SE' EXIT; echo sub); echo main
set -- -a -a; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen782
#!mode=file
B0=$PWD
g() { local A; A='a b'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
trap 'echo X' 12 2>&1; (trap); echo ---; trap
set -- -a -- --; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen783
#!mode=file
B0=$PWD
g() { local -; set -i; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen784
#!mode=file
B0=$PWD
set -- -ab; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen785
#!mode=file
B0=$PWD
trap 'echo A' 1 2>&1; trap - 1 2>&1; trap; echo rc=$?
set -- -a -b -c -bvalue; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen786
#!mode=file
B0=$PWD
g() { local A B; A='1'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen787
#!mode=file
B0=$PWD
set -- -a -a -z; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen788
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
unset TERM
%%%
#!name gen789
#!mode=file
B0=$PWD
set -- -a x -a -b; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen790
#!mode=file
B0=$PWD
set -- -a -b -a -a -b; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen791
#!mode=file
B0=$PWD
trap 10 2>&1; trap; echo rc=$?
%%%
#!name gen792
#!mode=file
B0=$PWD
trap 'echo A' HUP 2>&1; trap - HUP 2>&1; trap; echo rc=$?
g() { local -; set -o vi; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen793
#!mode=file
B0=$PWD
trap 'echo A' HUP 2>&1; trap - HUP 2>&1; trap; echo rc=$?
set -; echo "$-"
%%%
#!name gen794
#!mode=file
B0=$PWD
set +I; echo "$-"
%%%
#!name gen795
#!mode=file
B0=$PWD
set -- -b; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -c; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset PS4
%%%
#!name gen796
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -a x -bvalue -a; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen797
#!mode=file
B0=$PWD
set | grep -c '^PPID='
%%%
#!name gen798
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
exec 9<&-; read v <&9 2>&1; echo rc=$?
trap 'echo E' EXIT; echo body
%%%
#!name gen799
#!mode=file
B0=$PWD
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
set -ea; echo "$-"
%%%
#!name gen800
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen801
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 9<&-; read v <&9 2>&1; echo rc=$?
%%%
#!name gen802
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
g() { local -; set -o verbose; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen803
#!mode=file
B0=$PWD
g() { local PATH; PATH='abc'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
export CDPATH
%%%
#!name gen804
#!mode=file
B0=$PWD
set -x; echo "$-"
%%%
#!name gen805
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
g() { local B='a'b'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen806
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
set -- -a -z; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen807
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen808
#!mode=file
B0=$PWD
{ echo p; } 3>&- ; echo rc=$?
%%%
#!name gen809
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
g() { local V1 B; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
TERM='0'
%%%
#!name gen810
#!mode=file
B0=$PWD
set -- -a x -ab; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen811
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen812
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
g() { local -; set +b; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen813
#!mode=file
B0=$PWD
set -- a b c d e; shift 3 2>&1; echo "rc=$? $#:$*"
trap 'echo T' PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen814
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
cat <<-E
	indented
	E
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen815
#!mode=file
B0=$PWD
unset IFS
%%%
#!name gen816
#!mode=file
B0=$PWD
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set -o | md5sum
%%%
#!name gen817
#!mode=file
B0=$PWD
export -p | grep -c 'LINENO' 
%%%
#!name gen818
#!mode=file
B0=$PWD
unset -v OLDPWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen819
#!mode=file
B0=$PWD
set -- -a x; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen820
#!mode=file
B0=$PWD
env | grep -c '^MAILPATH='
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
export PPID='x=y'
%%%
#!name gen821
#!mode=file
B0=$PWD
g() { local vv B; vv='abc'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -ab -a x -a; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen822
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set -; echo "$-"
%%%
#!name gen823
#!mode=file
B0=$PWD
set -fE; echo "$-"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen824
#!mode=file
B0=$PWD
set +o | md5sum
g() { local -; set +E; echo "in:$-"; }; g; echo "out:$-"
set +o nounset; echo "$-"
%%%
#!name gen825
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen826
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
trap '' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen827
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen828
#!mode=file
B0=$PWD
set -- -a -a -b; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen829
#!mode=file
B0=$PWD
set -- -c; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen830
#!mode=file
B0=$PWD
env | grep -c '^LC_NUMERIC='
trap 'echo E' EXIT; echo body
%%%
#!name gen831
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen832
#!mode=file
B0=$PWD
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
set -; echo "$-"
set -; echo "$-"
%%%
#!name gen833
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- -z -bvalue -a x; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -e; echo "$-"
%%%
#!name gen834
#!mode=file
B0=$PWD
trap HUP 2>&1; trap; echo rc=$?
set -- -a -bvalue; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; a; alias a
%%%
#!name gen835
#!mode=file
B0=$PWD
g() { local IFS='POSIX'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set +o | md5sum
%%%
#!name gen836
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -vf; echo "$-"
%%%
#!name gen837
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
trap 'echo T' EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen838
#!mode=file
B0=$PWD
set -- -c -a x; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local V1='\t'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -; echo "$-"
%%%
#!name gen839
#!mode=file
B0=$PWD
g() { local A B; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
alias a='b'; alias b='echo B'; a
g() { local A=':'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen840
#!mode=file
B0=$PWD
g() { local -; set -a; echo "in:$-"; }; g; echo "out:$-"
trap '' 0 2>&1; trap; echo rc=$?
%%%
#!name gen841
#!mode=file
B0=$PWD
set -- -c -ab -a; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen842
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set -- -a x -a x; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen843
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen844
#!mode=file
B0=$PWD
g() { local V1; V1='C'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen845
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen846
#!mode=file
B0=$PWD
export -p | grep -c 'PS1' 
trap CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen847
#!mode=file
B0=$PWD
set +V; echo "$-"
set -fx; echo "$-"
%%%
#!name gen848
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen849
#!mode=file
B0=$PWD
set -- -a x x; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- --; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
%%%
#!name gen850
#!mode=file
B0=$PWD
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
set -- -a x -a -b; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen851
#!mode=file
B0=$PWD
trap 'echo T' QUIT 2>&1; trap; echo rc=$?
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
%%%
#!name gen852
#!mode=file
B0=$PWD
set -- -- -bvalue; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo A' 10 2>&1; trap - 10 2>&1; trap; echo rc=$?
%%%
#!name gen853
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen854
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen855
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen856
#!mode=file
B0=$PWD
set -- -- -ab; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen857
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
unset -v CDPATH
%%%
#!name gen858
#!mode=file
B0=$PWD
set -b; echo "$-"
unset -v IFS
%%%
#!name gen859
#!mode=file
B0=$PWD
trap USR1 2>&1; trap; echo rc=$?
%%%
#!name gen860
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='C'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set --; echo "$#"
%%%
#!name gen861
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
set -o vi; echo "$-"
%%%
#!name gen862
#!mode=file
B0=$PWD
set -- -c; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export B
(exec 3>s.txt; echo s >&3); cat s.txt; echo t >&3 2>&1; echo rc=$?
%%%
#!name gen863
#!mode=file
B0=$PWD
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen864
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen865
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen866
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen867
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen868
#!mode=file
B0=$PWD
g() { local vv B; vv=':'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -b; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -- -bvalue -b; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen869
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
{ echo p; } 4>&- ; echo rc=$?
%%%
#!name gen870
#!mode=file
B0=$PWD
set -- -a -b -a x -b; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen871
#!mode=file
B0=$PWD
g() { local vv B; vv=':'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen872
#!mode=file
B0=$PWD
set -- x -c; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen873
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
%%%
#!name gen874
#!mode=file
B0=$PWD
exec 5<&-; read v <&5 2>&1; echo rc=$?
%%%
#!name gen875
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
set -- -b x -a x; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen876
#!mode=file
B0=$PWD
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
trap 'echo T' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen877
#!mode=file
B0=$PWD
set -vi; echo "$-"
%%%
#!name gen878
#!mode=file
B0=$PWD
set -o monitor; echo "$-"
trap 'echo X' QUIT 2>&1; (trap); echo ---; trap
%%%
#!name gen879
#!mode=file
B0=$PWD
set -- -ab -b x; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen880
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
exec 9<&-; read v <&9 2>&1; echo rc=$?
%%%
#!name gen881
#!mode=file
B0=$PWD
set -- 1 b 1 b; echo "$#:$*"
env | grep -c '^LC_ALL='
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen882
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
readonly CDPATH='"q"'
%%%
#!name gen883
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -a -b -c -a -b; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local V1 B; V1='"q"'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen884
#!mode=file
B0=$PWD
set +o | md5sum
%%%
#!name gen885
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen886
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
g() { local PATH; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
echo "[${A-U}]"
%%%
#!name gen887
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen888
#!mode=file
B0=$PWD
set -- -ab; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local IFS='\t'; IFS='\t'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen889
#!mode=file
B0=$PWD
set -- -z -- -a; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen890
#!mode=file
B0=$PWD
set -- -z; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen891
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -z -a --; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -b -c; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen892
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
g() { local OPTIND='a'b'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set +o | md5sum
%%%
#!name gen893
#!mode=file
B0=$PWD
set --; echo "$#"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen894
#!mode=file
B0=$PWD
set --; echo "$#"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
echo "[${PS2-U}]"
%%%
#!name gen895
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
alias a='b'; alias b='echo B'; a
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen896
#!mode=file
B0=$PWD
echo "[${MAILPATH}]"
set | grep -c '^MAILPATH='
%%%
#!name gen897
#!mode=file
B0=$PWD
set +V; echo "$-"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen898
#!mode=file
B0=$PWD
set -- -a x -a -b; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -c -z -a; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
{ echo p; } 3>&- ; echo rc=$?
%%%
#!name gen899
#!mode=file
B0=$PWD
g() { local PATH; PATH='a b'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen900
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
trap 'echo T' 0 2>&1; trap; echo rc=$?
trap '' 10 2>&1; trap; echo rc=$?
%%%
#!name gen901
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set -; echo "$-"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen902
#!mode=file
B0=$PWD
export -p | grep -c 'HISTSIZE' 
trap 'echo X' 0 2>&1; (trap); echo ---; trap
%%%
#!name gen903
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen904
#!mode=file
B0=$PWD
set -- -- -a -b; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set | grep -c '^PS1='
g() { local V1; V1='.'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen905
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
echo "[${HOME}]"
%%%
#!name gen906
#!mode=file
B0=$PWD
readonly HISTSIZE=':'
%%%
#!name gen907
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
g() { local A='a b'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen908
#!mode=file
B0=$PWD
set -fV; echo "$-"
%%%
#!name gen909
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen910
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen911
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen912
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen913
#!mode=file
B0=$PWD
export IFS
alias a='b'; alias b='echo B'; a
%%%
#!name gen914
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
{ echo p; } 5>&- ; echo rc=$?
%%%
#!name gen915
#!mode=file
B0=$PWD
g() { local vv='abc'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen916
#!mode=file
B0=$PWD
{ echo p; } 5>&- ; echo rc=$?
export TERM
alias a='echo A'; a; alias a
%%%
#!name gen917
#!mode=file
B0=$PWD
PPID=''
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen918
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen919
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen920
#!mode=file
B0=$PWD
set -- -a x -a -b -ab; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen921
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen922
#!mode=file
B0=$PWD
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
alias a='b '; alias b='echo B'; alias c='echo C'; a c
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen923
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen924
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen925
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
trap TERM 2>&1; trap; echo rc=$?
%%%
#!name gen926
#!mode=file
B0=$PWD
trap 'echo T' 0 2>&1; trap; echo rc=$?
%%%
#!name gen927
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -ab -b; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
%%%
#!name gen928
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen929
#!mode=file
B0=$PWD
set -- -a x; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen930
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
set -o allexport; echo "$-"
trap 'echo T' ALRM 2>&1; trap; echo rc=$?
%%%
#!name gen931
#!mode=file
B0=$PWD
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
env | grep -c '^PATH='
%%%
#!name gen932
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
export -p | grep -c 'HOME' 
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen933
#!mode=file
B0=$PWD
env | grep -c '^B='
%%%
#!name gen934
#!mode=file
B0=$PWD
env | grep -c '^LC_CTYPE='
set -- --; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo "[${MAIL-U}]"
%%%
#!name gen935
#!mode=file
B0=$PWD
set | grep -c '^OLDPWD='
%%%
#!name gen936
#!mode=file
B0=$PWD
trap 'echo A' 10 2>&1; trap - 10 2>&1; trap; echo rc=$?
g() { local -; set -o noclobber; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen937
#!mode=file
B0=$PWD
set | grep -c '^V1='
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen938
#!mode=file
B0=$PWD
echo "[${LC_NUMERIC-U}]"
set -- -ab; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set | grep -c '^OPTIND='
%%%
#!name gen939
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -m; echo "$-"
%%%
#!name gen940
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
g() { local IFS='/tmp'; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen941
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen942
#!mode=file
B0=$PWD
set -- a b c d e; shift 0 2>&1; echo "rc=$? $#:$*"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen943
#!mode=file
B0=$PWD
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen944
#!mode=file
B0=$PWD
set -- -a -c; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen945
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
set -o | md5sum
%%%
#!name gen946
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set --; echo "$#"
alias a='b'; alias b='echo B'; a
%%%
#!name gen947
#!mode=file
B0=$PWD
PS1='1'
%%%
#!name gen948
#!mode=file
B0=$PWD
set -- -z --; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
cat <<E
line1
line2
E
%%%
#!name gen949
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen950
#!mode=file
B0=$PWD
env | grep -c '^MAILPATH='
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen951
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
alias a='echo A'; a; alias a
%%%
#!name gen952
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen953
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
trap 'echo E' EXIT; echo body
%%%
#!name gen954
#!mode=file
B0=$PWD
set -- -z -c --; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -ab -a x; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
env | grep -c '^OLDPWD='
%%%
#!name gen955
#!mode=file
B0=$PWD
set --; echo "$#"
alias 'a b'=1 2>&1; echo rc=$?
alias a='echo A'; a; alias a
%%%
#!name gen956
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias a='echo A'; a; alias a
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen957
#!mode=file
B0=$PWD
set -- -c; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen958
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
set -- b  1; echo "$#:$*"
%%%
#!name gen959
#!mode=file
B0=$PWD
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
g() { local vv='a b'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen960
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen961
#!mode=file
B0=$PWD
set | grep -c '^CDPATH='
set -o vi; echo "$-"
set -o vi; echo "$-"
%%%
#!name gen962
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen963
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -b; echo "$-"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen964
#!mode=file
B0=$PWD
g() { local V1=':'; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
g() { local OPTIND; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -I; echo "$-"
%%%
#!name gen965
#!mode=file
B0=$PWD
set -- -b -a -c; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen966
#!mode=file
B0=$PWD
g() { local PATH='C'; PATH='C'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
echo "[${LANG}]"
%%%
#!name gen967
#!mode=file
B0=$PWD
echo "[${HOME-U}]"
cat <<'E'
$notexpanded
E
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen968
#!mode=file
B0=$PWD
set -- -bvalue; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -; echo "$-"
%%%
#!name gen969
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen970
#!mode=file
B0=$PWD
trap 'echo A' CHLD 2>&1; trap - CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen971
#!mode=file
B0=$PWD
set -- -b -z -b; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen972
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen973
#!mode=file
B0=$PWD
set -- 1 a; echo "$#:$*"
alias a='echo A'; a; alias a
%%%
#!name gen974
#!mode=file
B0=$PWD
env | grep -c '^HISTSIZE='
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen975
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen976
#!mode=file
B0=$PWD
echo "[${LC_ALL-U}]"
g() { local B; B='/tmp'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
export HISTSIZE='a b'
%%%
#!name gen977
#!mode=file
B0=$PWD
set -- -z; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 4<<E
fromfd
E
read v <&4; echo "[$v]"; exec 4<&-
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen978
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set -- -c x; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen979
#!mode=file
B0=$PWD
g() { local -; set -i; echo "in:$-"; }; g; echo "out:$-"
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen980
#!mode=file
B0=$PWD
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
%%%
#!name gen981
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen982
#!mode=file
B0=$PWD
echo "[${LC_NUMERIC}]"
cat <<-E
	indented
	E
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen983
#!mode=file
B0=$PWD
unset LC_COLLATE
%%%
#!name gen984
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local -; set -m; echo "in:$-"; }; g; echo "out:$-"
echo "[${PWD#$B0}]"
%%%
#!name gen985
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen986
#!mode=file
B0=$PWD
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen987
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen988
#!mode=file
B0=$PWD
set -- -- -a -b; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen989
#!mode=file
B0=$PWD
set +C; echo "$-"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen990
#!mode=file
B0=$PWD
g() { local vv='\t'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen991
#!mode=file
B0=$PWD
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen992
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set +o ignoreeof; echo "$-"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen993
#!mode=file
B0=$PWD
set -- -ab; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -b -a x; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen994
#!mode=file
B0=$PWD
set -o nounset; echo "$-"
set --; echo "$#"
%%%
#!name gen995
#!mode=file
B0=$PWD
echo "[${OLDPWD#$B0}]"
%%%
#!name gen996
#!mode=file
B0=$PWD
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
unset OLDPWD
%%%
#!name gen997
#!mode=file
B0=$PWD
set --; echo "$#"
alias echo='echo pre'; echo hi
%%%
#!name gen998
#!mode=file
B0=$PWD
trap 'echo T' USR2 2>&1; trap; echo rc=$?
cat <<-E
	indented
	E
%%%
#!name gen999
#!mode=file
B0=$PWD
trap 'echo T' 12 2>&1; trap; echo rc=$?
set +e; echo "$-"
%%%
#!name gen1000
#!mode=file
B0=$PWD
set -a; echo "$-"
echo "[${PWD#$B0}]"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen1001
#!mode=file
B0=$PWD
unset -v MAIL
%%%
#!name gen1002
#!mode=file
B0=$PWD
g() { local A='C'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
cat <<E
line1
line2
E
g() { local IFS='\t'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1003
#!mode=file
B0=$PWD
set -- -z -a -b; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
PS1='x=y'
%%%
#!name gen1004
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1005
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -o noclobber; echo "$-"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1006
#!mode=file
B0=$PWD
export -p | grep -c 'B' 
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
trap 'echo X' INT 2>&1; (trap); echo ---; trap
%%%
#!name gen1007
#!mode=file
B0=$PWD
set +x; echo "$-"
set +o noexec; echo "$-"
%%%
#!name gen1008
#!mode=file
B0=$PWD
unset LINENO
env | grep -c '^IFS='
%%%
#!name gen1009
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen1010
#!mode=file
B0=$PWD
export -p | grep -c 'HISTSIZE' 
g() { local V1='C'; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1011
#!mode=file
B0=$PWD
g() { local IFS B; IFS='"q"'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
g() { local vv=':'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1012
#!mode=file
B0=$PWD
set -- -bvalue x -bvalue; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1013
#!mode=file
B0=$PWD
set -- a; echo "$#:$*"
%%%
#!name gen1014
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1015
#!mode=file
B0=$PWD
echo "[${HISTSIZE}]"
%%%
#!name gen1016
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1017
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1018
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- a; echo "$#:$*"
readonly A='/tmp'
%%%
#!name gen1019
#!mode=file
B0=$PWD
set -- -a x; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -- --; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo X' QUIT 2>&1; (trap); echo ---; trap
%%%
#!name gen1020
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
g() { local vv=':'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1021
#!mode=file
B0=$PWD
env | grep -c '^vv='
export LINENO
set -- -- -a x; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1022
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
trap 'echo T' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen1023
#!mode=file
B0=$PWD
g() { local PATH='1'; PATH='1'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1024
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1025
#!mode=file
B0=$PWD
export IFS='a'b'
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -- -bvalue; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1026
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen1027
#!mode=file
B0=$PWD
set -- x -z -ab; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1028
#!mode=file
B0=$PWD
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
g() { local IFS; IFS='\t'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1029
#!mode=file
B0=$PWD
set -- -c; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1030
#!mode=file
B0=$PWD
set +o nolog; echo "$-"
g() { local B B; B='1'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1031
#!mode=file
B0=$PWD
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1032
#!mode=file
B0=$PWD
export -p | grep -c 'MAIL' 
%%%
#!name gen1033
#!mode=file
B0=$PWD
trap USR1 2>&1; trap; echo rc=$?
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
%%%
#!name gen1034
#!mode=file
B0=$PWD
set -- -b; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1035
#!mode=file
B0=$PWD
trap 'echo X' 1 2>&1; (trap); echo ---; trap
%%%
#!name gen1036
#!mode=file
B0=$PWD
set +o | md5sum
%%%
#!name gen1037
#!mode=file
B0=$PWD
set -o nolog; echo "$-"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1038
#!mode=file
B0=$PWD
set +o allexport; echo "$-"
echo "[${LC_CTYPE-U}]"
%%%
#!name gen1039
#!mode=file
B0=$PWD
trap '' PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen1040
#!mode=file
B0=$PWD
unset PWD
%%%
#!name gen1041
#!mode=file
B0=$PWD
set -o | md5sum
alias a='b'; alias b='echo B'; a
%%%
#!name gen1042
#!mode=file
B0=$PWD
set -- a b; echo "$#:$*"
trap '' PIPE 2>&1; trap; echo rc=$?
{ echo p; } 4>&- ; echo rc=$?
%%%
#!name gen1043
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1044
#!mode=file
B0=$PWD
OPTIND='abc'
%%%
#!name gen1045
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
echo "[${CDPATH-U}]"
%%%
#!name gen1046
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
%%%
#!name gen1047
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1048
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen1049
#!mode=file
B0=$PWD
g() { local IFS='"q"'; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1050
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1051
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1052
#!mode=file
B0=$PWD
set -- -z; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1053
#!mode=file
B0=$PWD
trap '' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen1054
#!mode=file
B0=$PWD
set -o | md5sum
set -- -z -a -b -z; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1055
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
set | grep -c '^vv='
{ echo p; } 6>&- ; echo rc=$?
%%%
#!name gen1056
#!mode=file
B0=$PWD
trap 'echo X' ALRM 2>&1; (trap); echo ---; trap
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1057
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -- -z; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<'E'
$notexpanded
E
%%%
#!name gen1058
#!mode=file
B0=$PWD
trap USR1 2>&1; trap; echo rc=$?
%%%
#!name gen1059
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
export LC_NUMERIC='C'
set --; echo "$#"
%%%
#!name gen1060
#!mode=file
B0=$PWD
export -p | grep -c 'LINENO' 
%%%
#!name gen1061
#!mode=file
B0=$PWD
set -n; echo "$-"
cat <<E
line1
line2
E
set -- -b x; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1062
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set -aa; echo "$-"
set -- a b c d e; shift 0 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1063
#!mode=file
B0=$PWD
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1064
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1065
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
alias a='b'; alias b='echo B'; a
%%%
#!name gen1066
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
alias echo='echo pre'; echo hi
g() { local B; B='a b'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1067
#!mode=file
B0=$PWD
set -- -a -b; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1068
#!mode=file
B0=$PWD
trap 'echo X' 12 2>&1; (trap); echo ---; trap
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
unset PWD
%%%
#!name gen1069
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
set -Ce; echo "$-"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1070
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1071
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
set -Ex; echo "$-"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1072
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1073
#!mode=file
B0=$PWD
set -- -ab -ab --; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; a; alias a
%%%
#!name gen1074
#!mode=file
B0=$PWD
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
%%%
#!name gen1075
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1076
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen1077
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
set -- -a -a; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen1078
#!mode=file
B0=$PWD
g() { local -; set -m; echo "in:$-"; }; g; echo "out:$-"
alias a='echo A'; a; alias a
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1079
#!mode=file
B0=$PWD
set -x; echo "$-"
trap 0 2>&1; trap; echo rc=$?
%%%
#!name gen1080
#!mode=file
B0=$PWD
g() { local vv='a'b'; vv='a'b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1081
#!mode=file
B0=$PWD
set -- 1; echo "$#:$*"
%%%
#!name gen1082
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1083
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap 'echo T' ALRM 2>&1; trap; echo rc=$?
echo "[${LINENO-U}]"
%%%
#!name gen1084
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
g() { local -; set -o allexport; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1085
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1086
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
read v <<E
value
E
echo "[$v]"
alias a='echo A'; a; alias a
%%%
#!name gen1087
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
%%%
#!name gen1088
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
alias echo='echo pre'; echo hi
%%%
#!name gen1089
#!mode=file
B0=$PWD
set -- -z -a -b -b; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -ab -a x; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1090
#!mode=file
B0=$PWD
set -- a  1 c; echo "$#:$*"
set -- -a x --; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1091
#!mode=file
B0=$PWD
g() { local V1 B; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
unset -v B
%%%
#!name gen1092
#!mode=file
B0=$PWD
set -- 1  c b; echo "$#:$*"
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
set -- -c -a x; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1093
#!mode=file
B0=$PWD
set -- -a x -a; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo "[${LC_NUMERIC-U}]"
set -- -- -a -b -a; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1094
#!mode=file
B0=$PWD
set -- -z -b -a x; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1095
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
alias a='b'; alias b='echo B'; a
%%%
#!name gen1096
#!mode=file
B0=$PWD
set -- -b -b -a; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1097
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1098
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local -; set -o vi; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1099
#!mode=file
B0=$PWD
set -- -b -b; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x -z -bvalue; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen1100
#!mode=file
B0=$PWD
exec 5<&-; read v <&5 2>&1; echo rc=$?
cat <<'E'
$notexpanded
E
export LC_NUMERIC
%%%
#!name gen1101
#!mode=file
B0=$PWD
set +V; echo "$-"
set -vE; echo "$-"
set -- ; echo "$#:$*"
%%%
#!name gen1102
#!mode=file
B0=$PWD
g() { local -; set +E; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1103
#!mode=file
B0=$PWD
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
g() { local -; set -f; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1104
#!mode=file
B0=$PWD
g() { local vv; vv='C'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
{ echo p; } 4>&- ; echo rc=$?
unset -v MAILPATH
%%%
#!name gen1105
#!mode=file
B0=$PWD
set -- -ab; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -b -a -b; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen1106
#!mode=file
B0=$PWD
set -- -b -a -a x; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x -a -a; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
%%%
#!name gen1107
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
alias a='b'; alias b='echo B'; a
set -- x -z -bvalue; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1108
#!mode=file
B0=$PWD
set -- -c; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b'; alias b='echo B'; a
%%%
#!name gen1109
#!mode=file
B0=$PWD
env | grep -c '^LC_NUMERIC='
set -- -a -b -a; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1110
#!mode=file
B0=$PWD
export LANG='\t'
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
%%%
#!name gen1111
#!mode=file
B0=$PWD
set -- -bvalue -z -a; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local OPTIND; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1112
#!mode=file
B0=$PWD
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
%%%
#!name gen1113
#!mode=file
B0=$PWD
g() { local V1; V1='C'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -- -- -a -a -b; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1114
#!mode=file
B0=$PWD
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
exec 5>&1; echo x >&5; exec 5>&-; echo y >&5 2>&1; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1115
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
trap 'echo X' 12 2>&1; (trap); echo ---; trap
%%%
#!name gen1116
#!mode=file
B0=$PWD
set -- x -- -ab; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local -; set -o ignoreeof; echo "in:$-"; }; g; echo "out:$-"
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
%%%
#!name gen1117
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
%%%
#!name gen1118
#!mode=file
B0=$PWD
env | grep -c '^CDPATH='
%%%
#!name gen1119
#!mode=file
B0=$PWD
set +o errexit; echo "$-"
alias echo='echo pre'; echo hi
%%%
#!name gen1120
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1121
#!mode=file
B0=$PWD
g() { local B='abc'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1122
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -; echo "$-"
echo "[${HISTSIZE}]"
%%%
#!name gen1123
#!mode=file
B0=$PWD
set -- x -c -z; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1124
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
export vv
%%%
#!name gen1125
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='.'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -- --; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo "[${PS4-U}]"
%%%
#!name gen1126
#!mode=file
B0=$PWD
unset -v PS1
%%%
#!name gen1127
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
alias a='b '; alias b='echo B'; alias c='echo C'; a c
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1128
#!mode=file
B0=$PWD
set -eE; echo "$-"
%%%
#!name gen1129
#!mode=file
B0=$PWD
trap 'echo T' QUIT 2>&1; trap; echo rc=$?
set -- -bvalue -b; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo X' HUP 2>&1; (trap); echo ---; trap
%%%
#!name gen1130
#!mode=file
B0=$PWD
set -- -a x -a -b -ab; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo T' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen1131
#!mode=file
B0=$PWD
set -- x; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set | grep -c '^LC_NUMERIC='
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1132
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1133
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1134
#!mode=file
B0=$PWD
set -- -bvalue -z; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap '' QUIT 2>&1; trap; echo rc=$?
trap 'echo T' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen1135
#!mode=file
B0=$PWD
OLDPWD='C'
trap 'echo X' INT 2>&1; (trap); echo ---; trap
%%%
#!name gen1136
#!mode=file
B0=$PWD
g() { local PATH='1'; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1137
#!mode=file
B0=$PWD
set -- ; echo "$#:$*"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1138
#!mode=file
B0=$PWD
g() { local vv B; vv='POSIX'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
trap INT 2>&1; trap; echo rc=$?
%%%
#!name gen1139
#!mode=file
B0=$PWD
g() { local IFS B; IFS='x=y'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1140
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- -a x x; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1141
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1142
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen1143
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen1144
#!mode=file
B0=$PWD
trap 'echo T' EXIT 2>&1; trap; echo rc=$?
echo "[${vv-U}]"
%%%
#!name gen1145
#!mode=file
B0=$PWD
export -p | grep -c 'HISTSIZE' 
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
echo "[${OPTIND}]"
%%%
#!name gen1146
#!mode=file
B0=$PWD
env | grep -c '^PWD='
%%%
#!name gen1147
#!mode=file
B0=$PWD
g() { local V1='\t'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1148
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen1149
#!mode=file
B0=$PWD
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1150
#!mode=file
B0=$PWD
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
g() { local vv='/tmp'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1151
#!mode=file
B0=$PWD
export -p | grep -c 'MAIL' 
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1152
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set -- a a; echo "$#:$*"
%%%
#!name gen1153
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1154
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen1155
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1156
#!mode=file
B0=$PWD
g() { local -; set +e; echo "in:$-"; }; g; echo "out:$-"
alias a='echo A'; a; alias a
set -- x --; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1157
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
export PWD
%%%
#!name gen1158
#!mode=file
B0=$PWD
g() { local IFS='POSIX'; IFS='POSIX'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
g() { local V1 B; V1='C'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1159
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen1160
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1161
#!mode=file
B0=$PWD
g() { local A B; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local V1='x=y'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -- x -a -b -ab; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1162
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1163
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
cd /nope 2>&1; echo rc=$?
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
%%%
#!name gen1164
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
g() { local vv='x=y'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1165
#!mode=file
B0=$PWD
set -- -b -ab; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1166
#!mode=file
B0=$PWD
export LC_COLLATE
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
g() { local PATH='a b'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1167
#!mode=file
B0=$PWD
export HISTSIZE='\t'
%%%
#!name gen1168
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
exec 5<&-; read v <&5 2>&1; echo rc=$?
set +o | md5sum
%%%
#!name gen1169
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen1170
#!mode=file
B0=$PWD
export MAIL='x=y'
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1171
#!mode=file
B0=$PWD
env | grep -c '^LC_CTYPE='
%%%
#!name gen1172
#!mode=file
B0=$PWD
set -- -a -z -bvalue; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap INT 2>&1; trap; echo rc=$?
%%%
#!name gen1173
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
alias a='b'; alias b='echo B'; a
%%%
#!name gen1174
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1175
#!mode=file
B0=$PWD
g() { local B; B='"q"'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
{ echo p; } 6>&- ; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1176
#!mode=file
B0=$PWD
readonly PS2='a'b'
%%%
#!name gen1177
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
set -- b a; echo "$#:$*"
%%%
#!name gen1178
#!mode=file
B0=$PWD
set -- -bvalue -a -b -z; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1179
#!mode=file
B0=$PWD
trap '' 10 2>&1; trap; echo rc=$?
trap 'echo E' EXIT; echo body
%%%
#!name gen1180
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
{ echo p; } 6>&- ; echo rc=$?
%%%
#!name gen1181
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
set -- -b -a x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1182
#!mode=file
B0=$PWD
PPID='1'
%%%
#!name gen1183
#!mode=file
B0=$PWD
set -- a   b; echo "$#:$*"
set -- -z -z -z; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1184
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
%%%
#!name gen1185
#!mode=file
B0=$PWD
trap '' 12 2>&1; trap; echo rc=$?
%%%
#!name gen1186
#!mode=file
B0=$PWD
set | grep -c '^CDPATH='
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
g() { local PATH='.'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1187
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
alias echo='echo pre'; echo hi
set -- -z -z; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1188
#!mode=file
B0=$PWD
g() { local A B; A='x=y'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1189
#!mode=file
B0=$PWD
set -- -bvalue; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local -; set -n; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1190
#!mode=file
B0=$PWD
trap TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1191
#!mode=file
B0=$PWD
set -- -a -b -c; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -eE; echo "$-"
export LC_COLLATE='1'
%%%
#!name gen1192
#!mode=file
B0=$PWD
export vv='a b'
%%%
#!name gen1193
#!mode=file
B0=$PWD
set --  1 c a; echo "$#:$*"
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
g() { local -; set -I; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1194
#!mode=file
B0=$PWD
g() { local -; set +x; echo "in:$-"; }; g; echo "out:$-"
read v <<E
value
E
echo "[$v]"
set -- -bvalue -- -b; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1195
#!mode=file
B0=$PWD
readonly MAILPATH='.'
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1196
#!mode=file
B0=$PWD
trap 'echo X' HUP 2>&1; (trap); echo ---; trap
cat <<E
line1
line2
E
%%%
#!name gen1197
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1198
#!mode=file
B0=$PWD
unset V1
trap '' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen1199
#!mode=file
B0=$PWD
exec 9>r9.txt; echo hi >&9; exec 9>&-; cat r9.txt
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
export LC_ALL
%%%
#!name gen1200
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1201
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set +o | md5sum
%%%
#!name gen1202
#!mode=file
B0=$PWD
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
%%%
#!name gen1203
#!mode=file
B0=$PWD
set +o ignoreeof; echo "$-"
set -- a b; echo "$#:$*"
trap '' PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen1204
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
g() { local B='\t'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1205
#!mode=file
B0=$PWD
set -- -a -b -b; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a x -z; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1206
#!mode=file
B0=$PWD
set -- -a -b -a; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1207
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
cat <<'E'
$notexpanded
E
%%%
#!name gen1208
#!mode=file
B0=$PWD
echo "[${V1-U}]"
export A
%%%
#!name gen1209
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen1210
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
set +o errexit; echo "$-"
%%%
#!name gen1211
#!mode=file
B0=$PWD
exec 9<&-; read v <&9 2>&1; echo rc=$?
unset -v IFS
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1212
#!mode=file
B0=$PWD
echo "[${OPTIND-U}]"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1213
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -o monitor; echo "$-"
set +o | md5sum
%%%
#!name gen1214
#!mode=file
B0=$PWD
trap 'echo T' 0 2>&1; trap; echo rc=$?
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
cat <<E
line1
line2
E
%%%
#!name gen1215
#!mode=file
B0=$PWD
trap '' USR1 2>&1; trap; echo rc=$?
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1216
#!mode=file
B0=$PWD
export MAIL
%%%
#!name gen1217
#!mode=file
B0=$PWD
g() { local V1; V1=':'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
TERM=''
%%%
#!name gen1218
#!mode=file
B0=$PWD
set -EV; echo "$-"
unset -v HISTSIZE
%%%
#!name gen1219
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
set -; echo "$-"
readonly PWD='a'b'
%%%
#!name gen1220
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen1221
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- --; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen1222
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen1223
#!mode=file
B0=$PWD
trap '' ALRM 2>&1; trap; echo rc=$?
set +x; echo "$-"
trap 'echo T' CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen1224
#!mode=file
B0=$PWD
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
set -- -a; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1225
#!mode=file
B0=$PWD
trap 'echo T' QUIT 2>&1; trap; echo rc=$?
%%%
#!name gen1226
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
set -aa; echo "$-"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1227
#!mode=file
B0=$PWD
g() { local V1 B; V1='\t'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1228
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
set -- x -b -b; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1229
#!mode=file
B0=$PWD
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
%%%
#!name gen1230
#!mode=file
B0=$PWD
trap 'echo X' 0 2>&1; (trap); echo ---; trap
trap 'echo T' INT 2>&1; trap; echo rc=$?
%%%
#!name gen1231
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -o emacs; echo "$-"
%%%
#!name gen1232
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen1233
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1234
#!mode=file
B0=$PWD
set +o | md5sum
g() { local V1 B; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1235
#!mode=file
B0=$PWD
export LC_COLLATE='C'
%%%
#!name gen1236
#!mode=file
B0=$PWD
echo "[${A-U}]"
echo "[${B-U}]"
%%%
#!name gen1237
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1238
#!mode=file
B0=$PWD
set | grep -c '^MAILPATH='
env | grep -c '^OPTIND='
trap 'echo X' INT 2>&1; (trap); echo ---; trap
%%%
#!name gen1239
#!mode=file
B0=$PWD
set +u; echo "$-"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1240
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
set -- --; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1241
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
set -- -ab -a -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset -v OPTIND
%%%
#!name gen1242
#!mode=file
B0=$PWD
set -- -z x -b; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1243
#!mode=file
B0=$PWD
set -; echo "$-"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set --; echo "$#"
%%%
#!name gen1244
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1245
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
export -p | grep -c 'B' 
%%%
#!name gen1246
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
trap PIPE 2>&1; trap; echo rc=$?
export PS4='POSIX'
%%%
#!name gen1247
#!mode=file
B0=$PWD
unset PS2
%%%
#!name gen1248
#!mode=file
B0=$PWD
set -o noexec; echo "$-"
%%%
#!name gen1249
#!mode=file
B0=$PWD
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
set -- -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1250
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1251
#!mode=file
B0=$PWD
trap 10 2>&1; trap; echo rc=$?
f() { echo F; }; f >f.txt 2>&1; cat f.txt
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
%%%
#!name gen1252
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
set --; echo "$#"
PS1='/tmp'
%%%
#!name gen1253
#!mode=file
B0=$PWD
set | grep -c '^LANG='
%%%
#!name gen1254
#!mode=file
B0=$PWD
set +b; echo "$-"
g() { local B='0'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1255
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen1256
#!mode=file
B0=$PWD
echo "[${HOME-U}]"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1257
#!mode=file
B0=$PWD
g() { local OPTIND='0'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
{ echo p; } 4>&- ; echo rc=$?
set -- x -bvalue -ab; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1258
#!mode=file
B0=$PWD
trap '' TERM 2>&1; trap; echo rc=$?
g() { local B B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1259
#!mode=file
B0=$PWD
echo "[${PPID-U}]"
%%%
#!name gen1260
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
%%%
#!name gen1261
#!mode=file
B0=$PWD
set -- -a x; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export -p | grep -c 'PS2' 
%%%
#!name gen1262
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
g() { local vv='/tmp'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1263
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen1264
#!mode=file
B0=$PWD
set +i; echo "$-"
%%%
#!name gen1265
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
trap CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen1266
#!mode=file
B0=$PWD
unset TERM
%%%
#!name gen1267
#!mode=file
B0=$PWD
g() { local B B; B='POSIX'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1268
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
trap EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen1269
#!mode=file
B0=$PWD
set --; echo "$#"
cat <<'E'
$notexpanded
E
g() { local A B; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1270
#!mode=file
B0=$PWD
set -- -z --; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
readonly PS4='a b'
V1='x=y'
%%%
#!name gen1271
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen1272
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1273
#!mode=file
B0=$PWD
set -- -a -b -b -b; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local V1='/tmp'; V1='/tmp'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1274
#!mode=file
B0=$PWD
set -uv; echo "$-"
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen1275
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
alias echo='echo pre'; echo hi
%%%
#!name gen1276
#!mode=file
B0=$PWD
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
set -- -- x; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1277
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
alias a='echo A'; a; alias a
g() { local A='\t'; A='\t'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1278
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen1279
#!mode=file
B0=$PWD
set -- -- -c; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1280
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1281
#!mode=file
B0=$PWD
set --; echo "$#"
set -- a b c d e; shift 0 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1282
#!mode=file
B0=$PWD
env | grep -c '^V1='
%%%
#!name gen1283
#!mode=file
B0=$PWD
trap ALRM 2>&1; trap; echo rc=$?
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1284
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -f; echo "$-"
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
%%%
#!name gen1285
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen1286
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
trap 'echo E' EXIT; echo body
%%%
#!name gen1287
#!mode=file
B0=$PWD
g() { local B B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen1288
#!mode=file
B0=$PWD
exec 6<<E
fromfd
E
read v <&6; echo "[$v]"; exec 6<&-
%%%
#!name gen1289
#!mode=file
B0=$PWD
unset vv
%%%
#!name gen1290
#!mode=file
B0=$PWD
export LC_NUMERIC=''
set -mb; echo "$-"
set -- -ab; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1291
#!mode=file
B0=$PWD
set -- x -b; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set --; echo "$#"
%%%
#!name gen1292
#!mode=file
B0=$PWD
g() { local V1='"q"'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1293
#!mode=file
B0=$PWD
env | grep -c '^IFS='
trap USR2 2>&1; trap; echo rc=$?
alias a='echo A'; a; alias a
%%%
#!name gen1294
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set -- -c -b -c; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1295
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
{ echo p; } 3>&- ; echo rc=$?
set -- -ab; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1296
#!mode=file
B0=$PWD
export OPTIND
%%%
#!name gen1297
#!mode=file
B0=$PWD
g() { local IFS=''; IFS=''; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
export LC_COLLATE='C'
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1298
#!mode=file
B0=$PWD
readonly V1='/tmp'
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen1299
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen1300
#!mode=file
B0=$PWD
env | grep -c '^LANG='
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen1301
#!mode=file
B0=$PWD
LC_ALL='abc'
cd /nope 2>&1; echo rc=$?
cat <<'E'
$notexpanded
E
%%%
#!name gen1302
#!mode=file
B0=$PWD
set --; echo "$#"
echo "[${vv}]"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1303
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
{ echo p; } 9>&- ; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1304
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen1305
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1306
#!mode=file
B0=$PWD
set -b; echo "$-"
%%%
#!name gen1307
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen1308
#!mode=file
B0=$PWD
g() { local B; B=':'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1309
#!mode=file
B0=$PWD
LC_COLLATE='"q"'
%%%
#!name gen1310
#!mode=file
B0=$PWD
trap '' USR2 2>&1; trap; echo rc=$?
export -p | grep -c 'IFS' 
%%%
#!name gen1311
#!mode=file
B0=$PWD
set -o noglob; echo "$-"
set --  1 b b; echo "$#:$*"
%%%
#!name gen1312
#!mode=file
B0=$PWD
set -vb; echo "$-"
set -fu; echo "$-"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1313
#!mode=file
B0=$PWD
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1314
#!mode=file
B0=$PWD
set -o ignoreeof; echo "$-"
g() { local vv='a b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1315
#!mode=file
B0=$PWD
set -o verbose; echo "$-"
%%%
#!name gen1316
#!mode=file
B0=$PWD
g() { local OPTIND; OPTIND='"q"'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1317
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
readonly MAIL='1'
g() { local OPTIND B; OPTIND='x=y'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1318
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen1319
#!mode=file
B0=$PWD
g() { local IFS='C'; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
g() { local -; set +n; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1320
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
g() { local -; set +V; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1321
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1322
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1323
#!mode=file
B0=$PWD
g() { local -; set -o pipefail; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1324
#!mode=file
B0=$PWD
set -o notify; echo "$-"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set +i; echo "$-"
%%%
#!name gen1325
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
g() { local IFS='"q"'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set | grep -c '^A='
%%%
#!name gen1326
#!mode=file
B0=$PWD
set -- -a x -c; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap USR2 2>&1; trap; echo rc=$?
%%%
#!name gen1327
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1328
#!mode=file
B0=$PWD
set -o monitor; echo "$-"
%%%
#!name gen1329
#!mode=file
B0=$PWD
trap 'echo T' 0 2>&1; trap; echo rc=$?
set +o ignoreeof; echo "$-"
trap 'echo E' EXIT; echo body
%%%
#!name gen1330
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
g() { local OPTIND B; OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap 'echo A' QUIT 2>&1; trap - QUIT 2>&1; trap; echo rc=$?
%%%
#!name gen1331
#!mode=file
B0=$PWD
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
set -- -z; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
env | grep -c '^PS4='
%%%
#!name gen1332
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1333
#!mode=file
B0=$PWD
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1334
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- -a x; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1335
#!mode=file
B0=$PWD
set -- x -a; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local IFS B; IFS='1'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1336
#!mode=file
B0=$PWD
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
export LC_NUMERIC
%%%
#!name gen1337
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1338
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1339
#!mode=file
B0=$PWD
env | grep -c '^LINENO='
%%%
#!name gen1340
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
trap 'echo A' USR2 2>&1; trap - USR2 2>&1; trap; echo rc=$?
%%%
#!name gen1341
#!mode=file
B0=$PWD
trap '' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen1342
#!mode=file
B0=$PWD
set +a; echo "$-"
set -im; echo "$-"
%%%
#!name gen1343
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local B='abc'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1344
#!mode=file
B0=$PWD
set +o | md5sum
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1345
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
alias echo='echo pre'; echo hi
%%%
#!name gen1346
#!mode=file
B0=$PWD
set -- -a x -ab; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1347
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
%%%
#!name gen1348
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
unset LC_NUMERIC
%%%
#!name gen1349
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
set +o nounset; echo "$-"
%%%
#!name gen1350
#!mode=file
B0=$PWD
echo "[${CDPATH-U}]"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1351
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen1352
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1353
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1354
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1355
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen1356
#!mode=file
B0=$PWD
g() { local V1='x=y'; V1='x=y'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1357
#!mode=file
B0=$PWD
set -- -z; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1358
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1359
#!mode=file
B0=$PWD
{ echo p; } 4>&- ; echo rc=$?
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1360
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen1361
#!mode=file
B0=$PWD
set -- -a x; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -a -bvalue; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local -; set -o notify; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1362
#!mode=file
B0=$PWD
g() { local -; set -E; echo "in:$-"; }; g; echo "out:$-"
echo "[${LC_ALL}]"
%%%
#!name gen1363
#!mode=file
B0=$PWD
set +o | md5sum
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1364
#!mode=file
B0=$PWD
set -- -- -c; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local A='\t'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1365
#!mode=file
B0=$PWD
set -- -ab; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1366
#!mode=file
B0=$PWD
g() { local V1 B; V1='abc'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1367
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
alias a='echo A'; a; alias a
set -- -c -c; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1368
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen1369
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
unset OPTIND
%%%
#!name gen1370
#!mode=file
B0=$PWD
echo "[${MAIL-U}]"
alias a='echo A'; a; alias a
trap 'echo E' EXIT; echo body
%%%
#!name gen1371
#!mode=file
B0=$PWD
echo "[${MAILPATH-U}]"
g() { local B='a'b'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1372
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
echo "[${LC_ALL}]"
set +o allexport; echo "$-"
%%%
#!name gen1373
#!mode=file
B0=$PWD
env | grep -c '^HISTSIZE='
%%%
#!name gen1374
#!mode=file
B0=$PWD
trap HUP 2>&1; trap; echo rc=$?
%%%
#!name gen1375
#!mode=file
B0=$PWD
set -- -z -a x --; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -; echo "$-"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1376
#!mode=file
B0=$PWD
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
echo "[${LC_COLLATE-U}]"
set | grep -c '^LC_CTYPE='
%%%
#!name gen1377
#!mode=file
B0=$PWD
g() { local -; set -o nolog; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1378
#!mode=file
B0=$PWD
set +o errexit; echo "$-"
g() { local B B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1379
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set --; echo "$#"
%%%
#!name gen1380
#!mode=file
B0=$PWD
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1381
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen1382
#!mode=file
B0=$PWD
set +o noexec; echo "$-"
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
%%%
#!name gen1383
#!mode=file
B0=$PWD
set -o | md5sum
%%%
#!name gen1384
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1385
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
cat <<E
line1
line2
E
%%%
#!name gen1386
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
set -u; echo "$-"
%%%
#!name gen1387
#!mode=file
B0=$PWD
g() { local -; set +i; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1388
#!mode=file
B0=$PWD
g() { local A B; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1389
#!mode=file
B0=$PWD
set -o | md5sum
set -- -c -a x -a; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1390
#!mode=file
B0=$PWD
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
set -- -z -a -b -c; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1391
#!mode=file
B0=$PWD
g() { local V1 B; V1=''; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1392
#!mode=file
B0=$PWD
g() { local A='\t'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
cat <<E
line1
line2
E
set -- -a -b x -c; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1393
#!mode=file
B0=$PWD
set -- --; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set +o noglob; echo "$-"
%%%
#!name gen1394
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias echo='echo pre'; echo hi
%%%
#!name gen1395
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
exec 6>&1; echo x >&6; exec 6>&-; echo y >&6 2>&1; echo rc=$?
set -- -a x --; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1396
#!mode=file
B0=$PWD
set -- -z -a; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -e; echo "$-"
%%%
#!name gen1397
#!mode=file
B0=$PWD
set -- -z -b; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -c -a -b -a x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1398
#!mode=file
B0=$PWD
g() { local -; set -o verbose; echo "in:$-"; }; g; echo "out:$-"
set -; echo "$-"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1399
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1400
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen1401
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1402
#!mode=file
B0=$PWD
set -Vm; echo "$-"
%%%
#!name gen1403
#!mode=file
B0=$PWD
export -p | grep -c 'OLDPWD' 
%%%
#!name gen1404
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
set -- -z -a --; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1405
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1406
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1407
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set +o | md5sum
export OPTIND='C'
%%%
#!name gen1408
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
alias echo='echo pre'; echo hi
%%%
#!name gen1409
#!mode=file
B0=$PWD
g() { local -; set -o allexport; echo "in:$-"; }; g; echo "out:$-"
unset -v LC_COLLATE
export PATH='1'
%%%
#!name gen1410
#!mode=file
B0=$PWD
unset -v IFS
%%%
#!name gen1411
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1412
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen1413
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1414
#!mode=file
B0=$PWD
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
read v <<E
value
E
echo "[$v]"
%%%
#!name gen1415
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1416
#!mode=file
B0=$PWD
export PS1
alias a='echo A'; a; alias a
%%%
#!name gen1417
#!mode=file
B0=$PWD
set -- x; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1418
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen1419
#!mode=file
B0=$PWD
set -- -a; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
%%%
#!name gen1420
#!mode=file
B0=$PWD
set --; echo "$#"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1421
#!mode=file
B0=$PWD
trap '' HUP 2>&1; trap; echo rc=$?
trap 10 2>&1; trap; echo rc=$?
%%%
#!name gen1422
#!mode=file
B0=$PWD
unset LINENO
g() { local -; set -C; echo "in:$-"; }; g; echo "out:$-"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1423
#!mode=file
B0=$PWD
export PATH='\t'
%%%
#!name gen1424
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1425
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
readonly LANG='POSIX'
%%%
#!name gen1426
#!mode=file
B0=$PWD
LC_NUMERIC='C'
%%%
#!name gen1427
#!mode=file
B0=$PWD
trap '' 12 2>&1; trap; echo rc=$?
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
export LC_NUMERIC
%%%
#!name gen1428
#!mode=file
B0=$PWD
g() { local vv; vv='C'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
export OPTIND=''
trap '' TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1429
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
unset -v PWD
set -- -a -b; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1430
#!mode=file
B0=$PWD
set --; echo "$#"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
g() { local V1; V1='abc'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1431
#!mode=file
B0=$PWD
set -o | md5sum
trap 'echo E' EXIT; echo body
%%%
#!name gen1432
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen1433
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1434
#!mode=file
B0=$PWD
set -- -bvalue --; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set | grep -c '^HOME='
exec 9>r9.txt; echo hi >&9; exec 9>&-; cat r9.txt
%%%
#!name gen1435
#!mode=file
B0=$PWD
set -- -ab x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -b; echo "$-"
trap 'echo E' EXIT; echo body
%%%
#!name gen1436
#!mode=file
B0=$PWD
trap 'echo X' USR2 2>&1; (trap); echo ---; trap
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1437
#!mode=file
B0=$PWD
set -eI; echo "$-"
g() { local A; A='x=y'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1438
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen1439
#!mode=file
B0=$PWD
set -- c 1 1 1; echo "$#:$*"
%%%
#!name gen1440
#!mode=file
B0=$PWD
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1441
#!mode=file
B0=$PWD
g() { local A B; A='0'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
trap 'echo A' USR1 2>&1; trap - USR1 2>&1; trap; echo rc=$?
%%%
#!name gen1442
#!mode=file
B0=$PWD
set +o | md5sum
%%%
#!name gen1443
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1444
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1445
#!mode=file
B0=$PWD
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1446
#!mode=file
B0=$PWD
set -- -z; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1447
#!mode=file
B0=$PWD
set +o pipefail; echo "$-"
%%%
#!name gen1448
#!mode=file
B0=$PWD
env | grep -c '^PATH='
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1449
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen1450
#!mode=file
B0=$PWD
B='1'
set | grep -c '^LC_NUMERIC='
set -- -a; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1451
#!mode=file
B0=$PWD
set -VV; echo "$-"
set -o ignoreeof; echo "$-"
alias a='echo A'; a; alias a
%%%
#!name gen1452
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen1453
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1454
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
trap '' ALRM 2>&1; trap; echo rc=$?
set -- -ab -a -b -a -b; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1455
#!mode=file
B0=$PWD
set -; echo "$-"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1456
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1457
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1458
#!mode=file
B0=$PWD
trap 'echo T' EXIT 2>&1; trap; echo rc=$?
g() { local PATH; PATH='.'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1459
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
exec 6<&-; read v <&6 2>&1; echo rc=$?
%%%
#!name gen1460
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
set -- -a -b; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1461
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1462
#!mode=file
B0=$PWD
echo "[${LINENO}]"
trap 'echo X' TERM 2>&1; (trap); echo ---; trap
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1463
#!mode=file
B0=$PWD
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1464
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
trap USR1 2>&1; trap; echo rc=$?
%%%
#!name gen1465
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen1466
#!mode=file
B0=$PWD
set +o monitor; echo "$-"
%%%
#!name gen1467
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen1468
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1469
#!mode=file
B0=$PWD
export LC_ALL
g() { local B='abc'; B='abc'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
set -- -c; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1470
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
echo "[${OPTIND-U}]"
%%%
#!name gen1471
#!mode=file
B0=$PWD
set +I; echo "$-"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1472
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
set -; echo "$-"
%%%
#!name gen1473
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
readonly vv='x=y'
%%%
#!name gen1474
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen1475
#!mode=file
B0=$PWD
echo "[${A-U}]"
%%%
#!name gen1476
#!mode=file
B0=$PWD
g() { local vv=':'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1477
#!mode=file
B0=$PWD
readonly LINENO='0'
%%%
#!name gen1478
#!mode=file
B0=$PWD
g() { local -; set +u; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1479
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen1480
#!mode=file
B0=$PWD
set -o pipefail; echo "$-"
set -- -c -a -b -z; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo T' PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen1481
#!mode=file
B0=$PWD
set +o | md5sum
g() { local IFS='x=y'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1482
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
alias a='b'; alias b='echo B'; a
%%%
#!name gen1483
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1484
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
export -p | grep -c 'PPID' 
%%%
#!name gen1485
#!mode=file
B0=$PWD
set -- -ab -z --; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b'; alias b='echo B'; a
trap 12 2>&1; trap; echo rc=$?
%%%
#!name gen1486
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen1487
#!mode=file
B0=$PWD
env | grep -c '^PS2='
cat <<'E'
$notexpanded
E
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1488
#!mode=file
B0=$PWD
set -; echo "$-"
set -- -bvalue -a -b; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1489
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1490
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1491
#!mode=file
B0=$PWD
set -- -z -a -ab; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo T' USR2 2>&1; trap; echo rc=$?
set -o | md5sum
%%%
#!name gen1492
#!mode=file
B0=$PWD
set -- a b c d e; shift 0 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1493
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
echo "[${CDPATH}]"
%%%
#!name gen1494
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='x=y'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1495
#!mode=file
B0=$PWD
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set | grep -c '^V1='
%%%
#!name gen1496
#!mode=file
B0=$PWD
(exec 3>s.txt; echo s >&3); cat s.txt; echo t >&3 2>&1; echo rc=$?
export -p | grep -c 'LINENO' 
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen1497
#!mode=file
B0=$PWD
g() { local OPTIND='"q"'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1498
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
readonly V1='0'
trap 'echo A' USR2 2>&1; trap - USR2 2>&1; trap; echo rc=$?
%%%
#!name gen1499
#!mode=file
B0=$PWD
set -o | md5sum
g() { local B='POSIX'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
