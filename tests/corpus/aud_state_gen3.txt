#!name gen0
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
trap 'echo T' USR2 2>&1; trap; echo rc=$?
set -- -c -b -c; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen2
#!mode=file
B0=$PWD
readonly PWD='"q"'
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen3
#!mode=file
B0=$PWD
set --  c a; echo "$#:$*"
%%%
#!name gen4
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen5
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen6
#!mode=file
B0=$PWD
set -x; echo "$-"
%%%
#!name gen7
#!mode=file
B0=$PWD
export PWD
IFS=':'
%%%
#!name gen8
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set +o
set -- -c -ab; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen9
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
%%%
#!name gen10
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen11
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set | grep -c '^OPTIND='
%%%
#!name gen12
#!mode=file
B0=$PWD
set -- -z; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen13
#!mode=file
B0=$PWD
set -- -c; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local PATH; PATH='0'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen14
#!mode=file
B0=$PWD
set +o noexec; echo "$-"
cd /nope 2>&1; echo rc=$?
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen15
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
%%%
#!name gen16
#!mode=file
B0=$PWD
set -- -ab -c; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen17
#!mode=file
B0=$PWD
g() { local OPTIND='0'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen18
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
g() { local -; set -o emacs; echo "in:$-"; }; g; echo "out:$-"
set +o emacs; echo "$-"
%%%
#!name gen19
#!mode=file
B0=$PWD
g() { local A B; A='a'b'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen20
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
%%%
#!name gen21
#!mode=file
B0=$PWD
set +o
%%%
#!name gen22
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
read v <<E
value
E
echo "[$v]"
%%%
#!name gen23
#!mode=file
B0=$PWD
set -o
%%%
#!name gen24
#!mode=file
B0=$PWD
g() { local A='0'; A='0'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen25
#!mode=file
B0=$PWD
HOME='0'
g() { local OPTIND; OPTIND='\t'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
env | grep -c '^PWD='
%%%
#!name gen26
#!mode=file
B0=$PWD
set -- -bvalue --; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export HOME='a'b'
%%%
#!name gen27
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen28
#!mode=file
B0=$PWD
trap USR1 2>&1; trap; echo rc=$?
set -o
%%%
#!name gen29
#!mode=file
B0=$PWD
set +o allexport; echo "$-"
set +o verbose; echo "$-"
%%%
#!name gen30
#!mode=file
B0=$PWD
trap 'echo X' INT 2>&1; (trap); echo ---; trap
g() { local A='\t'; A='\t'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen31
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
HOME='abc'
%%%
#!name gen32
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen33
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen34
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen35
#!mode=file
B0=$PWD
set -- -a -b -a -a x; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias echo='echo pre'; echo hi
%%%
#!name gen36
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
trap 'echo T' CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen37
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
cat <<-E
	indented
	E
%%%
#!name gen38
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
cat <<E
line1
line2
E
%%%
#!name gen39
#!mode=file
B0=$PWD
set -- -- -bvalue; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen40
#!mode=file
B0=$PWD
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
%%%
#!name gen41
#!mode=file
B0=$PWD
set -fn; echo "$-"
unset -v vv
set -- -a x x; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen42
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen43
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen44
#!mode=file
B0=$PWD
trap 'echo T' INT 2>&1; trap; echo rc=$?
trap 'echo A' INT 2>&1; trap - INT 2>&1; trap; echo rc=$?
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen45
#!mode=file
B0=$PWD
set -mI; echo "$-"
set -- -a -b -a x -c; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -o monitor; echo "$-"
%%%
#!name gen46
#!mode=file
B0=$PWD
TERM='abc'
%%%
#!name gen47
#!mode=file
B0=$PWD
readonly HISTSIZE='.'
trap USR2 2>&1; trap; echo rc=$?
%%%
#!name gen48
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set -- -a -b -z -b; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen49
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
echo "[${A-U}]"
alias a='echo A'; a; alias a
%%%
#!name gen50
#!mode=file
B0=$PWD
set +i; echo "$-"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen51
#!mode=file
B0=$PWD
set -x; echo "$-"
%%%
#!name gen52
#!mode=file
B0=$PWD
set -- --; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen53
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
cat <<-E
	indented
	E
trap 'echo X' PIPE 2>&1; (trap); echo ---; trap
%%%
#!name gen54
#!mode=file
B0=$PWD
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen55
#!mode=file
B0=$PWD
set | grep -c '^A='
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen56
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
set -o xtrace; echo "$-"
%%%
#!name gen57
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
alias a='echo A'; a; alias a
exec 5<&-; read v <&5 2>&1; echo rc=$?
%%%
#!name gen58
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
trap 'echo X' TERM 2>&1; (trap); echo ---; trap
%%%
#!name gen59
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
g() { local A B; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen60
#!mode=file
B0=$PWD
env | grep -c '^TERM='
%%%
#!name gen61
#!mode=file
B0=$PWD
export IFS
g() { local A; A='/tmp'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local PATH; PATH=''; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen62
#!mode=file
B0=$PWD
unset -v PATH
echo "[${LANG-U}]"
%%%
#!name gen63
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen64
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen65
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen66
#!mode=file
B0=$PWD
set +o
%%%
#!name gen67
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen68
#!mode=file
B0=$PWD
set -- x; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen69
#!mode=file
B0=$PWD
CDPATH='"q"'
%%%
#!name gen70
#!mode=file
B0=$PWD
trap 'echo A' PIPE 2>&1; trap - PIPE 2>&1; trap; echo rc=$?
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
unset -v OLDPWD
%%%
#!name gen71
#!mode=file
B0=$PWD
export HISTSIZE
%%%
#!name gen72
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
echo "[${MAIL}]"
%%%
#!name gen73
#!mode=file
B0=$PWD
set -- -a; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
g() { local OPTIND='a b'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen74
#!mode=file
B0=$PWD
trap INT 2>&1; trap; echo rc=$?
%%%
#!name gen75
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen76
#!mode=file
B0=$PWD
g() { local PATH B; PATH=''; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
echo "[${PS1}]"
%%%
#!name gen77
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
set --; echo "$#"
alias a='echo A'; a; alias a
%%%
#!name gen78
#!mode=file
B0=$PWD
trap '' EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen79
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
set -; echo "$-"
%%%
#!name gen80
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap '' QUIT 2>&1; trap; echo rc=$?
%%%
#!name gen81
#!mode=file
B0=$PWD
trap USR2 2>&1; trap; echo rc=$?
g() { local vv='0'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
%%%
#!name gen82
#!mode=file
B0=$PWD
set -- -b -a -b; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo T' 0 2>&1; trap; echo rc=$?
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen83
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen84
#!mode=file
B0=$PWD
LINENO='POSIX'
%%%
#!name gen85
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -- x -z; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen86
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
g() { local vv B; vv=''; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen87
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen88
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen89
#!mode=file
B0=$PWD
set -- -z -b -b; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local OPTIND='"q"'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen90
#!mode=file
B0=$PWD
readonly PPID=''
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
%%%
#!name gen91
#!mode=file
B0=$PWD
set -- -a -b -a -a -b; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -; echo "$-"
set -- -a; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen92
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen93
#!mode=file
B0=$PWD
export -p | grep -c 'LC_COLLATE' 
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen94
#!mode=file
B0=$PWD
set -- -a -b -c; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen95
#!mode=file
B0=$PWD
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
echo "[${MAILPATH-U}]"
%%%
#!name gen96
#!mode=file
B0=$PWD
trap '' PIPE 2>&1; trap; echo rc=$?
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen97
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
(trap 'echo SE' EXIT; echo sub); echo main
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen98
#!mode=file
B0=$PWD
set +o
env | grep -c '^HOME='
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen99
#!mode=file
B0=$PWD
export PPID
g() { local OPTIND B; OPTIND='.'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen100
#!mode=file
B0=$PWD
trap '' TERM 2>&1; trap; echo rc=$?
trap 'echo A' TERM 2>&1; trap - TERM 2>&1; trap; echo rc=$?
trap 'echo A' USR2 2>&1; trap - USR2 2>&1; trap; echo rc=$?
%%%
#!name gen101
#!mode=file
B0=$PWD
readonly PPID='0'
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen102
#!mode=file
B0=$PWD
unset -v B
%%%
#!name gen103
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen104
#!mode=file
B0=$PWD
trap 'echo X' INT 2>&1; (trap); echo ---; trap
alias a='b'; alias b='echo B'; a
%%%
#!name gen105
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
g() { local OPTIND; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen106
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen107
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen108
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- x; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen109
#!mode=file
B0=$PWD
g() { local -; set -n; echo "in:$-"; }; g; echo "out:$-"
readonly PS2='"q"'
%%%
#!name gen110
#!mode=file
B0=$PWD
trap '' HUP 2>&1; trap; echo rc=$?
echo "[${LC_NUMERIC-U}]"
%%%
#!name gen111
#!mode=file
B0=$PWD
export PS2
%%%
#!name gen112
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen113
#!mode=file
B0=$PWD
trap 'echo T' CHLD 2>&1; trap; echo rc=$?
set +v; echo "$-"
%%%
#!name gen114
#!mode=file
B0=$PWD
set -- -b -a -a -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -bvalue -b -z; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen115
#!mode=file
B0=$PWD
TERM=':'
%%%
#!name gen116
#!mode=file
B0=$PWD
export HISTSIZE
set -- -ab -z --; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
exec 4<<E
fromfd
E
read v <&4; echo "[$v]"; exec 4<&-
%%%
#!name gen117
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen118
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen119
#!mode=file
B0=$PWD
env | grep -c '^OLDPWD='
set -u; echo "$-"
%%%
#!name gen120
#!mode=file
B0=$PWD
set -- -c; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen121
#!mode=file
B0=$PWD
set | grep -c '^OLDPWD='
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
%%%
#!name gen122
#!mode=file
B0=$PWD
trap '' ALRM 2>&1; trap; echo rc=$?
%%%
#!name gen123
#!mode=file
B0=$PWD
export LC_CTYPE='POSIX'
%%%
#!name gen124
#!mode=file
B0=$PWD
set +v; echo "$-"
set +o
%%%
#!name gen125
#!mode=file
B0=$PWD
g() { local OPTIND='/tmp'; OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -o noexec; echo "$-"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen126
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
env | grep -c '^HISTSIZE='
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen127
#!mode=file
B0=$PWD
set -- -a -b -c -c; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen128
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
trap 'echo X' 10 2>&1; (trap); echo ---; trap
export -p | grep -c 'OPTIND' 
%%%
#!name gen129
#!mode=file
B0=$PWD
trap 'echo T' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen130
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen131
#!mode=file
B0=$PWD
env | grep -c '^LC_CTYPE='
set -- -a x -a; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen132
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
export OLDPWD
g() { local IFS; IFS='/tmp'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen133
#!mode=file
B0=$PWD
set -- -a; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
trap 'echo T' 10 2>&1; trap; echo rc=$?
%%%
#!name gen134
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen135
#!mode=file
B0=$PWD
trap '' 12 2>&1; trap; echo rc=$?
export MAILPATH
%%%
#!name gen136
#!mode=file
B0=$PWD
unset LC_ALL
%%%
#!name gen137
#!mode=file
B0=$PWD
trap 'echo A' USR1 2>&1; trap - USR1 2>&1; trap; echo rc=$?
%%%
#!name gen138
#!mode=file
B0=$PWD
echo "[${MAIL-U}]"
echo "[${MAILPATH-U}]"
export -p | grep -c 'HISTSIZE' 
%%%
#!name gen139
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
set -v; echo "$-"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen140
#!mode=file
B0=$PWD
trap 'echo X' TERM 2>&1; (trap); echo ---; trap
set -- -z -a -b; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
PS1='a b'
%%%
#!name gen141
#!mode=file
B0=$PWD
set -- c ; echo "$#:$*"
%%%
#!name gen142
#!mode=file
B0=$PWD
set -- -a -b -z; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set --; echo "$#"
g() { local IFS; IFS='x=y'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen143
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen144
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -ba; echo "$-"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen145
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias echo='echo pre'; echo hi
%%%
#!name gen146
#!mode=file
B0=$PWD
set +o
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen147
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen148
#!mode=file
B0=$PWD
set --  a  b; echo "$#:$*"
set | grep -c '^PPID='
%%%
#!name gen149
#!mode=file
B0=$PWD
set -mm; echo "$-"
readonly MAILPATH='a'b'
%%%
#!name gen150
#!mode=file
B0=$PWD
trap '' TERM 2>&1; trap; echo rc=$?
g() { local B='1'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen151
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
set -- -c; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<'E'
$notexpanded
E
%%%
#!name gen152
#!mode=file
B0=$PWD
set -ei; echo "$-"
set -- -a x -c; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -fI; echo "$-"
%%%
#!name gen153
#!mode=file
B0=$PWD
PS4='.'
%%%
#!name gen154
#!mode=file
B0=$PWD
set -- -c -a x; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -z -a -b; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen155
#!mode=file
B0=$PWD
set -o allexport; echo "$-"
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen156
#!mode=file
B0=$PWD
set -- -z -c x; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -b -ab; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen157
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen158
#!mode=file
B0=$PWD
set +o
%%%
#!name gen159
#!mode=file
B0=$PWD
export PATH
%%%
#!name gen160
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen161
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen162
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
read v <<E
value
E
echo "[$v]"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen163
#!mode=file
B0=$PWD
trap TERM 2>&1; trap; echo rc=$?
alias a='echo A'; a; alias a
g() { local OPTIND='POSIX'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen164
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
%%%
#!name gen165
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
readonly LC_COLLATE='abc'
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
%%%
#!name gen166
#!mode=file
B0=$PWD
export -p | grep -c 'A' 
%%%
#!name gen167
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen168
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen169
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
readonly vv='abc'
%%%
#!name gen170
#!mode=file
B0=$PWD
set -- -bvalue x -z; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen171
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
unset PS1
export -p | grep -c 'PS2' 
%%%
#!name gen172
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
set -V; echo "$-"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen173
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -- x -a x x; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen174
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen175
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
%%%
#!name gen176
#!mode=file
B0=$PWD
set -- -c -bvalue; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set --; echo "$#"
%%%
#!name gen177
#!mode=file
B0=$PWD
export -p | grep -c 'CDPATH' 
%%%
#!name gen178
#!mode=file
B0=$PWD
set -- -a x -c; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
alias a='echo A'; a; alias a
%%%
#!name gen179
#!mode=file
B0=$PWD
trap 'echo X' USR2 2>&1; (trap); echo ---; trap
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
export PWD
%%%
#!name gen180
#!mode=file
B0=$PWD
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
(exec 3>s.txt; echo s >&3); cat s.txt; echo t >&3 2>&1; echo rc=$?
%%%
#!name gen181
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen182
#!mode=file
B0=$PWD
set -- -a -b -bvalue -a; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen183
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen184
#!mode=file
B0=$PWD
set -o
set +V; echo "$-"
%%%
#!name gen185
#!mode=file
B0=$PWD
set --  b; echo "$#:$*"
%%%
#!name gen186
#!mode=file
B0=$PWD
env | grep -c '^PATH='
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen187
#!mode=file
B0=$PWD
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
%%%
#!name gen188
#!mode=file
B0=$PWD
trap 'echo X' ALRM 2>&1; (trap); echo ---; trap
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen189
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='0'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen190
#!mode=file
B0=$PWD
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen191
#!mode=file
B0=$PWD
g() { local A; A='a'b'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
readonly LANG='"q"'
%%%
#!name gen192
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
set -o
alias a='echo A'; a; alias a
%%%
#!name gen193
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen194
#!mode=file
B0=$PWD
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
alias echo='echo pre'; echo hi
%%%
#!name gen195
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
alias 'a b'=1 2>&1; echo rc=$?
export OLDPWD=''
%%%
#!name gen196
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen197
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen198
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
%%%
#!name gen199
#!mode=file
B0=$PWD
set -- x -z; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen200
#!mode=file
B0=$PWD
set -- --; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap '' 10 2>&1; trap; echo rc=$?
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen201
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
trap '' 0 2>&1; trap; echo rc=$?
%%%
#!name gen202
#!mode=file
B0=$PWD
unset LC_CTYPE
echo "[${PS1}]"
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen203
#!mode=file
B0=$PWD
g() { local vv; vv='x=y'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen204
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen205
#!mode=file
B0=$PWD
set +o pipefail; echo "$-"
%%%
#!name gen206
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen207
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen208
#!mode=file
B0=$PWD
exec 5<&-; read v <&5 2>&1; echo rc=$?
trap 'echo X' CHLD 2>&1; (trap); echo ---; trap
%%%
#!name gen209
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen210
#!mode=file
B0=$PWD
trap '' 10 2>&1; trap; echo rc=$?
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen211
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen212
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
g() { local OPTIND; OPTIND='.'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set | grep -c '^PS1='
%%%
#!name gen213
#!mode=file
B0=$PWD
LC_NUMERIC='abc'
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen214
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen215
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen216
#!mode=file
B0=$PWD
g() { local IFS B; IFS=''; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -- -a -b -a; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen217
#!mode=file
B0=$PWD
set -- -a -b -a -b -a x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset MAIL
set -- -bvalue; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen218
#!mode=file
B0=$PWD
set +o noglob; echo "$-"
set -bI; echo "$-"
set -Ia; echo "$-"
%%%
#!name gen219
#!mode=file
B0=$PWD
echo "[${vv}]"
%%%
#!name gen220
#!mode=file
B0=$PWD
export TERM
set -- -a -z x; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen221
#!mode=file
B0=$PWD
trap 'echo A' TERM 2>&1; trap - TERM 2>&1; trap; echo rc=$?
alias a='echo A'; a; alias a
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen222
#!mode=file
B0=$PWD
set -- -b; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen223
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen224
#!mode=file
B0=$PWD
trap '' CHLD 2>&1; trap; echo rc=$?
export LC_ALL='x=y'
set | grep -c '^PS1='
%%%
#!name gen225
#!mode=file
B0=$PWD
unset LINENO
trap '' TERM 2>&1; trap; echo rc=$?
%%%
#!name gen226
#!mode=file
B0=$PWD
g() { local OPTIND='a b'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
trap '' 1 2>&1; trap; echo rc=$?
%%%
#!name gen227
#!mode=file
B0=$PWD
trap '' PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen228
#!mode=file
B0=$PWD
export -p | grep -c 'OLDPWD' 
%%%
#!name gen229
#!mode=file
B0=$PWD
trap '' INT 2>&1; trap; echo rc=$?
alias a='echo A'; a; alias a
%%%
#!name gen230
#!mode=file
B0=$PWD
trap 'echo A' CHLD 2>&1; trap - CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen231
#!mode=file
B0=$PWD
g() { local A='a b'; A='a b'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
exec 5>&1; echo x >&5; exec 5>&-; echo y >&5 2>&1; echo rc=$?
set -- -b -a x; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen232
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen233
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen234
#!mode=file
B0=$PWD
set -- -- -bvalue --; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
g() { local B=''; B=''; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen235
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen236
#!mode=file
B0=$PWD
set -fn; echo "$-"
g() { local B; B='/tmp'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen237
#!mode=file
B0=$PWD
g() { local A='C'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
%%%
#!name gen238
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
set -- -- -ab -ab; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x -ab; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen239
#!mode=file
B0=$PWD
trap '' TERM 2>&1; trap; echo rc=$?
trap '' 1 2>&1; trap; echo rc=$?
readonly OPTIND=':'
%%%
#!name gen240
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set +o
%%%
#!name gen241
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
trap EXIT 2>&1; trap; echo rc=$?
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen242
#!mode=file
B0=$PWD
readonly LINENO='POSIX'
%%%
#!name gen243
#!mode=file
B0=$PWD
g() { local V1 B; V1=':'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -iC; echo "$-"
read v <<E
value
E
echo "[$v]"
%%%
#!name gen244
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local A B; A='C'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen245
#!mode=file
B0=$PWD
set +v; echo "$-"
set | grep -c '^B='
%%%
#!name gen246
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen247
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
alias a='echo A'; a; alias a
%%%
#!name gen248
#!mode=file
B0=$PWD
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
%%%
#!name gen249
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen250
#!mode=file
B0=$PWD
unset PATH
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen251
#!mode=file
B0=$PWD
trap QUIT 2>&1; trap; echo rc=$?
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
%%%
#!name gen252
#!mode=file
B0=$PWD
set -- -- -a -b; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -; echo "$-"
unset -v vv
%%%
#!name gen253
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen254
#!mode=file
B0=$PWD
set -- -c; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen255
#!mode=file
B0=$PWD
set | grep -c '^CDPATH='
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -- -a -b -bvalue x; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen256
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen257
#!mode=file
B0=$PWD
set +o verbose; echo "$-"
set -; echo "$-"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen258
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen259
#!mode=file
B0=$PWD
export PS1
%%%
#!name gen260
#!mode=file
B0=$PWD
set -- -c; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- a b c d e; shift 0 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen261
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a x; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen262
#!mode=file
B0=$PWD
MAIL='/tmp'
unset -v V1
unset -v LC_NUMERIC
%%%
#!name gen263
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
unset LC_ALL
%%%
#!name gen264
#!mode=file
B0=$PWD
set -- -c -b -a -b; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local PATH='C'; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen265
#!mode=file
B0=$PWD
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
%%%
#!name gen266
#!mode=file
B0=$PWD
set | grep -c '^PS4='
trap '' 0 2>&1; trap; echo rc=$?
trap 'echo E' EXIT; echo body
%%%
#!name gen267
#!mode=file
B0=$PWD
trap 'echo X' PIPE 2>&1; (trap); echo ---; trap
cat <<-E
	indented
	E
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen268
#!mode=file
B0=$PWD
set -; echo "$-"
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen269
#!mode=file
B0=$PWD
set -- -a x -bvalue; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen270
#!mode=file
B0=$PWD
trap 'echo A' 1 2>&1; trap - 1 2>&1; trap; echo rc=$?
%%%
#!name gen271
#!mode=file
B0=$PWD
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
f() { echo F; }; f >f.txt 2>&1; cat f.txt
trap 'echo E' EXIT; echo body
%%%
#!name gen272
#!mode=file
B0=$PWD
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
%%%
#!name gen273
#!mode=file
B0=$PWD
g() { local PATH='a'b'; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen274
#!mode=file
B0=$PWD
export MAILPATH='\t'
%%%
#!name gen275
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
trap 'echo A' PIPE 2>&1; trap - PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen276
#!mode=file
B0=$PWD
export -p | grep -c 'vv' 
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- -c; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen277
#!mode=file
B0=$PWD
g() { local vv; vv='/tmp'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen278
#!mode=file
B0=$PWD
g() { local B B; B='"q"'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen279
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen280
#!mode=file
B0=$PWD
trap '' USR2 2>&1; trap; echo rc=$?
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen281
#!mode=file
B0=$PWD
set -- x x; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen282
#!mode=file
B0=$PWD
set -- -a -b -z; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen283
#!mode=file
B0=$PWD
set --; echo "$#"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen284
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
PS2='C'
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen285
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
{ echo p; } 4>&- ; echo rc=$?
%%%
#!name gen286
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen287
#!mode=file
B0=$PWD
set -- -- x -ab; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen288
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen289
#!mode=file
B0=$PWD
set -- -a x -c --; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen290
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen291
#!mode=file
B0=$PWD
set -- -a x; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen292
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
g() { local A B; A='a b'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -f; echo "$-"
%%%
#!name gen293
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
set -- -- -c -b; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen294
#!mode=file
B0=$PWD
set -o nolog; echo "$-"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set +o pipefail; echo "$-"
%%%
#!name gen295
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen296
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen297
#!mode=file
B0=$PWD
trap 'echo T' QUIT 2>&1; trap; echo rc=$?
set -- -a -a -b -bvalue; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen298
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen299
#!mode=file
B0=$PWD
set -- -z -c; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo X' TERM 2>&1; (trap); echo ---; trap
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen300
#!mode=file
B0=$PWD
trap '' QUIT 2>&1; trap; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
g() { local OPTIND; OPTIND=''; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen301
#!mode=file
B0=$PWD
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
%%%
#!name gen302
#!mode=file
B0=$PWD
g() { local vv='a'b'; vv='a'b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
g() { local -; set -o notify; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen303
#!mode=file
B0=$PWD
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen304
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
g() { local V1='\t'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
trap CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen305
#!mode=file
B0=$PWD
exec 6<<E
fromfd
E
read v <&6; echo "[$v]"; exec 6<&-
%%%
#!name gen306
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
export CDPATH
%%%
#!name gen307
#!mode=file
B0=$PWD
g() { local B='0'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen308
#!mode=file
B0=$PWD
g() { local A='"q"'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
echo "[${LANG-U}]"
%%%
#!name gen309
#!mode=file
B0=$PWD
echo "[${PATH}]"
alias echo='echo pre'; echo hi
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen310
#!mode=file
B0=$PWD
env | grep -c '^LC_CTYPE='
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
alias a='echo A'; a; alias a
%%%
#!name gen311
#!mode=file
B0=$PWD
set | grep -c '^LC_CTYPE='
unset PATH
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen312
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
alias a='b'; alias b='echo B'; a
echo "[${LC_NUMERIC-U}]"
%%%
#!name gen313
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen314
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen315
#!mode=file
B0=$PWD
set -- -z -ab; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -I; echo "$-"
%%%
#!name gen316
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen317
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
trap 'echo E' EXIT; echo body
%%%
#!name gen318
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen319
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
alias a='echo A'; a; alias a
%%%
#!name gen320
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- -a -b --; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen321
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen322
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set -- -z -c -bvalue; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen323
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen324
#!mode=file
B0=$PWD
set -- -a -ab; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap TERM 2>&1; trap; echo rc=$?
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen325
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- -a; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen326
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen327
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen328
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
trap 'echo X' 12 2>&1; (trap); echo ---; trap
export B
%%%
#!name gen329
#!mode=file
B0=$PWD
set -- -a -bvalue; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- --; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen330
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen331
#!mode=file
B0=$PWD
set -ua; echo "$-"
alias echo='echo pre'; echo hi
trap EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen332
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
%%%
#!name gen333
#!mode=file
B0=$PWD
trap 'echo A' TERM 2>&1; trap - TERM 2>&1; trap; echo rc=$?
export HISTSIZE='a'b'
%%%
#!name gen334
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
g() { local -; set -o ignoreeof; echo "in:$-"; }; g; echo "out:$-"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen335
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
exec 6<&-; read v <&6 2>&1; echo rc=$?
%%%
#!name gen336
#!mode=file
B0=$PWD
g() { local PATH=''; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
trap 'echo X' PIPE 2>&1; (trap); echo ---; trap
%%%
#!name gen337
#!mode=file
B0=$PWD
set -- -z; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen338
#!mode=file
B0=$PWD
trap 'echo A' USR1 2>&1; trap - USR1 2>&1; trap; echo rc=$?
set -o
%%%
#!name gen339
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
set -- -c -c --; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen340
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
trap 'echo E' EXIT; echo body
export -p | grep -c 'PS4' 
%%%
#!name gen341
#!mode=file
B0=$PWD
set -o allexport; echo "$-"
g() { local B='abc'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen342
#!mode=file
B0=$PWD
trap 'echo T' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen343
#!mode=file
B0=$PWD
unset -v PS1
%%%
#!name gen344
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -- -z; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen345
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -EV; echo "$-"
%%%
#!name gen346
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen347
#!mode=file
B0=$PWD
echo "[${OLDPWD#$B0}]"
%%%
#!name gen348
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
set -- -a -b; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen349
#!mode=file
B0=$PWD
g() { local PATH; PATH='C'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen350
#!mode=file
B0=$PWD
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
g() { local A; A=''; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen351
#!mode=file
B0=$PWD
g() { local -; set -m; echo "in:$-"; }; g; echo "out:$-"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen352
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
g() { local B='"q"'; B='"q"'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
set +x; echo "$-"
%%%
#!name gen353
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
alias echo='echo pre'; echo hi
alias echo='echo pre'; echo hi
%%%
#!name gen354
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
set --; echo "$#"
%%%
#!name gen355
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen356
#!mode=file
B0=$PWD
trap '' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen357
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
set -vx; echo "$-"
%%%
#!name gen358
#!mode=file
B0=$PWD
export -p | grep -c 'LC_ALL' 
%%%
#!name gen359
#!mode=file
B0=$PWD
set -- -z x -c; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen360
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen361
#!mode=file
B0=$PWD
set -- -a x -c -bvalue; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset V1
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
%%%
#!name gen362
#!mode=file
B0=$PWD
trap 'echo T' QUIT 2>&1; trap; echo rc=$?
exec 9<&-; read v <&9 2>&1; echo rc=$?
echo "[${IFS-U}]"
%%%
#!name gen363
#!mode=file
B0=$PWD
set -- -a x -bvalue -b; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen364
#!mode=file
B0=$PWD
env | grep -c '^PATH='
g() { local V1; V1='"q"'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -- --; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen365
#!mode=file
B0=$PWD
set | grep -c '^OPTIND='
alias echo='echo pre'; echo hi
%%%
#!name gen366
#!mode=file
B0=$PWD
g() { local -; set +E; echo "in:$-"; }; g; echo "out:$-"
set | grep -c '^OPTIND='
set +u; echo "$-"
%%%
#!name gen367
#!mode=file
B0=$PWD
echo "[${PWD#$B0}]"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen368
#!mode=file
B0=$PWD
set -- -a x -b -ab; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen369
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
{ echo p; } 9>&- ; echo rc=$?
set -b; echo "$-"
%%%
#!name gen370
#!mode=file
B0=$PWD
echo "[${LC_ALL}]"
%%%
#!name gen371
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen372
#!mode=file
B0=$PWD
trap '' USR2 2>&1; trap; echo rc=$?
trap 'echo E' EXIT; echo body
%%%
#!name gen373
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
set +o
%%%
#!name gen374
#!mode=file
B0=$PWD
set -o monitor; echo "$-"
%%%
#!name gen375
#!mode=file
B0=$PWD
trap 'echo X' USR2 2>&1; (trap); echo ---; trap
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local vv B; vv='C'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen376
#!mode=file
B0=$PWD
export -p | grep -c 'MAIL' 
set -- -bvalue -z --; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen377
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen378
#!mode=file
B0=$PWD
set -- -a -b; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
unset -v CDPATH
%%%
#!name gen379
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
export -p | grep -c 'PWD' 
alias a='echo A'; a; alias a
%%%
#!name gen380
#!mode=file
B0=$PWD
readonly TERM='abc'
alias a='b'; alias b='echo B'; a
set -- --; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen381
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen382
#!mode=file
B0=$PWD
set -- -ab -ab -z; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen383
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
export -p | grep -c 'OPTIND' 
trap 'echo E' EXIT; echo body
%%%
#!name gen384
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen385
#!mode=file
B0=$PWD
unset PPID
set -- x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen386
#!mode=file
B0=$PWD
trap '' 0 2>&1; trap; echo rc=$?
%%%
#!name gen387
#!mode=file
B0=$PWD
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
trap HUP 2>&1; trap; echo rc=$?
%%%
#!name gen388
#!mode=file
B0=$PWD
readonly MAIL='POSIX'
echo "[${PS2-U}]"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen389
#!mode=file
B0=$PWD
export PS2=':'
%%%
#!name gen390
#!mode=file
B0=$PWD
set -ua; echo "$-"
%%%
#!name gen391
#!mode=file
B0=$PWD
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
trap 'echo E' EXIT; echo body
%%%
#!name gen392
#!mode=file
B0=$PWD
set -- b b a; echo "$#:$*"
g() { local -; set -m; echo "in:$-"; }; g; echo "out:$-"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen393
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen394
#!mode=file
B0=$PWD
export LC_CTYPE='abc'
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
%%%
#!name gen395
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen396
#!mode=file
B0=$PWD
set +o
set -- -a; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen397
#!mode=file
B0=$PWD
set -E; echo "$-"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen398
#!mode=file
B0=$PWD
export CDPATH
%%%
#!name gen399
#!mode=file
B0=$PWD
set +o nolog; echo "$-"
%%%
#!name gen400
#!mode=file
B0=$PWD
set -- -b -a -b; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<-E
	indented
	E
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen401
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen402
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
unset PATH
%%%
#!name gen403
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
g() { local OPTIND='POSIX'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen404
#!mode=file
B0=$PWD
trap 'echo T' QUIT 2>&1; trap; echo rc=$?
exec 6<&-; read v <&6 2>&1; echo rc=$?
%%%
#!name gen405
#!mode=file
B0=$PWD
env | grep -c '^A='
set -e; echo "$-"
%%%
#!name gen406
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
set -; echo "$-"
trap '' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen407
#!mode=file
B0=$PWD
set -- -ab -a x --; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- --; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias echo='echo pre'; echo hi
%%%
#!name gen408
#!mode=file
B0=$PWD
set -- x x -a; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export HISTSIZE
%%%
#!name gen409
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen410
#!mode=file
B0=$PWD
set -- -bvalue -a -b -ab; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set +o nounset; echo "$-"
%%%
#!name gen411
#!mode=file
B0=$PWD
trap 'echo A' PIPE 2>&1; trap - PIPE 2>&1; trap; echo rc=$?
trap '' 1 2>&1; trap; echo rc=$?
alias a='echo A'; a; alias a
%%%
#!name gen412
#!mode=file
B0=$PWD
set -- -z -z -b; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias echo='echo pre'; echo hi
env | grep -c '^LC_CTYPE='
%%%
#!name gen413
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
trap 'echo T' ALRM 2>&1; trap; echo rc=$?
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen414
#!mode=file
B0=$PWD
PATH=':'
%%%
#!name gen415
#!mode=file
B0=$PWD
trap USR2 2>&1; trap; echo rc=$?
set -- -ab -b -a; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen416
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set +x; echo "$-"
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
%%%
#!name gen417
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen418
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
unset -v PS1
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen419
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen420
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
cd /nope 2>&1; echo rc=$?
%%%
#!name gen421
#!mode=file
B0=$PWD
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen422
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen423
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
g() { local A='abc'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen424
#!mode=file
B0=$PWD
set -- -b; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen425
#!mode=file
B0=$PWD
set +a; echo "$-"
%%%
#!name gen426
#!mode=file
B0=$PWD
set +o
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen427
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen428
#!mode=file
B0=$PWD
set -- -c --; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen429
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
%%%
#!name gen430
#!mode=file
B0=$PWD
set -Vm; echo "$-"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen431
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen432
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
trap '' USR1 2>&1; trap; echo rc=$?
MAILPATH='x=y'
%%%
#!name gen433
#!mode=file
B0=$PWD
set -- x -z -b; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen434
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen435
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
set -- -z -ab -c; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen436
#!mode=file
B0=$PWD
trap '' QUIT 2>&1; trap; echo rc=$?
set -; echo "$-"
%%%
#!name gen437
#!mode=file
B0=$PWD
trap 'echo T' CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen438
#!mode=file
B0=$PWD
{ echo p; } 9>&- ; echo rc=$?
cat <<E
line1
line2
E
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen439
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen440
#!mode=file
B0=$PWD
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
set +o nounset; echo "$-"
%%%
#!name gen441
#!mode=file
B0=$PWD
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
trap '' ALRM 2>&1; trap; echo rc=$?
g() { local vv='a'b'; vv='a'b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen442
#!mode=file
B0=$PWD
set -- -- -a -a x; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen443
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
cat <<-E
	indented
	E
%%%
#!name gen444
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
g() { local OPTIND; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen445
#!mode=file
B0=$PWD
set -- -a -a -c; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<E
line1
line2
E
%%%
#!name gen446
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set --; echo "$#"
%%%
#!name gen447
#!mode=file
B0=$PWD
set | grep -c '^MAIL='
env | grep -c '^LANG='
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen448
#!mode=file
B0=$PWD
echo "[${LC_ALL-U}]"
set -o noclobber; echo "$-"
alias echo='echo pre'; echo hi
%%%
#!name gen449
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen450
#!mode=file
B0=$PWD
set -- -bvalue x x; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export LC_NUMERIC='0'
env | grep -c '^A='
%%%
#!name gen451
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen452
#!mode=file
B0=$PWD
set -- -bvalue -b; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen453
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set -- -c --; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen454
#!mode=file
B0=$PWD
trap 'echo T' 12 2>&1; trap; echo rc=$?
set -o
%%%
#!name gen455
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
alias a='b'; alias b='echo B'; a
%%%
#!name gen456
#!mode=file
B0=$PWD
trap '' 12 2>&1; trap; echo rc=$?
%%%
#!name gen457
#!mode=file
B0=$PWD
set +m; echo "$-"
%%%
#!name gen458
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
export -p | grep -c 'LC_COLLATE' 
set --; echo "$#"
%%%
#!name gen459
#!mode=file
B0=$PWD
env | grep -c '^PS4='
trap 'echo X' CHLD 2>&1; (trap); echo ---; trap
%%%
#!name gen460
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
echo "[${TERM}]"
%%%
#!name gen461
#!mode=file
B0=$PWD
set -- -b -a; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen462
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
exec 4<<E
fromfd
E
read v <&4; echo "[$v]"; exec 4<&-
%%%
#!name gen463
#!mode=file
B0=$PWD
g() { local A='x=y'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local V1 B; V1='POSIX'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen464
#!mode=file
B0=$PWD
g() { local IFS='a b'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
trap '' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen465
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen466
#!mode=file
B0=$PWD
set -II; echo "$-"
%%%
#!name gen467
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set +f; echo "$-"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen468
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen469
#!mode=file
B0=$PWD
g() { local A='"q"'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen470
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
alias 'a b'=1 2>&1; echo rc=$?
set -- -z --; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen471
#!mode=file
B0=$PWD
set | grep -c '^PPID='
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
set -- -a -z; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen472
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
trap '' EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen473
#!mode=file
B0=$PWD
set -- -a -b -bvalue; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -z -ab x; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen474
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen475
#!mode=file
B0=$PWD
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
alias echo='echo pre'; echo hi
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen476
#!mode=file
B0=$PWD
{ echo p; } 4>&- ; echo rc=$?
trap 'echo X' USR2 2>&1; (trap); echo ---; trap
%%%
#!name gen477
#!mode=file
B0=$PWD
trap INT 2>&1; trap; echo rc=$?
echo "[${MAILPATH}]"
%%%
#!name gen478
#!mode=file
B0=$PWD
set +o nounset; echo "$-"
export OPTIND='\t'
%%%
#!name gen479
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
alias echo='echo pre'; echo hi
set -- -a -b x -a; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen480
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen481
#!mode=file
B0=$PWD
{ echo p; } 6>&- ; echo rc=$?
read v <<E
value
E
echo "[$v]"
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
%%%
#!name gen482
#!mode=file
B0=$PWD
g() { local -; set -V; echo "in:$-"; }; g; echo "out:$-"
set -- -a -a --; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen483
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
trap 'echo X' TERM 2>&1; (trap); echo ---; trap
%%%
#!name gen484
#!mode=file
B0=$PWD
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
set -Ex; echo "$-"
g() { local V1 B; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen485
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
trap 'echo E' EXIT; echo body
%%%
#!name gen486
#!mode=file
B0=$PWD
trap 'echo X' 12 2>&1; (trap); echo ---; trap
MAIL='0'
readonly HISTSIZE='POSIX'
%%%
#!name gen487
#!mode=file
B0=$PWD
set | grep -c '^LINENO='
set -o
%%%
#!name gen488
#!mode=file
B0=$PWD
set -- -bvalue; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen489
#!mode=file
B0=$PWD
set -- -- -- -z; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -ab; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen490
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
set +o emacs; echo "$-"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen491
#!mode=file
B0=$PWD
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen492
#!mode=file
B0=$PWD
set -- -ab x; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- 1 a 1; echo "$#:$*"
g() { local B='a'b'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen493
#!mode=file
B0=$PWD
trap TERM 2>&1; trap; echo rc=$?
%%%
#!name gen494
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
trap '' TERM 2>&1; trap; echo rc=$?
exec 5>&1; echo x >&5; exec 5>&-; echo y >&5 2>&1; echo rc=$?
%%%
#!name gen495
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
OLDPWD=''
%%%
#!name gen496
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set -I; echo "$-"
%%%
#!name gen497
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen498
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
set +o xtrace; echo "$-"
env | grep -c '^CDPATH='
%%%
#!name gen499
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen500
#!mode=file
B0=$PWD
trap 'echo A' 1 2>&1; trap - 1 2>&1; trap; echo rc=$?
set -- -b -a x -bvalue; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen501
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
read v <<E
value
E
echo "[$v]"
%%%
#!name gen502
#!mode=file
B0=$PWD
set --; echo "$#"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen503
#!mode=file
B0=$PWD
echo "[${OLDPWD#$B0}]"
%%%
#!name gen504
#!mode=file
B0=$PWD
set -- -b -b -a -b; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
%%%
#!name gen505
#!mode=file
B0=$PWD
export PS1='POSIX'
alias a='echo A'; a; alias a
readonly A='abc'
%%%
#!name gen506
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen507
#!mode=file
B0=$PWD
set -- -a -a x; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen508
#!mode=file
B0=$PWD
export -p | grep -c 'PATH' 
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
g() { local A; A='1'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen509
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
trap '' 0 2>&1; trap; echo rc=$?
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen510
#!mode=file
B0=$PWD
set -C; echo "$-"
set +o
%%%
#!name gen511
#!mode=file
B0=$PWD
exec 3>&1; echo x >&3; exec 3>&-; echo y >&3 2>&1; echo rc=$?
%%%
#!name gen512
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen513
#!mode=file
B0=$PWD
set -o noclobber; echo "$-"
set --; echo "$#"
unset -v PS2
%%%
#!name gen514
#!mode=file
B0=$PWD
set +o
trap 'echo E' EXIT; echo body
%%%
#!name gen515
#!mode=file
B0=$PWD
set -o pipefail; echo "$-"
set | grep -c '^PS4='
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen516
#!mode=file
B0=$PWD
trap 'echo A' CHLD 2>&1; trap - CHLD 2>&1; trap; echo rc=$?
set --; echo "$#"
trap '' 10 2>&1; trap; echo rc=$?
%%%
#!name gen517
#!mode=file
B0=$PWD
g() { local OPTIND=':'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
echo "[${CDPATH-U}]"
%%%
#!name gen518
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen519
#!mode=file
B0=$PWD
trap '' PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen520
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
cd /nope 2>&1; echo rc=$?
set -o
%%%
#!name gen521
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen522
#!mode=file
B0=$PWD
set -- -- -a -b; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -ab; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen523
#!mode=file
B0=$PWD
set -- -- --; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<E
line1
line2
E
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen524
#!mode=file
B0=$PWD
set -o
env | grep -c '^vv='
set | grep -c '^PPID='
%%%
#!name gen525
#!mode=file
B0=$PWD
set +o ignoreeof; echo "$-"
export LC_COLLATE='POSIX'
%%%
#!name gen526
#!mode=file
B0=$PWD
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
set -- -a -a -b -ab; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo A' USR2 2>&1; trap - USR2 2>&1; trap; echo rc=$?
%%%
#!name gen527
#!mode=file
B0=$PWD
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
g() { local B B; B='/tmp'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen528
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen529
#!mode=file
B0=$PWD
set -- x; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -; echo "$-"
%%%
#!name gen530
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen531
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen532
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
trap 'echo E' EXIT; echo body
%%%
#!name gen533
#!mode=file
B0=$PWD
g() { local V1 B; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen534
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
trap '' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen535
#!mode=file
B0=$PWD
LINENO='0'
alias echo='echo pre'; echo hi
g() { local V1 B; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen536
#!mode=file
B0=$PWD
g() { local B B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen537
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen538
#!mode=file
B0=$PWD
set -- -bvalue; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -z -a -b x; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen539
#!mode=file
B0=$PWD
g() { local IFS='abc'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen540
#!mode=file
B0=$PWD
unset -v vv
%%%
#!name gen541
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
alias a='b'; alias b='echo B'; a
%%%
#!name gen542
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -a x; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen543
#!mode=file
B0=$PWD
export PS4
%%%
#!name gen544
#!mode=file
B0=$PWD
set -- -a -z -a; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen545
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen546
#!mode=file
B0=$PWD
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
%%%
#!name gen547
#!mode=file
B0=$PWD
set -iV; echo "$-"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
trap 'echo A' PIPE 2>&1; trap - PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen548
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen549
#!mode=file
B0=$PWD
set +o verbose; echo "$-"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
cd /nope 2>&1; echo rc=$?
%%%
#!name gen550
#!mode=file
B0=$PWD
set -o verbose; echo "$-"
%%%
#!name gen551
#!mode=file
B0=$PWD
export -p | grep -c 'vv' 
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
alias echo='echo pre'; echo hi
%%%
#!name gen552
#!mode=file
B0=$PWD
TERM='0'
trap 'echo A' PIPE 2>&1; trap - PIPE 2>&1; trap; echo rc=$?
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen553
#!mode=file
B0=$PWD
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen554
#!mode=file
B0=$PWD
set -- -bvalue -a -b -b; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
set -- -b -a -a x; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen555
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- -b -a x; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen556
#!mode=file
B0=$PWD
trap '' INT 2>&1; trap; echo rc=$?
f() { echo F; }; f >f.txt 2>&1; cat f.txt
{ echo p; } 5>&- ; echo rc=$?
%%%
#!name gen557
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
%%%
#!name gen558
#!mode=file
B0=$PWD
set -- -b; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen559
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen560
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen561
#!mode=file
B0=$PWD
unset -v PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen562
#!mode=file
B0=$PWD
set -- -a x -a; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; a; alias a
%%%
#!name gen563
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen564
#!mode=file
B0=$PWD
set -o
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen565
#!mode=file
B0=$PWD
set -bi; echo "$-"
%%%
#!name gen566
#!mode=file
B0=$PWD
g() { local IFS B; IFS='.'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen567
#!mode=file
B0=$PWD
(exec 3>s.txt; echo s >&3); cat s.txt; echo t >&3 2>&1; echo rc=$?
%%%
#!name gen568
#!mode=file
B0=$PWD
set -- -ab -- x; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -bvalue -a -b; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -ab; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen569
#!mode=file
B0=$PWD
trap '' INT 2>&1; trap; echo rc=$?
set -- -c -c; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen570
#!mode=file
B0=$PWD
set +o xtrace; echo "$-"
%%%
#!name gen571
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen572
#!mode=file
B0=$PWD
g() { local -; set -u; echo "in:$-"; }; g; echo "out:$-"
g() { local -; set -o xtrace; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen573
#!mode=file
B0=$PWD
readonly B='x=y'
g() { local PATH='x=y'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
cat <<-E
	indented
	E
%%%
#!name gen574
#!mode=file
B0=$PWD
g() { local -; set -o errexit; echo "in:$-"; }; g; echo "out:$-"
alias echo='echo pre'; echo hi
g() { local vv; vv='a'b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen575
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
%%%
#!name gen576
#!mode=file
B0=$PWD
set -; echo "$-"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
set --; echo "$#"
%%%
#!name gen577
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- -a x -b; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen578
#!mode=file
B0=$PWD
g() { local IFS='"q"'; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
export -p | grep -c 'PS4' 
alias echo='echo pre'; echo hi
%%%
#!name gen579
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen580
#!mode=file
B0=$PWD
trap 'echo T' 12 2>&1; trap; echo rc=$?
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
set -- -b -bvalue -bvalue; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen581
#!mode=file
B0=$PWD
env | grep -c '^LC_COLLATE='
%%%
#!name gen582
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen583
#!mode=file
B0=$PWD
set -- -a x -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen584
#!mode=file
B0=$PWD
g() { local V1 B; V1=':'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen585
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen586
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
cd /nope 2>&1; echo rc=$?
export -p | grep -c 'CDPATH' 
%%%
#!name gen587
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
%%%
#!name gen588
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen589
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
readonly PS2=':'
%%%
#!name gen590
#!mode=file
B0=$PWD
g() { local PATH='"q"'; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen591
#!mode=file
B0=$PWD
g() { local -; set -o pipefail; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen592
#!mode=file
B0=$PWD
trap 'echo T' INT 2>&1; trap; echo rc=$?
%%%
#!name gen593
#!mode=file
B0=$PWD
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
set -- x; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set --; echo "$#"
%%%
#!name gen594
#!mode=file
B0=$PWD
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen595
#!mode=file
B0=$PWD
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen596
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- --; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen597
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
read v <<E
value
E
echo "[$v]"
%%%
#!name gen598
#!mode=file
B0=$PWD
set -o
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen599
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap 'echo T' INT 2>&1; trap; echo rc=$?
%%%
#!name gen600
#!mode=file
B0=$PWD
export LC_CTYPE='1'
%%%
#!name gen601
#!mode=file
B0=$PWD
export -p | grep -c 'HOME' 
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -; echo "$-"
%%%
#!name gen602
#!mode=file
B0=$PWD
MAILPATH='POSIX'
set -- x -- -b; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen603
#!mode=file
B0=$PWD
set | grep -c '^LC_NUMERIC='
g() { local -; set +v; echo "in:$-"; }; g; echo "out:$-"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen604
#!mode=file
B0=$PWD
trap 'echo T' EXIT 2>&1; trap; echo rc=$?
g() { local IFS='/tmp'; IFS='/tmp'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
unset -v PWD
%%%
#!name gen605
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
alias a='b'; alias b='echo B'; a
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen606
#!mode=file
B0=$PWD
g() { local PATH B; PATH='\t'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen607
#!mode=file
B0=$PWD
trap USR1 2>&1; trap; echo rc=$?
unset B
g() { local OPTIND='a b'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen608
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen609
#!mode=file
B0=$PWD
set +o
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen610
#!mode=file
B0=$PWD
set -o allexport; echo "$-"
%%%
#!name gen611
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
g() { local OPTIND='C'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen612
#!mode=file
B0=$PWD
g() { local A='C'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set +o
%%%
#!name gen613
#!mode=file
B0=$PWD
trap 'echo T' 10 2>&1; trap; echo rc=$?
echo "[${IFS-U}]"
%%%
#!name gen614
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen615
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen616
#!mode=file
B0=$PWD
export PS1=''
set -- -ab -ab -a -b; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen617
#!mode=file
B0=$PWD
set -- -b -a x -c; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b'; alias b='echo B'; a
%%%
#!name gen618
#!mode=file
B0=$PWD
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
set -; echo "$-"
%%%
#!name gen619
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
trap '' PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen620
#!mode=file
B0=$PWD
env | grep -c '^CDPATH='
%%%
#!name gen621
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
unset -v LINENO
set -C; echo "$-"
%%%
#!name gen622
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
g() { local B='x=y'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen623
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
readonly HISTSIZE=''
%%%
#!name gen624
#!mode=file
B0=$PWD
set -- -bvalue -c x; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen625
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
g() { local A B; A='a'b'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen626
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
set -- -- -z; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen627
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -- -bvalue -b; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen628
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen629
#!mode=file
B0=$PWD
set -- -a -z -bvalue; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -c -a -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set --; echo "$#"
%%%
#!name gen630
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen631
#!mode=file
B0=$PWD
env | grep -c '^IFS='
%%%
#!name gen632
#!mode=file
B0=$PWD
set --; echo "$#"
set -m; echo "$-"
%%%
#!name gen633
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
set -- -a; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen634
#!mode=file
B0=$PWD
g() { local B B; B=''; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen635
#!mode=file
B0=$PWD
echo "[${MAILPATH}]"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
env | grep -c '^LC_ALL='
%%%
#!name gen636
#!mode=file
B0=$PWD
echo "[${LC_NUMERIC}]"
%%%
#!name gen637
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen638
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
cat <<'E'
$notexpanded
E
%%%
#!name gen639
#!mode=file
B0=$PWD
set -- -a -ab -bvalue; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b'; alias b='echo B'; a
%%%
#!name gen640
#!mode=file
B0=$PWD
g() { local -; set -I; echo "in:$-"; }; g; echo "out:$-"
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
%%%
#!name gen641
#!mode=file
B0=$PWD
trap TERM 2>&1; trap; echo rc=$?
set -o pipefail; echo "$-"
trap 'echo A' CHLD 2>&1; trap - CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen642
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
alias a='b'; alias b='echo B'; a
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen643
#!mode=file
B0=$PWD
set --; echo "$#"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
%%%
#!name gen644
#!mode=file
B0=$PWD
set -- -ab x; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a x -b; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local -; set +i; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen645
#!mode=file
B0=$PWD
trap 'echo T' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen646
#!mode=file
B0=$PWD
set +o
%%%
#!name gen647
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen648
#!mode=file
B0=$PWD
set -; echo "$-"
set -- a c b; echo "$#:$*"
set -; echo "$-"
%%%
#!name gen649
#!mode=file
B0=$PWD
unset -v PATH
{ echo p; } 9>&- ; echo rc=$?
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen650
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen651
#!mode=file
B0=$PWD
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen652
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen653
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
set -- -a x -z -ab; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen654
#!mode=file
B0=$PWD
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
%%%
#!name gen655
#!mode=file
B0=$PWD
g() { local -; set -f; echo "in:$-"; }; g; echo "out:$-"
unset OLDPWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen656
#!mode=file
B0=$PWD
g() { local V1; V1='POSIX'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
g() { local B B; B='POSIX'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen657
#!mode=file
B0=$PWD
trap 'echo A' 1 2>&1; trap - 1 2>&1; trap; echo rc=$?
%%%
#!name gen658
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -f; echo "$-"
%%%
#!name gen659
#!mode=file
B0=$PWD
set -- -ab -bvalue -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen660
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
%%%
#!name gen661
#!mode=file
B0=$PWD
g() { local V1; V1='x=y'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen662
#!mode=file
B0=$PWD
set -- -ab -bvalue; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
trap 'echo E' EXIT; echo body
%%%
#!name gen663
#!mode=file
B0=$PWD
exec 3<&-; read v <&3 2>&1; echo rc=$?
g() { local B; B='\t'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
cat <<E
line1
line2
E
%%%
#!name gen664
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen665
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen666
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
set -- -ab -c -b; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen667
#!mode=file
B0=$PWD
set -- -a -b -bvalue -c; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo "[${PWD#$B0}]"
set -- -z x; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen668
#!mode=file
B0=$PWD
unset -v vv
%%%
#!name gen669
#!mode=file
B0=$PWD
set -- -z -a -b; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo T' 1 2>&1; trap; echo rc=$?
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen670
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen671
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen672
#!mode=file
B0=$PWD
set +o noexec; echo "$-"
%%%
#!name gen673
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen674
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen675
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -an; echo "$-"
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
%%%
#!name gen676
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
set -o
%%%
#!name gen677
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen678
#!mode=file
B0=$PWD
trap 'echo A' INT 2>&1; trap - INT 2>&1; trap; echo rc=$?
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
g() { local PATH B; PATH='POSIX'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen679
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
set -- -ab; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set --; echo "$#"
%%%
#!name gen680
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
{ echo p; } 6>&- ; echo rc=$?
%%%
#!name gen681
#!mode=file
B0=$PWD
g() { local V1 B; V1=''; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen682
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen683
#!mode=file
B0=$PWD
trap '' 0 2>&1; trap; echo rc=$?
%%%
#!name gen684
#!mode=file
B0=$PWD
echo "[${HOME-U}]"
%%%
#!name gen685
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen686
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen687
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
alias a='b'; alias b='echo B'; a
echo "[${CDPATH}]"
%%%
#!name gen688
#!mode=file
B0=$PWD
set +a; echo "$-"
%%%
#!name gen689
#!mode=file
B0=$PWD
set +o
cd /nope 2>&1; echo rc=$?
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen690
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen691
#!mode=file
B0=$PWD
set -o
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen692
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen693
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local -; set +n; echo "in:$-"; }; g; echo "out:$-"
set -Cu; echo "$-"
%%%
#!name gen694
#!mode=file
B0=$PWD
set -- -a x -bvalue; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
readonly V1='a b'
%%%
#!name gen695
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen696
#!mode=file
B0=$PWD
PS2='0'
trap 'echo E' EXIT; echo body
%%%
#!name gen697
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen698
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen699
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
g() { local vv; vv='"q"'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen700
#!mode=file
B0=$PWD
set -- -ab; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
unset CDPATH
%%%
#!name gen701
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -o notify; echo "$-"
%%%
#!name gen702
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
trap '' 10 2>&1; trap; echo rc=$?
%%%
#!name gen703
#!mode=file
B0=$PWD
export PPID='1'
set -- -a; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo "[${LANG-U}]"
%%%
#!name gen704
#!mode=file
B0=$PWD
unset -v OPTIND
%%%
#!name gen705
#!mode=file
B0=$PWD
g() { local V1 B; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set +o notify; echo "$-"
%%%
#!name gen706
#!mode=file
B0=$PWD
{ echo p; } 4>&- ; echo rc=$?
%%%
#!name gen707
#!mode=file
B0=$PWD
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen708
#!mode=file
B0=$PWD
set -- x; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo T' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen709
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen710
#!mode=file
B0=$PWD
set -o xtrace; echo "$-"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
g() { local V1='POSIX'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen711
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen712
#!mode=file
B0=$PWD
env | grep -c '^CDPATH='
trap 'echo X' INT 2>&1; (trap); echo ---; trap
%%%
#!name gen713
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen714
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
set --; echo "$#"
%%%
#!name gen715
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen716
#!mode=file
B0=$PWD
export -p | grep -c 'vv' 
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen717
#!mode=file
B0=$PWD
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
cat <<-E
	indented
	E
%%%
#!name gen718
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
set -; echo "$-"
trap 'echo X' 12 2>&1; (trap); echo ---; trap
%%%
#!name gen719
#!mode=file
B0=$PWD
set -- -a -a x; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -nb; echo "$-"
%%%
#!name gen720
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
trap 'echo E' EXIT; echo body
trap '' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen721
#!mode=file
B0=$PWD
set -xv; echo "$-"
%%%
#!name gen722
#!mode=file
B0=$PWD
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
cd /nope 2>&1; echo rc=$?
%%%
#!name gen723
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen724
#!mode=file
B0=$PWD
g() { local A; A='1'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local OPTIND; OPTIND='abc'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen725
#!mode=file
B0=$PWD
set -- -bvalue --; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen726
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
cat <<E
line1
line2
E
exec 4<&-; read v <&4 2>&1; echo rc=$?
%%%
#!name gen727
#!mode=file
B0=$PWD
export -p | grep -c 'IFS' 
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen728
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set +o
%%%
#!name gen729
#!mode=file
B0=$PWD
set -- -ab -a -b -a -b; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen730
#!mode=file
B0=$PWD
trap '' 12 2>&1; trap; echo rc=$?
%%%
#!name gen731
#!mode=file
B0=$PWD
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
set -- c  a a; echo "$#:$*"
exec 5>&1; echo x >&5; exec 5>&-; echo y >&5 2>&1; echo rc=$?
%%%
#!name gen732
#!mode=file
B0=$PWD
export -p | grep -c 'IFS' 
%%%
#!name gen733
#!mode=file
B0=$PWD
set +u; echo "$-"
%%%
#!name gen734
#!mode=file
B0=$PWD
g() { local A='1'; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen735
#!mode=file
B0=$PWD
trap 'echo X' HUP 2>&1; (trap); echo ---; trap
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen736
#!mode=file
B0=$PWD
g() { local IFS='.'; IFS='.'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -- b; echo "$#:$*"
%%%
#!name gen737
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen738
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
set -- -a x; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen739
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
cd /nope 2>&1; echo rc=$?
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen740
#!mode=file
B0=$PWD
exec 6<<E
fromfd
E
read v <&6; echo "[$v]"; exec 6<&-
trap 'echo X' QUIT 2>&1; (trap); echo ---; trap
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen741
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen742
#!mode=file
B0=$PWD
unset -v HISTSIZE
set -- -b; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b'; alias b='echo B'; a
%%%
#!name gen743
#!mode=file
B0=$PWD
{ echo p; } 5>&- ; echo rc=$?
alias a='b'; alias b='echo B'; a
%%%
#!name gen744
#!mode=file
B0=$PWD
set -- -ab -a x; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen745
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen746
#!mode=file
B0=$PWD
export PS4='/tmp'
{ echo p; } 5>&- ; echo rc=$?
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen747
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen748
#!mode=file
B0=$PWD
set -- -ab -bvalue; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap '' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen749
#!mode=file
B0=$PWD
set -- -a -b -bvalue -a x; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
LANG='x=y'
%%%
#!name gen750
#!mode=file
B0=$PWD
set +o
%%%
#!name gen751
#!mode=file
B0=$PWD
set -- -z; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen752
#!mode=file
B0=$PWD
trap 'echo T' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen753
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
trap 'echo T' TERM 2>&1; trap; echo rc=$?
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen754
#!mode=file
B0=$PWD
set -- -c -a x x; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -o nolog; echo "$-"
%%%
#!name gen755
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
set -; echo "$-"
%%%
#!name gen756
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
set +v; echo "$-"
%%%
#!name gen757
#!mode=file
B0=$PWD
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
%%%
#!name gen758
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
%%%
#!name gen759
#!mode=file
B0=$PWD
set -- -b -ab -bvalue; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<'E'
$notexpanded
E
%%%
#!name gen760
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen761
#!mode=file
B0=$PWD
trap 'echo T' CHLD 2>&1; trap; echo rc=$?
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set -- -a -b -a; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen762
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local A='\t'; A='\t'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen763
#!mode=file
B0=$PWD
trap 'echo X' 1 2>&1; (trap); echo ---; trap
%%%
#!name gen764
#!mode=file
B0=$PWD
trap 'echo X' PIPE 2>&1; (trap); echo ---; trap
alias a='b'; alias b='echo B'; a
cat <<-E
	indented
	E
%%%
#!name gen765
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
g() { local B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen766
#!mode=file
B0=$PWD
unset -v PS4
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen767
#!mode=file
B0=$PWD
export -p | grep -c 'MAIL' 
%%%
#!name gen768
#!mode=file
B0=$PWD
set -- -a -b; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<-E
	indented
	E
set -o notify; echo "$-"
%%%
#!name gen769
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
trap 'echo E' EXIT; echo body
%%%
#!name gen770
#!mode=file
B0=$PWD
unset MAILPATH
%%%
#!name gen771
#!mode=file
B0=$PWD
set -- x; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set +E; echo "$-"
set -- -c -ab -z; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen772
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
set -- c b c; echo "$#:$*"
set -- -a; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen773
#!mode=file
B0=$PWD
trap 'echo X' 12 2>&1; (trap); echo ---; trap
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
trap 'echo X' TERM 2>&1; (trap); echo ---; trap
%%%
#!name gen774
#!mode=file
B0=$PWD
set -- -bvalue --; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen775
#!mode=file
B0=$PWD
trap CHLD 2>&1; trap; echo rc=$?
g() { local PATH='a'b'; PATH='a'b'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen776
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='C'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen777
#!mode=file
B0=$PWD
g() { local PATH='1'; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen778
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
%%%
#!name gen779
#!mode=file
B0=$PWD
set -- c 1; echo "$#:$*"
trap 'echo A' 10 2>&1; trap - 10 2>&1; trap; echo rc=$?
%%%
#!name gen780
#!mode=file
B0=$PWD
trap 'echo X' USR2 2>&1; (trap); echo ---; trap
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen781
#!mode=file
B0=$PWD
exec 6<&-; read v <&6 2>&1; echo rc=$?
g() { local -; set -o monitor; echo "in:$-"; }; g; echo "out:$-"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen782
#!mode=file
B0=$PWD
set -- -a x -bvalue -bvalue; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -V; echo "$-"
%%%
#!name gen783
#!mode=file
B0=$PWD
trap 'echo A' INT 2>&1; trap - INT 2>&1; trap; echo rc=$?
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
%%%
#!name gen784
#!mode=file
B0=$PWD
set -e; echo "$-"
%%%
#!name gen785
#!mode=file
B0=$PWD
set -- -z -z -b; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
CDPATH='POSIX'
set -- a b c d e; shift 3 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen786
#!mode=file
B0=$PWD
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
set -- -b -a x -c; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen787
#!mode=file
B0=$PWD
g() { local vv; vv='a b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
g() { local -; set -o vi; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen788
#!mode=file
B0=$PWD
set -- -b -a -b -c; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -o
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen789
#!mode=file
B0=$PWD
set -o
%%%
#!name gen790
#!mode=file
B0=$PWD
set -- -a -b -a -ab; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen791
#!mode=file
B0=$PWD
trap 'echo T' CHLD 2>&1; trap; echo rc=$?
export -p | grep -c 'LANG' 
%%%
#!name gen792
#!mode=file
B0=$PWD
set -- -a x; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 12 2>&1; trap; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen793
#!mode=file
B0=$PWD
exec 3>r3.txt; echo hi >&3; exec 3>&-; cat r3.txt
set +o
set -- -bvalue; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen794
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
set -- -- -- -a; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen795
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen796
#!mode=file
B0=$PWD
set -- -a x -a -b -a x; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen797
#!mode=file
B0=$PWD
g() { local V1 B; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -; echo "$-"
g() { local vv; vv='a'b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen798
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen799
#!mode=file
B0=$PWD
exec 9<&-; read v <&9 2>&1; echo rc=$?
%%%
#!name gen800
#!mode=file
B0=$PWD
exec 9>r9.txt; echo hi >&9; exec 9>&-; cat r9.txt
set -- a b c d e; shift 3 2>&1; echo "rc=$? $#:$*"
PPID='.'
%%%
#!name gen801
#!mode=file
B0=$PWD
set | grep -c '^OLDPWD='
%%%
#!name gen802
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen803
#!mode=file
B0=$PWD
set -- -a -b -c -z; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen804
#!mode=file
B0=$PWD
set -CV; echo "$-"
%%%
#!name gen805
#!mode=file
B0=$PWD
trap 12 2>&1; trap; echo rc=$?
%%%
#!name gen806
#!mode=file
B0=$PWD
trap 'echo X' 12 2>&1; (trap); echo ---; trap
%%%
#!name gen807
#!mode=file
B0=$PWD
g() { local IFS='1'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen808
#!mode=file
B0=$PWD
export OLDPWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen809
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen810
#!mode=file
B0=$PWD
MAIL='POSIX'
set -- a c ; echo "$#:$*"
%%%
#!name gen811
#!mode=file
B0=$PWD
export PPID='x=y'
unset OPTIND
set -- -b -z; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen812
#!mode=file
B0=$PWD
g() { local IFS='a b'; IFS='a b'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen813
#!mode=file
B0=$PWD
set +b; echo "$-"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
trap 'echo E' EXIT; echo body
%%%
#!name gen814
#!mode=file
B0=$PWD
g() { local V1='0'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen815
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen816
#!mode=file
B0=$PWD
trap 'echo T' INT 2>&1; trap; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen817
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen818
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
(trap 'echo SE' EXIT; echo sub); echo main
export -p | grep -c 'A' 
%%%
#!name gen819
#!mode=file
B0=$PWD
g() { local vv='0'; vv='0'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- -a x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen820
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
alias 'a b'=1 2>&1; echo rc=$?
set -- -a -b -a -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen821
#!mode=file
B0=$PWD
set -ma; echo "$-"
set +o
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen822
#!mode=file
B0=$PWD
trap 'echo A' 1 2>&1; trap - 1 2>&1; trap; echo rc=$?
%%%
#!name gen823
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
trap 'echo X' QUIT 2>&1; (trap); echo ---; trap
set -- x -- -ab; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen824
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
trap 'echo A' INT 2>&1; trap - INT 2>&1; trap; echo rc=$?
set -- 1; echo "$#:$*"
%%%
#!name gen825
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen826
#!mode=file
B0=$PWD
{ echo p; } 4>&- ; echo rc=$?
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
trap 'echo E' EXIT; echo body
%%%
#!name gen827
#!mode=file
B0=$PWD
trap 'echo A' 1 2>&1; trap - 1 2>&1; trap; echo rc=$?
set -- -a x -c; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen828
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen829
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
set | grep -c '^V1='
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen830
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
alias 'a b'=1 2>&1; echo rc=$?
set -n; echo "$-"
%%%
#!name gen831
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen832
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen833
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen834
#!mode=file
B0=$PWD
env | grep -c '^LC_COLLATE='
%%%
#!name gen835
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
export -p | grep -c 'A' 
%%%
#!name gen836
#!mode=file
B0=$PWD
unset IFS
set -eE; echo "$-"
%%%
#!name gen837
#!mode=file
B0=$PWD
export PS1
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
%%%
#!name gen838
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
read v <<E
value
E
echo "[$v]"
%%%
#!name gen839
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen840
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -o verbose; echo "$-"
readonly LANG='a'b'
%%%
#!name gen841
#!mode=file
B0=$PWD
set -Vn; echo "$-"
%%%
#!name gen842
#!mode=file
B0=$PWD
unset A
set -- -ab -z -b; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b'; alias b='echo B'; a
%%%
#!name gen843
#!mode=file
B0=$PWD
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
set +o
%%%
#!name gen844
#!mode=file
B0=$PWD
g() { local -; set -o vi; echo "in:$-"; }; g; echo "out:$-"
set -; echo "$-"
g() { local PATH B; PATH=''; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen845
#!mode=file
B0=$PWD
set -- x -bvalue; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen846
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
readonly LC_ALL='a b'
%%%
#!name gen847
#!mode=file
B0=$PWD
set -o xtrace; echo "$-"
set -- -z; unset OPTIND; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen848
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen849
#!mode=file
B0=$PWD
set | grep -c '^OPTIND='
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen850
#!mode=file
B0=$PWD
g() { local A='abc'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen851
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias echo='echo pre'; echo hi
%%%
#!name gen852
#!mode=file
B0=$PWD
set -- -z -b; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen853
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
set -- x -bvalue; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen854
#!mode=file
B0=$PWD
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen855
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
%%%
#!name gen856
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen857
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
g() { local V1 B; V1='.'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen858
#!mode=file
B0=$PWD
g() { local OPTIND B; OPTIND='a'b'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen859
#!mode=file
B0=$PWD
(exec 3>s.txt; echo s >&3); cat s.txt; echo t >&3 2>&1; echo rc=$?
alias a='b'; alias b='echo B'; a
HOME='a'b'
%%%
#!name gen860
#!mode=file
B0=$PWD
TERM=''
set -- -c -ab -a; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen861
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
echo "[${A}]"
%%%
#!name gen862
#!mode=file
B0=$PWD
set -- -ab -b --; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -bvalue x -c; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen863
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
trap 'echo X' INT 2>&1; (trap); echo ---; trap
exec 9>r9.txt; echo hi >&9; exec 9>&-; cat r9.txt
%%%
#!name gen864
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen865
#!mode=file
B0=$PWD
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
%%%
#!name gen866
#!mode=file
B0=$PWD
set -- -b; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local A; A='1'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen867
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen868
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
export LC_CTYPE
%%%
#!name gen869
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen870
#!mode=file
B0=$PWD
unset HISTSIZE
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen871
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -ea; echo "$-"
%%%
#!name gen872
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen873
#!mode=file
B0=$PWD
echo "[${PS4-U}]"
set -- -b -b -z; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen874
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -b; echo "$-"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen875
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
alias a='b'; alias b='echo B'; a
%%%
#!name gen876
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
set -- -a x -- -b; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -V; echo "$-"
%%%
#!name gen877
#!mode=file
B0=$PWD
unset -v PS1
g() { local A; A=''; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen878
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -; echo "$-"
%%%
#!name gen879
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen880
#!mode=file
B0=$PWD
set -- -a -a -a; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen881
#!mode=file
B0=$PWD
env | grep -c '^PS1='
set -- -a x; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -o
%%%
#!name gen882
#!mode=file
B0=$PWD
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen883
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
unset -v A
g() { local B B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen884
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
readonly HISTSIZE='abc'
%%%
#!name gen885
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
set +o nounset; echo "$-"
set -- -z -bvalue; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen886
#!mode=file
B0=$PWD
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen887
#!mode=file
B0=$PWD
export MAIL
alias echo='echo pre'; echo hi
%%%
#!name gen888
#!mode=file
B0=$PWD
set -o
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen889
#!mode=file
B0=$PWD
export MAILPATH
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
g() { local B; B='1'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen890
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen891
#!mode=file
B0=$PWD
trap 'echo T' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen892
#!mode=file
B0=$PWD
export -p | grep -c 'PS4' 
g() { local PATH='/tmp'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen893
#!mode=file
B0=$PWD
trap 'echo A' CHLD 2>&1; trap - CHLD 2>&1; trap; echo rc=$?
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- -z -z; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen894
#!mode=file
B0=$PWD
set -u; echo "$-"
%%%
#!name gen895
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -- -- -a -a -b; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cd /nope 2>&1; echo rc=$?
%%%
#!name gen896
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
%%%
#!name gen897
#!mode=file
B0=$PWD
set -o
alias a='b'; alias b='echo B'; a
alias echo='echo pre'; echo hi
%%%
#!name gen898
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen899
#!mode=file
B0=$PWD
set -- -a -b -a -b; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -b; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen900
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
MAIL='abc'
set -f; echo "$-"
%%%
#!name gen901
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen902
#!mode=file
B0=$PWD
trap 'echo A' TERM 2>&1; trap - TERM 2>&1; trap; echo rc=$?
%%%
#!name gen903
#!mode=file
B0=$PWD
trap 'echo T' 12 2>&1; trap; echo rc=$?
set -- -a; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen904
#!mode=file
B0=$PWD
{ echo p; } 4>&- ; echo rc=$?
set +o notify; echo "$-"
%%%
#!name gen905
#!mode=file
B0=$PWD
set +V; echo "$-"
trap '' INT 2>&1; trap; echo rc=$?
%%%
#!name gen906
#!mode=file
B0=$PWD
g() { local IFS B; IFS=''; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -o
%%%
#!name gen907
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen908
#!mode=file
B0=$PWD
g() { local OPTIND='a'b'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen909
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen910
#!mode=file
B0=$PWD
trap USR2 2>&1; trap; echo rc=$?
%%%
#!name gen911
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen912
#!mode=file
B0=$PWD
export PS4=''
%%%
#!name gen913
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen914
#!mode=file
B0=$PWD
set -- -c -a -b -bvalue; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -o allexport; echo "$-"
set -- c c; echo "$#:$*"
%%%
#!name gen915
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
set -o pipefail; echo "$-"
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen916
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen917
#!mode=file
B0=$PWD
set -- -z -a x x; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
(trap 'echo SE' EXIT; echo sub); echo main
set -; echo "$-"
%%%
#!name gen918
#!mode=file
B0=$PWD
set -- -c -bvalue -z; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen919
#!mode=file
B0=$PWD
readonly A='/tmp'
set -- -a -b; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen920
#!mode=file
B0=$PWD
trap '' HUP 2>&1; trap; echo rc=$?
%%%
#!name gen921
#!mode=file
B0=$PWD
{ echo p; } 3>&- ; echo rc=$?
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen922
#!mode=file
B0=$PWD
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen923
#!mode=file
B0=$PWD
g() { local A B; A='abc'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -- -ab -a -c; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen924
#!mode=file
B0=$PWD
set +o
%%%
#!name gen925
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen926
#!mode=file
B0=$PWD
env | grep -c '^PS1='
%%%
#!name gen927
#!mode=file
B0=$PWD
echo "[${CDPATH-U}]"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen928
#!mode=file
B0=$PWD
trap 0 2>&1; trap; echo rc=$?
%%%
#!name gen929
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen930
#!mode=file
B0=$PWD
readonly LC_COLLATE=''
g() { local V1 B; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen931
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen932
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -o vi; echo "$-"
trap 1 2>&1; trap; echo rc=$?
%%%
#!name gen933
#!mode=file
B0=$PWD
echo "[${OPTIND-U}]"
set -- -b -a -b; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen934
#!mode=file
B0=$PWD
g() { local -; set +I; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen935
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen936
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen937
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
readonly PWD='x=y'
set +o
%%%
#!name gen938
#!mode=file
B0=$PWD
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set -o
%%%
#!name gen939
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen940
#!mode=file
B0=$PWD
set -v; echo "$-"
%%%
#!name gen941
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen942
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
%%%
#!name gen943
#!mode=file
B0=$PWD
set -- -a -a x; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo "[${LC_CTYPE-U}]"
%%%
#!name gen944
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
g() { local IFS B; IFS='C'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen945
#!mode=file
B0=$PWD
A='"q"'
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen946
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen947
#!mode=file
B0=$PWD
set -- -ab --; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local vv B; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen948
#!mode=file
B0=$PWD
set -- -ab -a; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export LC_NUMERIC='abc'
%%%
#!name gen949
#!mode=file
B0=$PWD
g() { local vv; vv='POSIX'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
env | grep -c '^TERM='
set -- -z -b -bvalue; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen950
#!mode=file
B0=$PWD
set -- -c; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- b  ; echo "$#:$*"
%%%
#!name gen951
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
set +V; echo "$-"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen952
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
%%%
#!name gen953
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
env | grep -c '^LC_COLLATE='
cat <<'E'
$notexpanded
E
%%%
#!name gen954
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
%%%
#!name gen955
#!mode=file
B0=$PWD
set -; echo "$-"
alias a='echo A'; a; alias a
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen956
#!mode=file
B0=$PWD
set +o ignoreeof; echo "$-"
%%%
#!name gen957
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
trap 'echo A' TERM 2>&1; trap - TERM 2>&1; trap; echo rc=$?
set -- -bvalue -a -b; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen958
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
env | grep -c '^PS2='
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen959
#!mode=file
B0=$PWD
set -o
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen960
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen961
#!mode=file
B0=$PWD
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
unset -v LINENO
%%%
#!name gen962
#!mode=file
B0=$PWD
trap 'echo X' ALRM 2>&1; (trap); echo ---; trap
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen963
#!mode=file
B0=$PWD
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
echo "[${LC_COLLATE}]"
%%%
#!name gen964
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias a='b'; alias b='echo B'; a
exec 6>r6.txt; echo hi >&6; exec 6>&-; cat r6.txt
%%%
#!name gen965
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen966
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
%%%
#!name gen967
#!mode=file
B0=$PWD
set +o
%%%
#!name gen968
#!mode=file
B0=$PWD
set +C; echo "$-"
alias a='echo A'; a; alias a
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen969
#!mode=file
B0=$PWD
set -- b; echo "$#:$*"
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
%%%
#!name gen970
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen971
#!mode=file
B0=$PWD
set --; echo "$#"
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -b; echo "$-"
%%%
#!name gen972
#!mode=file
B0=$PWD
set -- -ab -a -b; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local B='x=y'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
%%%
#!name gen973
#!mode=file
B0=$PWD
set +e; echo "$-"
%%%
#!name gen974
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
g() { local vv='1'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -b; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen975
#!mode=file
B0=$PWD
set -; echo "$-"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen976
#!mode=file
B0=$PWD
set -o noclobber; echo "$-"
%%%
#!name gen977
#!mode=file
B0=$PWD
set -- x -a -b; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen978
#!mode=file
B0=$PWD
set -- -a x -ab; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<-E
	indented
	E
%%%
#!name gen979
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
export PS4='0'
%%%
#!name gen980
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen981
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
g() { local IFS=':'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
cat <<-E
	indented
	E
%%%
#!name gen982
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
echo "[${MAIL-U}]"
%%%
#!name gen983
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen984
#!mode=file
B0=$PWD
{ echo p; } 9>&- ; echo rc=$?
g() { local A=':'; A=':'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -- -ab; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen985
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
g() { local PATH='a b'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen986
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
alias a='echo A'; a; alias a
%%%
#!name gen987
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen988
#!mode=file
B0=$PWD
set -o noglob; echo "$-"
set --; echo "$#"
%%%
#!name gen989
#!mode=file
B0=$PWD
set -- -- -a; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -c -a; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen990
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen991
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
cat <<E
line1
line2
E
%%%
#!name gen992
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -- -c -ab -b; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export LANG
%%%
#!name gen993
#!mode=file
B0=$PWD
set -o vi; echo "$-"
%%%
#!name gen994
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- -z -b; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen995
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -- -c; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen996
#!mode=file
B0=$PWD
set -- --; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen997
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
trap 'echo X' CHLD 2>&1; (trap); echo ---; trap
set -- -ab -ab; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen998
#!mode=file
B0=$PWD
set -- b  b; echo "$#:$*"
%%%
#!name gen999
#!mode=file
B0=$PWD
set -o noexec; echo "$-"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
alias echo='echo pre'; echo hi
%%%
#!name gen1000
#!mode=file
B0=$PWD
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
g() { local OPTIND='"q"'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1001
#!mode=file
B0=$PWD
set -- -a x; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1002
#!mode=file
B0=$PWD
set -xI; echo "$-"
%%%
#!name gen1003
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
readonly MAIL='abc'
%%%
#!name gen1004
#!mode=file
B0=$PWD
g() { local -; set -o verbose; echo "in:$-"; }; g; echo "out:$-"
trap 'echo E' EXIT; echo body
IFS='POSIX'
%%%
#!name gen1005
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
export PWD='POSIX'
%%%
#!name gen1006
#!mode=file
B0=$PWD
set -- --; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1007
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
cat <<'E'
$notexpanded
E
%%%
#!name gen1008
#!mode=file
B0=$PWD
set -- -c -a -b; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1009
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
export -p | grep -c 'IFS' 
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1010
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
env | grep -c '^MAILPATH='
%%%
#!name gen1011
#!mode=file
B0=$PWD
set -vv; echo "$-"
%%%
#!name gen1012
#!mode=file
B0=$PWD
g() { local -; set -o noglob; echo "in:$-"; }; g; echo "out:$-"
trap 'echo A' USR2 2>&1; trap - USR2 2>&1; trap; echo rc=$?
%%%
#!name gen1013
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
alias a='b'; alias b='echo B'; a
%%%
#!name gen1014
#!mode=file
B0=$PWD
g() { local IFS='/tmp'; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1015
#!mode=file
B0=$PWD
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -- -a -b; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local B=':'; B=':'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1016
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen1017
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1018
#!mode=file
B0=$PWD
set -- x -a x; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1019
#!mode=file
B0=$PWD
env | grep -c '^LC_CTYPE='
env | grep -c '^OLDPWD='
%%%
#!name gen1020
#!mode=file
B0=$PWD
g() { local V1 B; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -- -bvalue -a x; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x -a -a x; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1021
#!mode=file
B0=$PWD
set -- -a x -a x; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1022
#!mode=file
B0=$PWD
set -aI; echo "$-"
%%%
#!name gen1023
#!mode=file
B0=$PWD
g() { local vv='a'b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
alias echo='echo pre'; echo hi
set -- --; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1024
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
{ echo p; } 9>&- ; echo rc=$?
set -- -b -bvalue x; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1025
#!mode=file
B0=$PWD
unset -v PS1
%%%
#!name gen1026
#!mode=file
B0=$PWD
set +o nounset; echo "$-"
env | grep -c '^LINENO='
%%%
#!name gen1027
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1028
#!mode=file
B0=$PWD
echo "[${LINENO-U}]"
%%%
#!name gen1029
#!mode=file
B0=$PWD
exec 5<&-; read v <&5 2>&1; echo rc=$?
%%%
#!name gen1030
#!mode=file
B0=$PWD
echo "[${OLDPWD#$B0}]"
%%%
#!name gen1031
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
g() { local PATH=':'; PATH=':'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local B; B=':'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1032
#!mode=file
B0=$PWD
LC_NUMERIC='POSIX'
cd /nope 2>&1; echo rc=$?
%%%
#!name gen1033
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set +o
PATH='C'
%%%
#!name gen1034
#!mode=file
B0=$PWD
g() { local IFS='0'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
{ echo p; } 3>&- ; echo rc=$?
exec 5<&-; read v <&5 2>&1; echo rc=$?
%%%
#!name gen1035
#!mode=file
B0=$PWD
set -- x; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1036
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen1037
#!mode=file
B0=$PWD
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
g() { local OPTIND; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
echo "[${PS4-U}]"
%%%
#!name gen1038
#!mode=file
B0=$PWD
set +o
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1039
#!mode=file
B0=$PWD
set -- x; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a -b; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1040
#!mode=file
B0=$PWD
g() { local OPTIND='x=y'; OPTIND='x=y'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -u; echo "$-"
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1041
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
export -p | grep -c 'LINENO' 
%%%
#!name gen1042
#!mode=file
B0=$PWD
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen1043
#!mode=file
B0=$PWD
set | grep -c '^B='
%%%
#!name gen1044
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
set -o
%%%
#!name gen1045
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1046
#!mode=file
B0=$PWD
set -o
%%%
#!name gen1047
#!mode=file
B0=$PWD
export CDPATH='C'
exec 5>r5.txt; echo hi >&5; exec 5>&-; cat r5.txt
read v <<E
value
E
echo "[$v]"
%%%
#!name gen1048
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1049
#!mode=file
B0=$PWD
set +b; echo "$-"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
set -o
%%%
#!name gen1050
#!mode=file
B0=$PWD
set +x; echo "$-"
read v <<E
value
E
echo "[$v]"
g() { local -; set +b; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1051
#!mode=file
B0=$PWD
set -- -a; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<E
line1
line2
E
set -- -z -z; OPTIND=3; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1052
#!mode=file
B0=$PWD
g() { local PATH B; PATH='a b'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1053
#!mode=file
B0=$PWD
set -Ea; echo "$-"
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
%%%
#!name gen1054
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
export CDPATH
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1055
#!mode=file
B0=$PWD
g() { local -; set +x; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1056
#!mode=file
B0=$PWD
set -; echo "$-"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1057
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
f() { echo F; }; f >f.txt 2>&1; cat f.txt
set -- -a x; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1058
#!mode=file
B0=$PWD
trap 0 2>&1; trap; echo rc=$?
set +o
%%%
#!name gen1059
#!mode=file
B0=$PWD
trap 10 2>&1; trap; echo rc=$?
trap 'echo A' TERM 2>&1; trap - TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1060
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen1061
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1062
#!mode=file
B0=$PWD
trap INT 2>&1; trap; echo rc=$?
set -o allexport; echo "$-"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1063
#!mode=file
B0=$PWD
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1064
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1065
#!mode=file
B0=$PWD
set +o
set -- x; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1066
#!mode=file
B0=$PWD
set | grep -c '^B='
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1067
#!mode=file
B0=$PWD
export -p | grep -c 'CDPATH' 
%%%
#!name gen1068
#!mode=file
B0=$PWD
set -- -ab; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- -a x; unset OPTIND; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap INT 2>&1; trap; echo rc=$?
%%%
#!name gen1069
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1070
#!mode=file
B0=$PWD
export A='abc'
trap 'echo E' EXIT; echo body
%%%
#!name gen1071
#!mode=file
B0=$PWD
export HOME='/tmp'
trap 'echo E' EXIT; echo body
%%%
#!name gen1072
#!mode=file
B0=$PWD
g() { local V1='/tmp'; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
(trap 'echo SE' EXIT; echo sub); echo main
cat <<-E
	indented
	E
%%%
#!name gen1073
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1074
#!mode=file
B0=$PWD
trap 'echo A' INT 2>&1; trap - INT 2>&1; trap; echo rc=$?
%%%
#!name gen1075
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
set -- -z -bvalue; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set --; echo "$#"
%%%
#!name gen1076
#!mode=file
B0=$PWD
g() { local OPTIND='1'; OPTIND='1'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -o
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1077
#!mode=file
B0=$PWD
set +o notify; echo "$-"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1078
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
trap 'echo T' TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1079
#!mode=file
B0=$PWD
g() { local vv B; vv='"q"'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1080
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
set -- -ab -b -ab; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1081
#!mode=file
B0=$PWD
trap CHLD 2>&1; trap; echo rc=$?
alias a='echo A'; a; alias a
set -- --; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1082
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
%%%
#!name gen1083
#!mode=file
B0=$PWD
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1084
#!mode=file
B0=$PWD
export -p | grep -c 'LC_COLLATE' 
%%%
#!name gen1085
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1086
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1087
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
set -- -bvalue -a x --; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1088
#!mode=file
B0=$PWD
exec 5<&-; read v <&5 2>&1; echo rc=$?
%%%
#!name gen1089
#!mode=file
B0=$PWD
echo "[${OPTIND-U}]"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1090
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1091
#!mode=file
B0=$PWD
unset LC_COLLATE
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1092
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
g() { local A B; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1093
#!mode=file
B0=$PWD
set -- -z -a -b; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
LANG='"q"'
%%%
#!name gen1094
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
g() { local vv=''; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1095
#!mode=file
B0=$PWD
export -p | grep -c 'PS4' 
set -o noexec; echo "$-"
%%%
#!name gen1096
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
set -- x -a -c; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1097
#!mode=file
B0=$PWD
g() { local A='x=y'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
g() { local vv B; vv='a'b'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1098
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1099
#!mode=file
B0=$PWD
set -- -z x; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
trap 'echo A' INT 2>&1; trap - INT 2>&1; trap; echo rc=$?
%%%
#!name gen1100
#!mode=file
B0=$PWD
set +C; echo "$-"
unset A
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1101
#!mode=file
B0=$PWD
set -- -bvalue -a -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x -b --; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1102
#!mode=file
B0=$PWD
echo "[${PS2}]"
%%%
#!name gen1103
#!mode=file
B0=$PWD
export OLDPWD
exec 4<&-; read v <&4 2>&1; echo rc=$?
%%%
#!name gen1104
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -Ee; echo "$-"
set +o vi; echo "$-"
%%%
#!name gen1105
#!mode=file
B0=$PWD
trap CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen1106
#!mode=file
B0=$PWD
unset OPTIND
cat <<'E'
$notexpanded
E
%%%
#!name gen1107
#!mode=file
B0=$PWD
unset -v LC_ALL
%%%
#!name gen1108
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
unset -v LC_COLLATE
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen1109
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen1110
#!mode=file
B0=$PWD
set -- ; echo "$#:$*"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1111
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
env | grep -c '^HISTSIZE='
export -p | grep -c 'HOME' 
%%%
#!name gen1112
#!mode=file
B0=$PWD
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -- --; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1113
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
cat <<-E
	indented
	E
%%%
#!name gen1114
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
%%%
#!name gen1115
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
trap 'echo X' 1 2>&1; (trap); echo ---; trap
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1116
#!mode=file
B0=$PWD
g() { local B='C'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1117
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
%%%
#!name gen1118
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1119
#!mode=file
B0=$PWD
export -p | grep -c 'PPID' 
%%%
#!name gen1120
#!mode=file
B0=$PWD
trap '' EXIT 2>&1; trap; echo rc=$?
trap 'echo A' TERM 2>&1; trap - TERM 2>&1; trap; echo rc=$?
set -o verbose; echo "$-"
%%%
#!name gen1121
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
echo "[${LC_CTYPE-U}]"
%%%
#!name gen1122
#!mode=file
B0=$PWD
export LC_COLLATE='x=y'
export V1=''
set -- -bvalue; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1123
#!mode=file
B0=$PWD
set -- --; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -V; echo "$-"
%%%
#!name gen1124
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1125
#!mode=file
B0=$PWD
trap '' TERM 2>&1; trap; echo rc=$?
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
g() { local A; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1126
#!mode=file
B0=$PWD
set -- c b ; echo "$#:$*"
%%%
#!name gen1127
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -- -bvalue; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1128
#!mode=file
B0=$PWD
set -- -z x; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
env | grep -c '^OPTIND='
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1129
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -; echo "$-"
readonly PS2='x=y'
%%%
#!name gen1130
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1131
#!mode=file
B0=$PWD
echo "[${IFS}]"
echo "[${LC_NUMERIC-U}]"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1132
#!mode=file
B0=$PWD
set -I; echo "$-"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1133
#!mode=file
B0=$PWD
trap 'echo X' INT 2>&1; (trap); echo ---; trap
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
g() { local IFS; IFS='C'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1134
#!mode=file
B0=$PWD
export PS1
%%%
#!name gen1135
#!mode=file
B0=$PWD
(exec 5>s.txt; echo s >&5); cat s.txt; echo t >&5 2>&1; echo rc=$?
set +o nounset; echo "$-"
trap '' 0 2>&1; trap; echo rc=$?
%%%
#!name gen1136
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1137
#!mode=file
B0=$PWD
set -n; echo "$-"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
export PPID='a b'
%%%
#!name gen1138
#!mode=file
B0=$PWD
set -- x -z; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -- x; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1139
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
export -p | grep -c 'TERM' 
%%%
#!name gen1140
#!mode=file
B0=$PWD
set +o noglob; echo "$-"
trap '' QUIT 2>&1; trap; echo rc=$?
g() { local -; set +a; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1141
#!mode=file
B0=$PWD
env | grep -c '^vv='
%%%
#!name gen1142
#!mode=file
B0=$PWD
export -p | grep -c 'MAIL' 
set -- -bvalue -z; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo E' EXIT; echo body
%%%
#!name gen1143
#!mode=file
B0=$PWD
export -p | grep -c 'PWD' 
%%%
#!name gen1144
#!mode=file
B0=$PWD
set -- -bvalue -a -b; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1145
#!mode=file
B0=$PWD
g() { local -; set +i; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1146
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1147
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
set -m; echo "$-"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1148
#!mode=file
B0=$PWD
env | grep -c '^LC_ALL='
export A
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1149
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
unset MAIL
set -- x -a -b -c; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1150
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
alias a='b'; alias b='echo B'; a
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1151
#!mode=file
B0=$PWD
set -- -b; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
g() { local IFS='POSIX'; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1152
#!mode=file
B0=$PWD
set --; echo "$#"
cat <<-E
	indented
	E
g() { local OPTIND B; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1153
#!mode=file
B0=$PWD
g() { local -; set -m; echo "in:$-"; }; g; echo "out:$-"
set -o
%%%
#!name gen1154
#!mode=file
B0=$PWD
set +o
%%%
#!name gen1155
#!mode=file
B0=$PWD
export HISTSIZE='.'
%%%
#!name gen1156
#!mode=file
B0=$PWD
set -- a b c d e; shift 1 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1157
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- -a x -a x -a; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1158
#!mode=file
B0=$PWD
g() { local A='a'b'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1159
#!mode=file
B0=$PWD
trap '' HUP 2>&1; trap; echo rc=$?
set +o
g() { local -; set -n; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1160
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
set -- -a x -a -b -z; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1161
#!mode=file
B0=$PWD
set -- -a -b -z; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1162
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1163
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen1164
#!mode=file
B0=$PWD
unset -v V1
readonly CDPATH='POSIX'
set --; echo "$#"
%%%
#!name gen1165
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1166
#!mode=file
B0=$PWD
export PS4='x=y'
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
alias echo='echo pre'; echo hi
%%%
#!name gen1167
#!mode=file
B0=$PWD
set -- -a --; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export PS2='/tmp'
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1168
#!mode=file
B0=$PWD
g() { local B B; B='a'b'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1169
#!mode=file
B0=$PWD
set -- -a x -ab x; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1170
#!mode=file
B0=$PWD
CDPATH='\t'
echo "[${OPTIND}]"
%%%
#!name gen1171
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
trap 'echo A' USR1 2>&1; trap - USR1 2>&1; trap; echo rc=$?
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1172
#!mode=file
B0=$PWD
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
cat <<-E
	indented
	E
g() { local B='abc'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1173
#!mode=file
B0=$PWD
trap 'echo T' USR2 2>&1; trap; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
env | grep -c '^PWD='
%%%
#!name gen1174
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen1175
#!mode=file
B0=$PWD
set +o xtrace; echo "$-"
%%%
#!name gen1176
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
f() { echo F; }; f >f.txt 2>&1; cat f.txt
trap 'echo X' 0 2>&1; (trap); echo ---; trap
%%%
#!name gen1177
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1178
#!mode=file
B0=$PWD
g() { local OPTIND='x=y'; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1179
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1180
#!mode=file
B0=$PWD
unset -v LC_COLLATE
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1181
#!mode=file
B0=$PWD
trap 'echo X' 0 2>&1; (trap); echo ---; trap
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
g() { local -; set -a; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1182
#!mode=file
B0=$PWD
set -; echo "$-"
%%%
#!name gen1183
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
set | grep -c '^PS4='
%%%
#!name gen1184
#!mode=file
B0=$PWD
trap '' TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1185
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
LINENO=''
trap 'echo A' PIPE 2>&1; trap - PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen1186
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1187
#!mode=file
B0=$PWD
g() { local OPTIND='a'b'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1188
#!mode=file
B0=$PWD
set -- x -b -ab; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export PS4='1'
%%%
#!name gen1189
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
env | grep -c '^MAIL='
%%%
#!name gen1190
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1191
#!mode=file
B0=$PWD
export -p | grep -c 'PATH' 
exec 4>&1; echo x >&4; exec 4>&-; echo y >&4 2>&1; echo rc=$?
%%%
#!name gen1192
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
cat <<E
line1
line2
E
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1193
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen1194
#!mode=file
B0=$PWD
echo "[${vv-U}]"
%%%
#!name gen1195
#!mode=file
B0=$PWD
set -- -ab; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -I; echo "$-"
%%%
#!name gen1196
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1197
#!mode=file
B0=$PWD
set -; echo "$-"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
trap 'echo E' EXIT; echo body
%%%
#!name gen1198
#!mode=file
B0=$PWD
set -- a b c d e; shift 4 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1199
#!mode=file
B0=$PWD
{ echo p; } 9>&- ; echo rc=$?
export vv
%%%
#!name gen1200
#!mode=file
B0=$PWD
set -- -bvalue -a x x; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; a; alias a
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1201
#!mode=file
B0=$PWD
set -- -z -z; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
readonly LC_COLLATE='1'
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1202
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
exec 5>&1; echo x >&5; exec 5>&-; echo y >&5 2>&1; echo rc=$?
{ echo p; } 6>&- ; echo rc=$?
%%%
#!name gen1203
#!mode=file
B0=$PWD
g() { local B B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
set -- -bvalue; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
export -p | grep -c 'MAIL' 
%%%
#!name gen1204
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
g() { local PATH='a'b'; PATH='a'b'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
export -p | grep -c 'PWD' 
%%%
#!name gen1205
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
set -o notify; echo "$-"
%%%
#!name gen1206
#!mode=file
B0=$PWD
set -- -a x x; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1207
#!mode=file
B0=$PWD
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
g() { local OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1208
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
echo "[${HISTSIZE-U}]"
%%%
#!name gen1209
#!mode=file
B0=$PWD
trap '' USR1 2>&1; trap; echo rc=$?
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1210
#!mode=file
B0=$PWD
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
%%%
#!name gen1211
#!mode=file
B0=$PWD
g() { local -; set -I; echo "in:$-"; }; g; echo "out:$-"
g() { local OPTIND B; OPTIND='0'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
trap '' TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1212
#!mode=file
B0=$PWD
export LC_ALL
unset HISTSIZE
set -- -bvalue -a x -a -b; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1213
#!mode=file
B0=$PWD
exec 4>r4.txt; echo hi >&4; exec 4>&-; cat r4.txt
%%%
#!name gen1214
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen1215
#!mode=file
B0=$PWD
g() { local B='a'b'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
cat <<'E'
$notexpanded
E
%%%
#!name gen1216
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1217
#!mode=file
B0=$PWD
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1218
#!mode=file
B0=$PWD
g() { local OPTIND; OPTIND='1'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1219
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1220
#!mode=file
B0=$PWD
set -vn; echo "$-"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1221
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1222
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
read v <<E
value
E
echo "[$v]"
set -- -bvalue; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1223
#!mode=file
B0=$PWD
trap 'echo T' TERM 2>&1; trap; echo rc=$?
g() { local B='abc'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
set -; echo "$-"
%%%
#!name gen1224
#!mode=file
B0=$PWD
set +o
export -p | grep -c 'OPTIND' 
%%%
#!name gen1225
#!mode=file
B0=$PWD
g() { local B; B=':'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
read v <<E
value
E
echo "[$v]"
%%%
#!name gen1226
#!mode=file
B0=$PWD
set +o
set -- -bvalue -a -b -z; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1227
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
echo "[${TERM-U}]"
%%%
#!name gen1228
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
set -- -ab; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1229
#!mode=file
B0=$PWD
set +o
alias echo='echo pre'; echo hi
%%%
#!name gen1230
#!mode=file
B0=$PWD
set -- -bvalue --; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -o monitor; echo "$-"
%%%
#!name gen1231
#!mode=file
B0=$PWD
set -o
trap 'echo E' EXIT; echo body
%%%
#!name gen1232
#!mode=file
B0=$PWD
g() { local IFS='a b'; IFS='a b'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
alias echo='echo pre'; echo hi
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1233
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -- -b -bvalue -ab; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1234
#!mode=file
B0=$PWD
set --; echo "$#"
set | grep -c '^PS1='
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1235
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1236
#!mode=file
B0=$PWD
g() { local B='1'; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1237
#!mode=file
B0=$PWD
set -- -a -b -c; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1238
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1239
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
%%%
#!name gen1240
#!mode=file
B0=$PWD
trap 'echo X' 1 2>&1; (trap); echo ---; trap
trap 'echo A' HUP 2>&1; trap - HUP 2>&1; trap; echo rc=$?
%%%
#!name gen1241
#!mode=file
B0=$PWD
f() { echo F; }; f >f.txt 2>&1; cat f.txt
set -- a b c d e; shift 2 2>&1; echo "rc=$? $#:$*"
set --; echo "$#"
%%%
#!name gen1242
#!mode=file
B0=$PWD
set -- -a x -ab; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -VE; echo "$-"
trap 'echo E' EXIT; echo body
%%%
#!name gen1243
#!mode=file
B0=$PWD
g() { local IFS='\t'; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
readonly LC_NUMERIC='x=y'
%%%
#!name gen1244
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen1245
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1246
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1247
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
alias a='echo A'; a; alias a
%%%
#!name gen1248
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
trap 'echo T' 1 2>&1; trap; echo rc=$?
%%%
#!name gen1249
#!mode=file
B0=$PWD
alias echo='echo pre'; echo hi
%%%
#!name gen1250
#!mode=file
B0=$PWD
export OPTIND
%%%
#!name gen1251
#!mode=file
B0=$PWD
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set +C; echo "$-"
exec 5<<E
fromfd
E
read v <&5; echo "[$v]"; exec 5<&-
%%%
#!name gen1252
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1253
#!mode=file
B0=$PWD
g() { local OPTIND='/tmp'; OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
set -o
%%%
#!name gen1254
#!mode=file
B0=$PWD
trap 'echo X' EXIT 2>&1; (trap); echo ---; trap
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1255
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1256
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
%%%
#!name gen1257
#!mode=file
B0=$PWD
g() { local IFS; IFS='.'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set -; echo "$-"
%%%
#!name gen1258
#!mode=file
B0=$PWD
g() { local B B; echo "[${B-U}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1259
#!mode=file
B0=$PWD
set -o notify; echo "$-"
%%%
#!name gen1260
#!mode=file
B0=$PWD
set -- -a x; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1261
#!mode=file
B0=$PWD
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1262
#!mode=file
B0=$PWD
set -- ; echo "$#:$*"
set -o allexport; echo "$-"
set -- -- -a; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1263
#!mode=file
B0=$PWD
g() { local PATH B; echo "[${PATH-U}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
exec 9<&-; read v <&9 2>&1; echo rc=$?
%%%
#!name gen1264
#!mode=file
B0=$PWD
trap '' CHLD 2>&1; trap; echo rc=$?
set --  1 ; echo "$#:$*"
%%%
#!name gen1265
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
trap TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1266
#!mode=file
B0=$PWD
set +o
alias echo='echo pre'; echo hi
%%%
#!name gen1267
#!mode=file
B0=$PWD
g() { local B; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1268
#!mode=file
B0=$PWD
set -- ; echo "$#:$*"
set --; echo "$#"
trap '' INT 2>&1; trap; echo rc=$?
%%%
#!name gen1269
#!mode=file
B0=$PWD
set +f; echo "$-"
trap 'echo A' CHLD 2>&1; trap - CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen1270
#!mode=file
B0=$PWD
echo "[${PS1-U}]"
cat <<'E'
$notexpanded
E
%%%
#!name gen1271
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1272
#!mode=file
B0=$PWD
g() { local -; set +E; echo "in:$-"; }; g; echo "out:$-"
g() { local IFS; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1273
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1274
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1275
#!mode=file
B0=$PWD
(exec 9>s.txt; echo s >&9); cat s.txt; echo t >&9 2>&1; echo rc=$?
cat <<E
line1
line2
E
%%%
#!name gen1276
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set +o
set -- --; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1277
#!mode=file
B0=$PWD
unset LC_CTYPE
%%%
#!name gen1278
#!mode=file
B0=$PWD
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
alias echo='echo pre'; echo hi
set -- a b c d e; shift 6 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1279
#!mode=file
B0=$PWD
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
g() { local V1 B; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1280
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1281
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
echo "[${HOME-U}]"
trap 'echo E' EXIT; echo body
%%%
#!name gen1282
#!mode=file
B0=$PWD
trap '' 1 2>&1; trap; echo rc=$?
%%%
#!name gen1283
#!mode=file
B0=$PWD
g() { local PATH=':'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -n; echo "$-"
set -- -z; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1284
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1285
#!mode=file
B0=$PWD
exec 6<&-; read v <&6 2>&1; echo rc=$?
%%%
#!name gen1286
#!mode=file
B0=$PWD
trap '' USR1 2>&1; trap; echo rc=$?
%%%
#!name gen1287
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen1288
#!mode=file
B0=$PWD
trap 'echo X' USR1 2>&1; (trap); echo ---; trap
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1289
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
trap 10 2>&1; trap; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1290
#!mode=file
B0=$PWD
g() { local B; B='0'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1291
#!mode=file
B0=$PWD
{ echo p; } 9>&- ; echo rc=$?
%%%
#!name gen1292
#!mode=file
B0=$PWD
g() { local OPTIND; echo "[${OPTIND-U}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1293
#!mode=file
B0=$PWD
set --; echo "$#"
PS2='"q"'
set -- -a -b -a -b x; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1294
#!mode=file
B0=$PWD
g() { local A B; A='0'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set +I; echo "$-"
%%%
#!name gen1295
#!mode=file
B0=$PWD
trap 'echo T' USR1 2>&1; trap; echo rc=$?
alias 'a b'=1 2>&1; echo rc=$?
set -- b a; echo "$#:$*"
%%%
#!name gen1296
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
(exec 4>s.txt; echo s >&4); cat s.txt; echo t >&4 2>&1; echo rc=$?
%%%
#!name gen1297
#!mode=file
B0=$PWD
trap TERM 2>&1; trap; echo rc=$?
cd /nope 2>&1; echo rc=$?
%%%
#!name gen1298
#!mode=file
B0=$PWD
set -- -a; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -; echo "$-"
%%%
#!name gen1299
#!mode=file
B0=$PWD
export -p | grep -c 'LINENO' 
set --; echo "$#"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1300
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1301
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen1302
#!mode=file
B0=$PWD
set -o pipefail; echo "$-"
export -p | grep -c 'vv' 
%%%
#!name gen1303
#!mode=file
B0=$PWD
set -- x -b -a; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1304
#!mode=file
B0=$PWD
trap 'echo T' USR2 2>&1; trap; echo rc=$?
g() { local V1='\t'; V1='\t'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen1305
#!mode=file
B0=$PWD
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1306
#!mode=file
B0=$PWD
set -; echo "$-"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1307
#!mode=file
B0=$PWD
trap '' USR1 2>&1; trap; echo rc=$?
g() { local -; set -o monitor; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1308
#!mode=file
B0=$PWD
set +o
%%%
#!name gen1309
#!mode=file
B0=$PWD
{ echo p; } 9>&- ; echo rc=$?
trap 'echo T' USR2 2>&1; trap; echo rc=$?
%%%
#!name gen1310
#!mode=file
B0=$PWD
set -- -- -c -a; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set +m; echo "$-"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1311
#!mode=file
B0=$PWD
set -- -a x -a x -a; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1312
#!mode=file
B0=$PWD
g() { local V1='C'; V1='C'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
g() { local -; set -o nolog; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1313
#!mode=file
B0=$PWD
unset -v IFS
set -; echo "$-"
%%%
#!name gen1314
#!mode=file
B0=$PWD
g() { local OPTIND B; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1315
#!mode=file
B0=$PWD
g() { local V1; V1=''; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
trap 'echo E' EXIT; echo body
%%%
#!name gen1316
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1317
#!mode=file
B0=$PWD
set -- -a -ab -z; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1318
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
trap 10 2>&1; trap; echo rc=$?
export IFS
%%%
#!name gen1319
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
set -- -a x -b -a x; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1320
#!mode=file
B0=$PWD
cat <<-E
	indented
	E
%%%
#!name gen1321
#!mode=file
B0=$PWD
export MAIL='1'
trap 'echo X' USR2 2>&1; (trap); echo ---; trap
%%%
#!name gen1322
#!mode=file
B0=$PWD
set -- -ab; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<-E
	indented
	E
%%%
#!name gen1323
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1324
#!mode=file
B0=$PWD
g() { local -; set -C; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1325
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
export LC_ALL='POSIX'
set -- -a; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1326
#!mode=file
B0=$PWD
trap 'echo A' USR1 2>&1; trap - USR1 2>&1; trap; echo rc=$?
set +v; echo "$-"
%%%
#!name gen1327
#!mode=file
B0=$PWD
set -- -c -a -b; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1328
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
export LC_COLLATE
trap 12 2>&1; trap; echo rc=$?
%%%
#!name gen1329
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
readonly TERM='\t'
%%%
#!name gen1330
#!mode=file
B0=$PWD
set -i; echo "$-"
set +o ignoreeof; echo "$-"
%%%
#!name gen1331
#!mode=file
B0=$PWD
env | grep -c '^CDPATH='
%%%
#!name gen1332
#!mode=file
B0=$PWD
set +C; echo "$-"
readonly PS4='abc'
%%%
#!name gen1333
#!mode=file
B0=$PWD
LINENO='POSIX'
set -- -bvalue; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local OPTIND=''; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1334
#!mode=file
B0=$PWD
set +o emacs; echo "$-"
%%%
#!name gen1335
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
%%%
#!name gen1336
#!mode=file
B0=$PWD
echo "[${PATH}]"
%%%
#!name gen1337
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1338
#!mode=file
B0=$PWD
trap 'echo X' QUIT 2>&1; (trap); echo ---; trap
set -- -- -z; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1339
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1340
#!mode=file
B0=$PWD
g() { local vv='C'; vv='C'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
set -ni; echo "$-"
%%%
#!name gen1341
#!mode=file
B0=$PWD
set -- x -b --; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1342
#!mode=file
B0=$PWD
set -fe; echo "$-"
g() { local PATH B; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
alias echo='echo pre'; echo hi
%%%
#!name gen1343
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1344
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
cat <<-E
	indented
	E
readonly LC_COLLATE='.'
%%%
#!name gen1345
#!mode=file
B0=$PWD
set -- -c -a x; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1346
#!mode=file
B0=$PWD
trap 10 2>&1; trap; echo rc=$?
set -- 1; echo "$#:$*"
trap 'echo X' INT 2>&1; (trap); echo ---; trap
%%%
#!name gen1347
#!mode=file
B0=$PWD
g() { local PATH='a'b'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -- -a x; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1348
#!mode=file
B0=$PWD
trap TERM 2>&1; trap; echo rc=$?
%%%
#!name gen1349
#!mode=file
B0=$PWD
set -o noclobber; echo "$-"
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
set -- -a; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1350
#!mode=file
B0=$PWD
set -- -c; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo E' EXIT; echo body
set -o noglob; echo "$-"
%%%
#!name gen1351
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- -a x --; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1352
#!mode=file
B0=$PWD
export LANG
g() { local vv B; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1353
#!mode=file
B0=$PWD
g() { local -; set -E; echo "in:$-"; }; g; echo "out:$-"
trap 'echo T' 0 2>&1; trap; echo rc=$?
%%%
#!name gen1354
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -- -c; OPTIND=3; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
IFS='abc'
%%%
#!name gen1355
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1356
#!mode=file
B0=$PWD
set -- -a x -- -b; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1357
#!mode=file
B0=$PWD
set -- a b c d e; shift 3 2>&1; echo "rc=$? $#:$*"
%%%
#!name gen1358
#!mode=file
B0=$PWD
set +o vi; echo "$-"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1359
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
trap '' 10 2>&1; trap; echo rc=$?
%%%
#!name gen1360
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
%%%
#!name gen1361
#!mode=file
B0=$PWD
echo "[${LANG-U}]"
set -; echo "$-"
%%%
#!name gen1362
#!mode=file
B0=$PWD
trap 'echo X' ALRM 2>&1; (trap); echo ---; trap
g() { local PATH='POSIX'; PATH='POSIX'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
g() { local A; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1363
#!mode=file
B0=$PWD
set -- -- -b -bvalue; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
cat <<-E
	indented
	E
g() { local A B; echo "[${A-U}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
%%%
#!name gen1364
#!mode=file
B0=$PWD
set | grep -c '^MAILPATH='
%%%
#!name gen1365
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
cd /nope 2>&1; echo rc=$?
set +o verbose; echo "$-"
%%%
#!name gen1366
#!mode=file
B0=$PWD
set --; echo "$#"
set -- a b c d e; shift 5 2>&1; echo "rc=$? $#:$*"
trap 'echo A' CHLD 2>&1; trap - CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen1367
#!mode=file
B0=$PWD
trap ALRM 2>&1; trap; echo rc=$?
set -- -b -b -ab; unset OPTIND; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1368
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
%%%
#!name gen1369
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set -- -c -b --; OPTIND=3; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1370
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
unset PS4
%%%
#!name gen1371
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1372
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1373
#!mode=file
B0=$PWD
g() { local OPTIND='0'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set --; echo "$#"
export -p | grep -c 'OLDPWD' 
%%%
#!name gen1374
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
%%%
#!name gen1375
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
set -- 1 ; echo "$#:$*"
%%%
#!name gen1376
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
alias a='b'; alias b='echo B'; a
%%%
#!name gen1377
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
cat <<-E
	indented
	E
%%%
#!name gen1378
#!mode=file
B0=$PWD
trap 'echo E' EXIT; echo body
%%%
#!name gen1379
#!mode=file
B0=$PWD
unset PS1
g() { local OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
set +n; echo "$-"
%%%
#!name gen1380
#!mode=file
B0=$PWD
export A
g() { local -; set +v; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1381
#!mode=file
B0=$PWD
alias a='echo A'; a; alias a
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1382
#!mode=file
B0=$PWD
set +o allexport; echo "$-"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1383
#!mode=file
B0=$PWD
trap 'echo X' CHLD 2>&1; (trap); echo ---; trap
set -- -ab -a x -a x; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias echo='echo pre'; echo hi
%%%
#!name gen1384
#!mode=file
B0=$PWD
unset -v OLDPWD
g() { local PATH; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
%%%
#!name gen1385
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
g() { local IFS='0'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1386
#!mode=file
B0=$PWD
set -- -ab -a -b; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1387
#!mode=file
B0=$PWD
cd /nope 2>&1; echo rc=$?
set -C; echo "$-"
set --; echo "$#"
%%%
#!name gen1388
#!mode=file
B0=$PWD
set --; echo "$#"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1389
#!mode=file
B0=$PWD
trap 'echo X' USR2 2>&1; (trap); echo ---; trap
alias a='b'; alias b='echo B'; a
set -- -a -b --; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1390
#!mode=file
B0=$PWD
set -- -- x -a -b; unset OPTIND; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
(trap 'echo SE' EXIT; echo sub); echo main
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
%%%
#!name gen1391
#!mode=file
B0=$PWD
cat <<'E'
$notexpanded
E
set -C; echo "$-"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
%%%
#!name gen1392
#!mode=file
B0=$PWD
trap 'echo X' HUP 2>&1; (trap); echo ---; trap
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1393
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
trap 'echo A' QUIT 2>&1; trap - QUIT 2>&1; trap; echo rc=$?
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1394
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1395
#!mode=file
B0=$PWD
g() { local -; set +i; echo "in:$-"; }; g; echo "out:$-"
g() { local A='1'; A='1'; echo "[${A}]"; }; A=outer 2>/dev/null; g; echo "[${A-U}]"
set -an; echo "$-"
%%%
#!name gen1396
#!mode=file
B0=$PWD
g() { local OPTIND='0'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1397
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
%%%
#!name gen1398
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
g() { local V1 B; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1399
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
g() { local IFS B; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1400
#!mode=file
B0=$PWD
LC_NUMERIC='/tmp'
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1401
#!mode=file
B0=$PWD
g() { local PATH='x=y'; PATH='x=y'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -- -c -z; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
trap 'echo A' HUP 2>&1; trap - HUP 2>&1; trap; echo rc=$?
%%%
#!name gen1402
#!mode=file
B0=$PWD
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1403
#!mode=file
B0=$PWD
set -; echo "$-"
trap 'echo T' CHLD 2>&1; trap; echo rc=$?
%%%
#!name gen1404
#!mode=file
B0=$PWD
g() { local IFS; IFS=''; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1405
#!mode=file
B0=$PWD
g() { local V1; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
g() { local V1; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1406
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
trap USR1 2>&1; trap; echo rc=$?
set -- -bvalue -b -c; unset OPTIND; OPTIND=1; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1407
#!mode=file
B0=$PWD
export -p | grep -c 'PATH' 
%%%
#!name gen1408
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1409
#!mode=file
B0=$PWD
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
alias a='b'; alias b='echo B'; a
%%%
#!name gen1410
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
read v <<E
value
E
echo "[$v]"
exec 9>&1; echo x >&9; exec 9>&-; echo y >&9 2>&1; echo rc=$?
%%%
#!name gen1411
#!mode=file
B0=$PWD
trap 'echo A' PIPE 2>&1; trap - PIPE 2>&1; trap; echo rc=$?
unset -v PPID
trap 'echo A' 12 2>&1; trap - 12 2>&1; trap; echo rc=$?
%%%
#!name gen1412
#!mode=file
B0=$PWD
{ echo p; } 6>&- ; echo rc=$?
trap 'echo T' PIPE 2>&1; trap; echo rc=$?
%%%
#!name gen1413
#!mode=file
B0=$PWD
g() { local B; B='a'b'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
(trap 'echo SE' EXIT; echo sub); echo main
set -- x -ab; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1414
#!mode=file
B0=$PWD
set -- ; echo "$#:$*"
%%%
#!name gen1415
#!mode=file
B0=$PWD
set -- -- -- -b; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; a; alias a
%%%
#!name gen1416
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
alias a='b '; alias b='echo B'; alias c='echo C'; a c
set -- 1 a; echo "$#:$*"
%%%
#!name gen1417
#!mode=file
B0=$PWD
g() { local PATH; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1418
#!mode=file
B0=$PWD
set -e; echo "$-"
%%%
#!name gen1419
#!mode=file
B0=$PWD
unset -v V1
set -- ; echo "$#:$*"
%%%
#!name gen1420
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
%%%
#!name gen1421
#!mode=file
B0=$PWD
set +o
alias a='echo A'; a; alias a
set -Ve; echo "$-"
%%%
#!name gen1422
#!mode=file
B0=$PWD
set -- -c -z x; unset OPTIND; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
(trap 'echo SE' EXIT; echo sub); echo main
set -- 1 ; echo "$#:$*"
%%%
#!name gen1423
#!mode=file
B0=$PWD
set -- -bvalue -c x; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='b '; alias b='echo B'; alias c='echo C'; a c
%%%
#!name gen1424
#!mode=file
B0=$PWD
set -- -bvalue -z -ab; OPTIND=3; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1425
#!mode=file
B0=$PWD
set +o xtrace; echo "$-"
mkdir -p cp/t; CDPATH=./cp; cd t >/dev/null 2>&1; echo "rc=$? ${PWD#$B0}"
%%%
#!name gen1426
#!mode=file
B0=$PWD
cat <<E
line1
line2
E
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
trap 'echo A' EXIT 2>&1; trap - EXIT 2>&1; trap; echo rc=$?
%%%
#!name gen1427
#!mode=file
B0=$PWD
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- -z; OPTIND=1; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1428
#!mode=file
B0=$PWD
trap USR1 2>&1; trap; echo rc=$?
%%%
#!name gen1429
#!mode=file
B0=$PWD
env | grep -c '^OPTIND='
%%%
#!name gen1430
#!mode=file
B0=$PWD
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1431
#!mode=file
B0=$PWD
alias a='b'; alias b='echo B'; a
read v <<E
value
E
echo "[$v]"
LC_NUMERIC='a'b'
%%%
#!name gen1432
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
cat <<E
line1
line2
E
%%%
#!name gen1433
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
set -- -a x -a -a -b; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1434
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
(trap 'echo SE' EXIT; echo sub); echo main
%%%
#!name gen1435
#!mode=file
B0=$PWD
set -- -a -ab; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1436
#!mode=file
B0=$PWD
set -- a; echo "$#:$*"
%%%
#!name gen1437
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
mkdir -p c1/c2; cd c1/c2; echo "${PWD#$B0}"; cd ../..; echo "R${PWD#$B0}"
%%%
#!name gen1438
#!mode=file
B0=$PWD
g() { local PATH B; PATH='a'b'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
export PS4
%%%
#!name gen1439
#!mode=file
B0=$PWD
g() { local V1='1'; echo "[${V1-U}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1440
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
cat <<E
line1
line2
E
%%%
#!name gen1441
#!mode=file
B0=$PWD
g() { local -; set -o nolog; echo "in:$-"; }; g; echo "out:$-"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1442
#!mode=file
B0=$PWD
alias a='x'; unalias a; unalias a 2>&1; echo rc=$?
%%%
#!name gen1443
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen1444
#!mode=file
B0=$PWD
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
exec 9<<E
fromfd
E
read v <&9; echo "[$v]"; exec 9<&-
%%%
#!name gen1445
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
OLDPWD='POSIX'
f() { echo F; }; f >f.txt 2>&1; cat f.txt
%%%
#!name gen1446
#!mode=file
B0=$PWD
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
unset HOME
alias a='b'; alias b='echo B'; a
%%%
#!name gen1447
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
g() { local -; set -o vi; echo "in:$-"; }; g; echo "out:$-"
set -; echo "$-"
%%%
#!name gen1448
#!mode=file
B0=$PWD
set -- -a x -ab -b; unset OPTIND; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
env | grep -c '^CDPATH='
%%%
#!name gen1449
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
trap ALRM 2>&1; trap; echo rc=$?
%%%
#!name gen1450
#!mode=file
B0=$PWD
mkdir -p l1; ln -s l1 s1; cd -L s1; echo "${PWD#$B0}"; cd ..; echo "R${PWD#$B0}"
%%%
#!name gen1451
#!mode=file
B0=$PWD
trap 'echo A' 0 2>&1; trap - 0 2>&1; trap; echo rc=$?
set -- b a; echo "$#:$*"
g() { local vv B; vv='/tmp'; echo "[${vv}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1452
#!mode=file
B0=$PWD
trap 'echo A' INT 2>&1; trap - INT 2>&1; trap; echo rc=$?
g() { local V1 B; V1='"q"'; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1453
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
set -uv; echo "$-"
g() { local -; set +i; echo "in:$-"; }; g; echo "out:$-"
%%%
#!name gen1454
#!mode=file
B0=$PWD
echo "[${LANG}]"
export -p | grep -c 'LC_ALL' 
%%%
#!name gen1455
#!mode=file
B0=$PWD
trap 'echo T' 1 2>&1; trap; echo rc=$?
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
mkdir -p aa/bb; cd aa/../aa/bb; echo "${PWD#$B0}"
%%%
#!name gen1456
#!mode=file
B0=$PWD
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
alias a='echo A'; a; alias a
set -- x -a -b -bvalue; unset OPTIND; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1457
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } 2>&1 > o.txt; cat o.txt
set +a; echo "$-"
%%%
#!name gen1458
#!mode=file
B0=$PWD
unset PS4
g() { local -; set -v; echo "in:$-"; }; g; echo "out:$-"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1459
#!mode=file
B0=$PWD
set -- -a x; unset OPTIND; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1460
#!mode=file
B0=$PWD
set -- -c; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1461
#!mode=file
B0=$PWD
read v <<E
value
E
echo "[$v]"
g() { local IFS; IFS='C'; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
g() { local vv; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
%%%
#!name gen1462
#!mode=file
B0=$PWD
set -- x; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1463
#!mode=file
B0=$PWD
trap 0 2>&1; trap; echo rc=$?
exec 6>&1; echo x >&6; exec 6>&-; echo y >&6 2>&1; echo rc=$?
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1464
#!mode=file
B0=$PWD
set -- -a -b; unset OPTIND; OPTIND=1; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
g() { local PATH; PATH='x=y'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1465
#!mode=file
B0=$PWD
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- -z -c -a; unset OPTIND; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
set -nb; echo "$-"
%%%
#!name gen1466
#!mode=file
B0=$PWD
set -; echo "$-"
alias 'a b'=1 2>&1; echo rc=$?
%%%
#!name gen1467
#!mode=file
B0=$PWD
set -e; echo "$-"
%%%
#!name gen1468
#!mode=file
B0=$PWD
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
mkdir -p q; cd q; cd - >/dev/null; echo "R${PWD#$B0}"
g() { local OPTIND='/tmp'; echo "[${OPTIND}]"; }; OPTIND=outer 2>/dev/null; g; echo "[${OPTIND-U}]"
%%%
#!name gen1469
#!mode=file
B0=$PWD
set | grep -c '^IFS='
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1470
#!mode=file
B0=$PWD
trap 'echo A' ALRM 2>&1; trap - ALRM 2>&1; trap; echo rc=$?
%%%
#!name gen1471
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
set | grep -c '^OLDPWD='
%%%
#!name gen1472
#!mode=file
B0=$PWD
g() { local PATH='1'; echo "[${PATH}]"; }; PATH=outer 2>/dev/null; g; echo "[${PATH-U}]"
set -- -bvalue -- -a; while getopts :a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1473
#!mode=file
B0=$PWD
set | grep -c '^A='
%%%
#!name gen1474
#!mode=file
B0=$PWD
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
alias a='echo A'; a; alias a
%%%
#!name gen1475
#!mode=file
B0=$PWD
trap '' USR1 2>&1; trap; echo rc=$?
exec 4<<E
fromfd
E
read v <&4; echo "[$v]"; exec 4<&-
%%%
#!name gen1476
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
alias a='unalias a; echo A'; a; a 2>&1; echo rc=$?
set -- x; OPTIND=1; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1477
#!mode=file
B0=$PWD
g() { local vv='POSIX'; echo "[${vv-U}]"; }; vv=outer 2>/dev/null; g; echo "[${vv-U}]"
g() { local B='\t'; B='\t'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1478
#!mode=file
B0=$PWD
trap 12 2>&1; trap; echo rc=$?
%%%
#!name gen1479
#!mode=file
B0=$PWD
LC_CTYPE=':'
{ echo a; echo b >&2; } > o.txt 2>&1; cat o.txt
set -- -bvalue; OPTIND=1; while getopts a:b o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1480
#!mode=file
B0=$PWD
echo "[${PWD#$B0}]"
%%%
#!name gen1481
#!mode=file
B0=$PWD
set -- -c -ab -bvalue; OPTIND=3; while getopts abc o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1482
#!mode=file
B0=$PWD
set --; echo "$#"
%%%
#!name gen1483
#!mode=file
B0=$PWD
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1484
#!mode=file
B0=$PWD
set -- -a -b; unset OPTIND; OPTIND=1; while getopts :ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
echo z > nz.txt; echo y >> nz.txt; cat nz.txt
set -- -a x; OPTIND=3; while getopts :a:bc: o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
%%%
#!name gen1485
#!mode=file
B0=$PWD
mkdir -p l2; ln -s l2 s2; cd -P s2; echo "${PWD#$B0}"
%%%
#!name gen1486
#!mode=file
B0=$PWD
alias 'a b'=1 2>&1; echo rc=$?
pwd -L > /dev/null; pwd -P > /dev/null; echo pwdok
%%%
#!name gen1487
#!mode=file
B0=$PWD
echo "[${PWD#$B0}]"
unset LC_COLLATE
%%%
#!name gen1488
#!mode=file
B0=$PWD
exec 3<<E
fromfd
E
read v <&3; echo "[$v]"; exec 3<&-
env | grep -c '^PPID='
alias a='echo A'; a; alias a
%%%
#!name gen1489
#!mode=file
B0=$PWD
cd .; echo "R${PWD#$B0}"; cd ./.; echo "R${PWD#$B0}"
set -C; echo a > nc.txt; echo b > nc.txt 2>&1; echo rc=$?; cat nc.txt
%%%
#!name gen1490
#!mode=file
B0=$PWD
unset PWD
set -E; echo "$-"
%%%
#!name gen1491
#!mode=file
B0=$PWD
set +o xtrace; echo "$-"
g() { local V1=''; echo "[${V1}]"; }; V1=outer 2>/dev/null; g; echo "[${V1-U}]"
%%%
#!name gen1492
#!mode=file
B0=$PWD
set -- -a x --; while getopts ab o 2>&1; do echo "$o|${OPTARG-U}|$OPTIND"; done; echo "end $OPTIND"
alias a='echo A'; f() { a; }; f; unalias a; f 2>&1; echo rc=$?
%%%
#!name gen1493
#!mode=file
B0=$PWD
set +f; echo "$-"
%%%
#!name gen1494
#!mode=file
B0=$PWD
set -o
alias a='echo A'; a; alias a
alias a=1 b=2 c=3; unalias -a; alias; echo rc=$?
%%%
#!name gen1495
#!mode=file
B0=$PWD
(trap 'echo SE' EXIT; echo sub); echo main
unset CDPATH; mkdir -p dd; cd dd; echo "${PWD#$B0}"
%%%
#!name gen1496
#!mode=file
B0=$PWD
g() { local IFS B; echo "[${IFS}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
g() { local -; set +f; echo "in:$-"; }; g; echo "out:$-"
export -p | grep -c 'PWD' 
%%%
#!name gen1497
#!mode=file
B0=$PWD
export PS2='\t'
g() { local -; set -e; echo "in:$-"; }; g; echo "out:$-"
g() { local IFS; echo "[${IFS-U}]"; }; IFS=outer 2>/dev/null; g; echo "[${IFS-U}]"
%%%
#!name gen1498
#!mode=file
B0=$PWD
g() { local B; B='C'; echo "[${B}]"; }; B=outer 2>/dev/null; g; echo "[${B-U}]"
%%%
#!name gen1499
#!mode=file
B0=$PWD
(exec 6>s.txt; echo s >&6); cat s.txt; echo t >&6 2>&1; echo rc=$?
echo "[${PS1}]"
mkdir -p o1 o2; cd o1; cd ../o2; echo "${OLDPWD#$B0}|${PWD#$B0}"
